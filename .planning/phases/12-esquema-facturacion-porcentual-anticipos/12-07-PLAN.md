---
phase: 12-esquema-facturacion-porcentual-anticipos
plan: "07"
type: gap_closure
gap_closure: true
autonomous: true
wave: 7
depends_on: [12-06]
subsystem: billing
tags: [react, typescript, presupuestos, facturacion, cuotas, create-flow, pdf, BILL-01, BILL-08]
---

# Phase 12 Plan 07: Gap Closure — EsquemaFacturacion in Create Flow

## Objective

Close the primary gap identified in 12-VERIFICATION.md: wire `EsquemaFacturacionSection` into `CreatePresupuestoModal` (for `tipo !== 'contrato'`), suppress the legacy `PresupuestoCuotasSection` for non-contrato types in both create and edit flows, and update `PresupuestoPDFEstandar` to read from `esquemaFacturacion` when present (with fallback to legacy `cuotas[]` for contrato).

## Context

- @.planning/phases/12-esquema-facturacion-porcentual-anticipos/12-VERIFICATION.md — primary gap description
- @apps/sistema-modular/src/components/presupuestos/CreatePresupuestoModal.tsx — primary target
- @apps/sistema-modular/src/hooks/useCreatePresupuestoForm.ts — hook to extend
- @apps/sistema-modular/src/hooks/usePresupuestoEdit.ts — BILL-01 pattern to replicate
- @apps/sistema-modular/src/components/presupuestos/EditPresupuestoModal.tsx — guard PresupuestoCuotasSection
- @apps/sistema-modular/src/components/presupuestos/pdf/PresupuestoPDFEstandar.tsx — PDF gap to fix

## Tasks

### Task 1 — Wire esquemaFacturacion to useCreatePresupuestoForm
type: auto

- Add `PresupuestoCuotaFacturacion` import from `@ags/shared` and `MonedaCuota` import
- Import `validateEsquemaSum`, `findEmptyCuotas` from utils/cuotasFacturacion
- Add `esquemaFacturacion: PresupuestoCuotaFacturacion[]` state via `useState<PresupuestoCuotaFacturacion[]>([])`
- In `handleClose`: reset `setEsquemaFacturacion([])`
- In `handleSave`: add BILL-01 validation (if `tipo !== 'contrato'` AND `esquemaFacturacion.length > 0`, call `validateEsquemaSum` and `findEmptyCuotas` — abort with alert on errors)
- In `handleSave` data payload: spread-conditionally add `esquemaFacturacion` for non-contrato (alongside existing `cuotas` spread — NOT inside it)
- Return `esquemaFacturacion` and `setEsquemaFacturacion` from the hook

done criteria:
- Hook exports `esquemaFacturacion` and `setEsquemaFacturacion`
- `handleSave` validates schema before persisting when non-contrato + non-empty
- Schema included in Firestore payload on save
- No TypeScript errors on the hook

### Task 2 — Render EsquemaFacturacionSection in CreatePresupuestoModal
type: auto

- Import `EsquemaFacturacionSection` from `./EsquemaFacturacionSection`
- Remove import of `PresupuestoCuotasSection` (or keep if contrato renders it — keep it since contrato uses it)
- Compute `monedasActivas` inline (same as EditPresupuestoModal line 116-123 pattern)
- For `tipo !== 'contrato'`: render `EsquemaFacturacionSection` with props `esquema={h.esquemaFacturacion}` `moneda={h.form.moneda}` `itemsForTotals={h.items}` `readOnly={false}` `onChange={h.setEsquemaFacturacion}`
- For `tipo === 'contrato'`: keep the existing `PresupuestoCuotasSection` block
- Remove the unconditional `PresupuestoCuotasSection` render that was there before for all types

done criteria:
- CreatePresupuestoModal renders EsquemaFacturacionSection for non-contrato tipos
- CreatePresupuestoModal renders PresupuestoCuotasSection only for contrato
- `data-testid="esquema-section"` visible in the DOM for non-contrato (comes from EsquemaFacturacionSection)
- No TypeScript errors on the modal

### Task 3 — Guard PresupuestoCuotasSection in EditPresupuestoModal with tipo === 'contrato'
type: auto

- Locate the `PresupuestoCuotasSection` render block around line 370-379 in EditPresupuestoModal.tsx
- Wrap with `{form.tipo === 'contrato' && ( ... )}`
- Leave EsquemaFacturacionSection block (guarded by `tipo !== 'contrato'`) unchanged
- Verify no TypeScript errors on the modal

done criteria:
- EditPresupuestoModal renders PresupuestoCuotasSection only for tipo==='contrato'
- Non-contrato presupuestos no longer show the legacy contrato cuotas planner
- No TypeScript errors

### Task 4 — PresupuestoPDFEstandar.tsx: read esquemaFacturacion when present
type: auto

- Add imports: `PresupuestoCuotaFacturacion`, `MonedaCuota` from `@ags/shared`; `computeTotalsByCurrency` from `utils/cuotasFacturacion` (lazy import to avoid bundling React hooks — use import at top level, it's a pure function)
- Find the "Plan de cuotas" block at lines ~411-433
- Add branch: if `presupuesto.esquemaFacturacion?.length > 0 && presupuesto.tipo !== 'contrato'`:
  - Render new section "Esquema de facturación (% por cuota)"
  - For each `PresupuestoCuotaFacturacion`: show numero, descripcion, hito (map hito to Spanish label), % per moneda, monto preview per moneda
  - Match Editorial Teal visual style (same header/table structure as existing cuotas section)
- Else: fallback to existing `presupuesto.cuotas` rendering (covers contrato + legacy)
- If file grows >250 lines: extract `PdfEsquemaFacturacionSection` as a colocated helper component in `pdf/`
- No React hooks in PDF tree (pure rendering only — verified OK since computeTotalsByCurrency is a pure function)

done criteria:
- PDF shows esquema when `esquemaFacturacion?.length > 0 && tipo !== 'contrato'`
- Legacy cuotas[] rendering preserved as fallback (contrato + legacy pptos)
- File ≤250 lines (or extracted to PdfEsquemaFacturacionSection.tsx)
- No TypeScript errors

### Task 5 — usePresupuestoEdit consistency check
type: auto

- Read usePresupuestoEdit.save() to check if it writes `cuotas` for non-contrato types
- If `cuotas` is written unconditionally: suppress it for non-contrato (non-contrato source of truth is now `esquemaFacturacion`)
- If `cuotas` is NOT written for non-contrato: note in SUMMARY as "no action needed"

done criteria:
- Non-contrato presupuestos do not have redundant `cuotas` write in save()
- Contrato write path (cantidadCuotasPorMoneda) preserved
- Document outcome in SUMMARY

### Task 6 — Build + typecheck verification
type: auto

- Run `pnpm --filter sistema-modular type-check` — must be clean for modified files
- Run `pnpm --filter sistema-modular build` — must succeed
- If failures: fix root cause and re-run; do NOT silence with `as any`

done criteria:
- type-check exits 0 (or pre-existing errors not introduced by this plan)
- build exits 0
- No new TypeScript errors from the 12-07 changes

## Verification

- All 6 tasks committed individually
- CreatePresupuestoModal shows EsquemaFacturacionSection for non-contrato
- EditPresupuestoModal shows PresupuestoCuotasSection only for contrato
- PresupuestoPDFEstandar reads esquemaFacturacion when present
- BILL-01 validation in create flow
- type-check + build green

## Success Criteria

- CreatePresupuestoModal renders EsquemaFacturacionSection for non-contrato; PresupuestoCuotasSection only for contrato
- EditPresupuestoModal renders PresupuestoCuotasSection only for contrato (no longer parallel to esquema for non-contrato)
- PresupuestoPDFEstandar reads esquemaFacturacion when present (non-contrato), legacy cuotas[] fallback preserved
- BILL-01 validation enforced in create flow (no save with Σ%≠100 when esquema non-empty)
- type-check + build green
- No edits inside apps/reportes-ot/ (frozen surface)
- PresupuestoCuotasSection.tsx NOT deleted (still used by contrato)
