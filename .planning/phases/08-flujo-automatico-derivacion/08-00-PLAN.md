---
phase: 08-flujo-automatico-derivacion
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/sistema-modular/e2e/helpers/firestore-assert.ts
  - apps/sistema-modular/e2e/circuits/12-pending-actions-retry.spec.ts
  - apps/sistema-modular/e2e/circuits/13-oc-cliente-flow.spec.ts
  - apps/sistema-modular/e2e/circuits/11-full-business-cycle.spec.ts
  - apps/sistema-modular/e2e/circuits/10-smoke-all-pages.spec.ts
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-04, FLOW-06, FLOW-07]
must_haves:
  truths:
    - "Specs E2E para FLOW-01 edge, FLOW-02 carga OC, FLOW-04 mailQueue y FLOW-06 dashboard existen y fallan (RED) antes de Wave 1"
    - "Helper firestore-assert.ts puede leer docs via Admin SDK para validar pendingActions[] y mailQueue"
    - "Smoke suite incluye /admin/config-flujos y /admin/acciones-pendientes"
  artifacts:
    - path: "apps/sistema-modular/e2e/helpers/firestore-assert.ts"
      provides: "Admin SDK read helpers (getPendingActions, getMailQueueDocs, getOCCliente)"
    - path: "apps/sistema-modular/e2e/circuits/12-pending-actions-retry.spec.ts"
      provides: "FLOW-01 edge + FLOW-06 dashboard E2E"
    - path: "apps/sistema-modular/e2e/circuits/13-oc-cliente-flow.spec.ts"
      provides: "FLOW-02 carga OC + N:M E2E"
  key_links:
    - from: "12-pending-actions-retry.spec.ts"
      to: "e2e/helpers/firestore-assert.ts"
      via: "import { getPendingActions } from '../helpers/firestore-assert'"
      pattern: "helpers/firestore-assert"
    - from: "13-oc-cliente-flow.spec.ts"
      to: "e2e/helpers/firestore-assert.ts"
      via: "import { getOCCliente, getTicketEstado }"
      pattern: "helpers/firestore-assert"
---

<objective>
Crear specs E2E y helpers de assertion contra Firestore para validar los flujos de Phase 8 ANTES de escribir el código de producción. Esto sigue la regla Nyquist: cada verify de Wave 1-3 debe tener un automated command que referencie un spec existente.

Purpose: VALIDATION.md marca Wave 0 como blocker absoluto — sin specs, los plans siguientes no pueden cerrar sus `<verify><automated>` blocks con algo real. Wave 0 produce el "test scaffolding" que los executors ejecutarán al completar cada task.

Output:
- `firestore-assert.ts` helper con funciones tipadas para leer docs via Admin SDK (o cliente si Admin no está wired aún)
- 2 specs nuevos (`12-pending-actions-retry`, `13-oc-cliente-flow`)
- 2 specs extendidos (`11-full-business-cycle`, `10-smoke-all-pages`)

Los specs fallan ahora (RED) porque los flujos no están implementados. Al terminar Wave 3, todos deben pasar (GREEN).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./.planning/PROJECT.md
@./.planning/ROADMAP.md
@./.planning/STATE.md
@./.planning/phases/08-flujo-automatico-derivacion/08-CONTEXT.md
@./.planning/phases/08-flujo-automatico-derivacion/08-RESEARCH.md
@./.planning/phases/08-flujo-automatico-derivacion/08-VALIDATION.md

@./apps/sistema-modular/e2e/circuits/11-full-business-cycle.spec.ts
@./apps/sistema-modular/e2e/circuits/10-smoke-all-pages.spec.ts
@./apps/sistema-modular/e2e/fixtures
@./apps/sistema-modular/e2e/global-setup.ts
@./apps/sistema-modular/playwright.config.ts

<interfaces>
<!-- Keys interfaces y naming locked by CONTEXT/RESEARCH que los specs deben reflejar. -->

From packages/shared/src/types/index.ts (a agregar en Wave 1 por plan 08-01):
```typescript
// Ticket state (agregar 'oc_recibida' en Wave 1)
type TicketEstado = /* existing 15 */ | 'oc_recibida';

// Nuevo tipo (agregar en Wave 1 por 08-01)
interface PendingAction {
  id: string;
  type: 'crear_ticket_seguimiento' | 'derivar_comex' | 'enviar_mail_facturacion' | 'notificar_coordinador_ot';
  reason: string;
  createdAt: string;       // ISO
  resolvedAt?: string;     // ISO
  attempts: number;
}

// Nuevo shape (agregar en Wave 1 por 08-01)
interface OrdenCompraCliente {
  id: string;
  numero: string;
  fecha: string;
  clienteId: string;
  presupuestosIds: string[];
  adjuntos: Array<{ id: string; url: string; tipo: 'pdf' | 'jpg' | 'png'; nombre: string; fechaCarga: string }>;
  notas?: string | null;
  createdAt: string;
  updatedAt: string;
  createdBy?: string | null;
  createdByName?: string | null;
  updatedBy?: string | null;
  updatedByName?: string | null;
}
```

Firestore collections que los specs deben querear:
- `presupuestos` — assert `pendingActions[]` appended
- `leads` — assert `estado === 'oc_recibida'`
- `ordenesCompraCliente` (NEW) — assert shape + N:M back-refs
- `adminConfig/flujos` (NEW) — assert saved users + mail
- `mailQueue` — assert doc con `type === 'cierre_admin_ot'` and `status === 'pending'`
- `requerimientosCompra` — assert `condicional === true` tras acceptance; `estado === 'cancelado'` tras anular
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear helper firestore-assert.ts con readers typed para pendingActions, mailQueue, OC cliente, ticket estado y adminConfig</name>
  <files>apps/sistema-modular/e2e/helpers/firestore-assert.ts</files>
  <action>
Crear `apps/sistema-modular/e2e/helpers/firestore-assert.ts`. Decisión locked (I1): **usar cliente SDK via la auth fixture de tests (test-base)** — NO Admin SDK (requiere service-account + env var setup out-of-scope). Si la suite e2e ya tiene un cliente Firebase inicializado en `global-setup.ts` o `fixtures/`, reusarlo (import desde la ubicación que corresponda). Si NO existe aún, inicializar cliente Firebase regular con las mismas credenciales que usa el browser en dev (leer envs desde `.env.e2e` o equivalente — no hardcodear).

Exportar las siguientes funciones (tipadas contra `@ags/shared`):

```typescript
import { db } from '../fixtures/firebase-e2e'; // o ruta equivalente
import { doc, getDoc, collection, query, where, getDocs, orderBy, limit } from 'firebase/firestore';
import type { Presupuesto, PendingAction, Lead, OrdenCompraCliente, RequerimientoCompra, TicketEstado } from '@ags/shared';

/** Lee el array pendingActions[] de un presupuesto */
export async function getPendingActions(presupuestoId: string): Promise<PendingAction[]>;

/** Lee todas las pendingActions NO resueltas (resolvedAt == null) across all presupuestos */
export async function getUnresolvedPendingActions(opts?: { type?: string }): Promise<Array<{ presupuestoId: string; action: PendingAction }>>;

/** Lee el estado actual de un ticket (lead) */
export async function getTicketEstado(leadId: string): Promise<TicketEstado | null>;

/** Lee una OC cliente por id */
export async function getOCCliente(id: string): Promise<OrdenCompraCliente | null>;

/** Lee todas las OC cliente vinculadas a un presupuesto (tanto back-ref como query) */
export async function getOCsByPresupuesto(presupuestoId: string): Promise<OrdenCompraCliente[]>;

/** Lee docs en mailQueue filtrados por type + status */
export async function getMailQueueDocs(opts: { type?: 'cierre_admin_ot' | string; status?: 'pending' | 'sent' | 'failed'; limit?: number }): Promise<Array<{ id: string; data: any }>>;

/** Lee el doc de config admin 'adminConfig/flujos' */
export async function getAdminConfigFlujos(): Promise<{ usuarioSeguimientoId?: string; usuarioCoordinadorOTId?: string; mailFacturacion?: string } | null>;

/** Lee requerimientos por presupuestoId con filtro opcional por estado */
export async function getRequerimientosByPresupuesto(presupuestoId: string, opts?: { estado?: string; condicional?: boolean }): Promise<RequerimientoCompra[]>;

/** Retry helper para eventual consistency: reintenta assertion N veces con delay */
export async function pollUntil<T>(fn: () => Promise<T>, predicate: (v: T) => boolean, opts?: { timeout?: number; interval?: number }): Promise<T>;
```

Budget: archivo ≤200 líneas. Si excede, extraer `pollUntil` a `helpers/poll.ts`.

Constraints:
- Cada función tiene JSDoc breve (1 línea) con uso esperado en specs
- Usar `getDoc` / `getDocs` del SDK cliente (no Admin)
- Si el proyecto e2e no tiene aún un módulo Firebase inicializado, documentar en top del archivo la asunción y crear fallback que use las mismas envs vars que el fixture global-setup usa (o reusar `import { db } from '@/services/firebase'` si eso funciona desde test runner — verificarlo con smoke)
- NO hardcodear project IDs ni keys — leer de env vars
- `pollUntil` default timeout 10s, interval 500ms (eventual consistency de Firestore puede tardar hasta unos segundos)
  </action>
  <verify>
    <automated>cd apps/sistema-modular &amp;&amp; pnpm type-check</automated>
  </verify>
  <done>
- `firestore-assert.ts` existe, exporta las 8 funciones declaradas
- `pnpm --filter @ags/sistema-modular type-check` pasa sin errores en el helper (puede haber errores en otros archivos — filtrar solo este)
- Archivo ≤200 líneas
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear spec 12-pending-actions-retry.spec.ts — FLOW-01 edge case + FLOW-06 dashboard</name>
  <files>apps/sistema-modular/e2e/circuits/12-pending-actions-retry.spec.ts</files>
  <action>
Crear `apps/sistema-modular/e2e/circuits/12-pending-actions-retry.spec.ts` siguiendo el patrón de `11-full-business-cycle.spec.ts` (mismo login fixture, misma navegación tab-based).

Cubrir:

**Scenario A (FLOW-01 edge):**
1. Crear presupuesto nuevo desde `/presupuestos/nuevo` con `clienteId: null` simulado — vendedor selecciona "Cliente sin registrar" o deja en blanco. Si la UI no soporta esto, forzar el estado via fixture injection en Firestore.
2. Enviar presupuesto (markEnviado flow).
3. Assert: `getPendingActions(presupuestoId)` retorna al menos 1 entry con `type: 'crear_ticket_seguimiento'`, `reason` contiene "clienteId null" (o substring equivalente), `resolvedAt` es undefined, `attempts: 0`.
4. Navegar a `/admin/revision-clienteid`, resolver el `clienteId` del presupuesto pendiente (flow ya existe desde Phase 5).
5. `pollUntil` que `getPendingActions(presupuestoId)` tenga `resolvedAt != null` (retry retroactivo disparado por `leadsService.resolverClienteIdPendiente`).
6. Assert: ticket de seguimiento creado — `getTicketEstado(newLeadId)` retorna `'esperando_oc'` (lock: auto-ticket arranca en este estado — ver 08-03 Task 1 Edit 2).

**Scenario B (FLOW-06 dashboard):**
1. Navegar a `/admin/acciones-pendientes`.
2. Assert: tabla muestra row(s) con tipo `crear_ticket_seguimiento` y cliente correspondiente.
3. Click en "Reintentar" del row → assert `pollUntil` que la action se marca `resolvedAt != null`.
4. Assert: tras retry exitoso, el row desaparece de la lista (o cambia a "Resuelto").
5. Scenario negativo: con un presupuesto cuya `pendingAction` NO se puede resolver (ej: usuario fijo está `activo: false`), click "Reintentar" → assert `attempts` incrementa pero `resolvedAt` sigue undefined; toast de error visible.
6. Click "Marcar resuelta manual" → assert `resolvedAt` seteado sin que `attempts` cambie (o con cambio marginal permitido).

Usar `test.describe.serial` si el orden importa. Inline fixtures (crear presupuesto/cliente) si los fixtures globales no cubren el case.

Los selectors de UI son placeholders documentados — los executors de Wave 2/3 los ajustarán cuando la UI exista. Usar roles ARIA + text content preferentemente (ej. `page.getByRole('button', { name: /reintentar/i })`) en lugar de CSS brittle.

**IMPORTANTE:** este spec DEBE fallar ahora (RED). Documentar en el header del archivo:
```
/**
 * RED baseline as of 2026-04-21.
 * - FLOW-01 edge: pendingActions[] not implemented yet (plan 08-01).
 * - FLOW-06 dashboard: /admin/acciones-pendientes not implemented yet (plan 08-05).
 * Will turn GREEN after Wave 3 completes.
 */
```
  </action>
  <verify>
    <automated>cd apps/sistema-modular &amp;&amp; pnpm playwright test e2e/circuits/12-pending-actions-retry.spec.ts --list</automated>
  </verify>
  <done>
- Spec file existe, es parsable por Playwright (`--list` enumera tests)
- Spec FALLA al correrlo (RED) — documentado en header
- Al menos 2 `test()` blocks: Scenario A (FLOW-01 edge) y Scenario B (FLOW-06 dashboard)
- Usa `firestore-assert.ts` helpers
  </done>
</task>

<task type="auto">
  <name>Task 3: Crear spec 13-oc-cliente-flow.spec.ts — FLOW-02 carga OC + N:M + extend 11 + 10 smoke</name>
  <files>apps/sistema-modular/e2e/circuits/13-oc-cliente-flow.spec.ts, apps/sistema-modular/e2e/circuits/11-full-business-cycle.spec.ts, apps/sistema-modular/e2e/circuits/10-smoke-all-pages.spec.ts</files>
  <action>
**Parte A — Nuevo spec `13-oc-cliente-flow.spec.ts`:**

Cubrir:

1. **Carga simple (FLOW-02 core):** crear presupuesto `aceptado` con ticket ligado → abrir modal "Cargar OC" desde list → ingresar numero + fecha + upload de 1 PDF (usar fixture PDF pequeño en `e2e/fixtures/oc-sample.pdf` — si no existe, crear uno trivial con `fs` en el `beforeAll`) → submit.
2. Assert Firestore:
   - `getOCsByPresupuesto(presupuestoId)` retorna 1 OC con `numero`, `adjuntos[0].tipo === 'pdf'`, `clienteId` coincide.
   - `presupuesto.ordenesCompraIds` contiene el nuevo ocId.
   - `getTicketEstado(leadId)` retorna `'oc_recibida'`.
3. **N:M (FLOW-02 multi):** crear segundo presupuesto aceptado del mismo cliente → abrir modal "Cargar OC" → seleccionar OC existente (la del paso 1) → checkbox "esta OC cubre otros presupuestos" → submit. Assert back-refs: `oc.presupuestosIds` tiene 2 ids; ambos `presupuesto.ordenesCompraIds` tienen el mismo ocId.
4. **Pendiente condicional (FLOW-02 → FLOW-03 setup):** crear presupuesto con item que requiere importación (articulo con stock disponible+tránsito+reservado == 0 → fixture forzado) → aceptar → cargar OC → assert que ticket deriva con Posta hacia área `materiales_comex` (o `importaciones` según lock final del naming).
5. **Idempotencia:** cargar una segunda OC diferente al mismo presupuesto → assert ticket sigue `'oc_recibida'` (no pasa a otro estado inválido); `ordenesCompraIds` tiene 2 ids.

Header con mismo comentario de RED baseline.

**Parte B — Extender `11-full-business-cycle.spec.ts`:**

Localizar el step donde la suite llega a `CIERRE_ADMINISTRATIVO` (probablemente al final del flow). Agregar:

```typescript
// FLOW-04 assertions
await pollUntil(
  () => getMailQueueDocs({ type: 'cierre_admin_ot', status: 'pending' }),
  (docs) => docs.length >= 1,
  { timeout: 10_000 }
);
// Assert ticket admin creado
const adminTickets = await /* query leads where area === 'administracion' y descripcion contiene OT number */;
expect(adminTickets.length).toBeGreaterThanOrEqual(1);
```

No romper los asserts existentes; solo extender. Si la suite no llega hoy a `CIERRE_ADMINISTRATIVO`, documentar con `test.fixme()` el step FLOW-04 y abrir TODO para Wave 3 (el executor del plan 08-05 lo desfixmeará).

**Parte C — Extender `10-smoke-all-pages.spec.ts`:**

Agregar 2 paths a la lista de rutas que smoke testea:
- `/admin/config-flujos`
- `/admin/acciones-pendientes`

Mismo patrón que las rutas admin existentes — gate por rol admin, navegar, esperar página, assert título/heading presente.
  </action>
  <verify>
    <automated>cd apps/sistema-modular &amp;&amp; pnpm playwright test e2e/circuits/13-oc-cliente-flow.spec.ts e2e/circuits/10-smoke-all-pages.spec.ts e2e/circuits/11-full-business-cycle.spec.ts --list</automated>
  </verify>
  <done>
- `13-oc-cliente-flow.spec.ts` existe con al menos 4 `test()` blocks (carga simple, N:M, pendiente condicional, idempotencia)
- `11-full-business-cycle.spec.ts` contiene assertion sobre `mailQueue` + ticket admin (o `test.fixme` documentado)
- `10-smoke-all-pages.spec.ts` incluye las 2 rutas admin nuevas
- Los 3 comandos `--list` no fallan parse
- Specs RED baseline documentado en headers
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @ags/sistema-modular type-check` pasa (el helper es typed)
- `pnpm playwright test --list` enumera los nuevos tests sin parse error
- Todos los specs fallan (RED) al correrlos — documentado
- Los executors de Wave 1-3 podrán referenciar estos specs en sus `<verify><automated>` blocks
</verification>

<success_criteria>
- [ ] `apps/sistema-modular/e2e/helpers/firestore-assert.ts` existe con las 8 funciones exportadas
- [ ] `apps/sistema-modular/e2e/circuits/12-pending-actions-retry.spec.ts` existe con scenarios A+B
- [ ] `apps/sistema-modular/e2e/circuits/13-oc-cliente-flow.spec.ts` existe con 4+ scenarios
- [ ] `11-full-business-cycle.spec.ts` extendido con FLOW-04 mailQueue assertion
- [ ] `10-smoke-all-pages.spec.ts` extendido con 2 rutas admin
- [ ] Playwright puede parsear + enumerar todos los tests (`--list` OK)
- [ ] Specs fallan (RED) — header documenta baseline
</success_criteria>

<output>
Al completar: crear `.planning/phases/08-flujo-automatico-derivacion/08-00-SUMMARY.md` con:
- Lista de específicas creados/extendidos con líneas clave
- Qué helpers quedan disponibles para Wave 1-3
- RED baseline confirmado: cuáles tests fallan hoy y qué plan los volverá GREEN
</output>
