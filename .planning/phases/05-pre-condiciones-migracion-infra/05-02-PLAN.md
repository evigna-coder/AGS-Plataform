---
phase: 05-pre-condiciones-migracion-infra
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/sistema-modular/scripts/migrate-tickets-contactos.mjs
autonomous: false
requirements:
  - PREC-02

must_haves:
  truths:
    - "Todo ticket legacy con `contacto/email/telefono` planos y sin `contactos[]` queda persistido con un array `contactos[]` con un contacto `esPrincipal: true`"
    - "Los campos planos `contacto`, `email`, `telefono` se preservan (no se borran) — siguen sincronizados para listas/búsqueda"
    - "El script es idempotente: correrlo dos veces no modifica nada en el segundo run"
    - "El modal de envío de mail siempre encuentra un destinatario (getContactoPrincipal devuelve non-null para cualquier ticket migrado con contacto/email previo)"
  artifacts:
    - path: "apps/sistema-modular/scripts/migrate-tickets-contactos.mjs"
      provides: "Migración batch que persiste el output de hydrateContactos() para todo ticket con contactos[] vacío"
      min_lines: 90
  key_links:
    - from: "apps/sistema-modular/scripts/migrate-tickets-contactos.mjs"
      to: "Firestore collection 'leads'"
      via: "firebase-admin batch updates setteando contactos[] desde campos planos"
      pattern: "esPrincipal: true"
---

<objective>
Persistir el resultado de la hidratación en-memory (`hydrateContactos()` en `leadsService.ts:89-103`) para todos los tickets legacy. Hoy la app funciona porque cada lectura hidrata en runtime, pero Phase 8 (FLOW-04 aviso facturación, FLOW-02 derivación a coordinador) necesita que los `contactos[]` estén persistidos para que el modal de envío de mail pueda seleccionar un destinatario con certeza y sin depender de que el ticket pase por `parseLeadDoc` primero.

Purpose: Cierra el gap entre "array hidratado on-read" y "array real en Firestore". Habilita queries Firestore sobre `contactos` (p.ej. `where('contactos', 'array-contains-any', ...)`) que hoy no son posibles.
Output: Script standalone `migrate-tickets-contactos.mjs`. Cero cambios en la app — la lógica ya existe, solo la persistimos.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-pre-condiciones-migracion-infra/05-CONTEXT.md

@CLAUDE.md
@.claude/rules/firestore.md

@apps/sistema-modular/scripts/migrate-establecimientos.js
@apps/sistema-modular/src/services/leadsService.ts
@packages/shared/src/types/index.ts

<interfaces>
<!-- Lógica exacta a replicar en el script (copia-pega del source). -->

From apps/sistema-modular/src/services/leadsService.ts (lines 89-103, `hydrateContactos`):
```typescript
function hydrateContactos(data: any): ContactoTicket[] {
  const existing: ContactoTicket[] = Array.isArray(data.contactos) ? data.contactos : [];
  if (existing.length > 0) return existing;  // <-- IDEMPOTENCY GUARD
  const nombre = (data.contacto ?? '').trim();
  const email = (data.email ?? '').trim();
  const telefono = (data.telefono ?? '').trim();
  if (!nombre && !email && !telefono) return [];
  return [{
    id: 'legacy-principal',
    nombre: nombre || '(Sin nombre)',
    email: email || undefined,
    telefono: telefono || undefined,
    esPrincipal: true,
  }];
}
```

From packages/shared/src/types/index.ts (line 667, `ContactoTicket`):
```typescript
export interface ContactoTicket {
  id: string;
  nombre: string;
  cargo?: string;
  sector?: string;
  telefono?: string;
  interno?: string;
  email?: string;
  esPrincipal: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear script migrate-tickets-contactos.mjs</name>
  <files>apps/sistema-modular/scripts/migrate-tickets-contactos.mjs</files>
  <behavior>
    Script idempotente. Criterios:
    - Corre en `--dry-run` (default) sin tocar Firestore; imprime resumen.
    - En `--run`, para cada ticket donde `contactos` sea missing o `[]` y haya al menos uno de {`contacto`, `email`, `telefono`}, escribe `contactos: [{ id: 'legacy-principal', nombre, email, telefono, esPrincipal: true }]`.
    - Tickets que ya tienen `contactos.length > 0` se skipean (idempotente).
    - Tickets sin ningún campo plano (`contacto` vacío AND `email` vacío AND `telefono` vacío) se skipean sin escribir nada.
    - NO borra ni modifica los campos planos `contacto`, `email`, `telefono`.
    - Batches de 400 docs por commit.
    - Output al final: `Migrados: N, ya-con-contactos: N, sin-datos: N, total: N`.
  </behavior>
  <action>
    Crear `apps/sistema-modular/scripts/migrate-tickets-contactos.mjs`. COPIAR el bootstrap de `apps/sistema-modular/scripts/migrate-establecimientos.js` (parseo de `--dry-run`/`--run`, import dinámico de `firebase-admin`, `SERVICE_ACCOUNT_PATH` + fallback a `./service-account.json`, inicialización de `db`).

    Definir un helper local (replicar `hydrateContactos` de `leadsService.ts:89-103` pero **SIN** el import — el script es `.mjs` con firebase-admin, no tiene `@ags/shared`):

    ```js
    /** Replica la lógica de hydrateContactos() en leadsService.ts, sin mutar campos planos. */
    function buildContactosArrayFromFlat(data) {
      const existing = Array.isArray(data.contactos) ? data.contactos : [];
      if (existing.length > 0) return null; // signal: skip, ya tiene contactos
      const nombre = (data.contacto ?? '').trim();
      const email = (data.email ?? '').trim();
      const telefono = (data.telefono ?? '').trim();
      if (!nombre && !email && !telefono) return []; // signal: nada que migrar
      const contacto = { id: 'legacy-principal', nombre: nombre || '(Sin nombre)', esPrincipal: true };
      if (email) contacto.email = email;       // OMITIR si vacío (never undefined in write)
      if (telefono) contacto.telefono = telefono;
      return [contacto];
    }
    ```

    Algoritmo principal:
    ```js
    async function main() {
      // ... bootstrap igual que migrate-establecimientos.js ...
      const db = admin.firestore();

      const counts = { migrados: 0, yaConContactos: 0, sinDatos: 0, total: 0 };
      const leadsSnap = await db.collection('leads').get();
      counts.total = leadsSnap.size;

      console.log(isDryRun ? '=== DRY RUN (no se escriben datos) ===' : '=== EJECUCIÓN REAL ===');

      let batch = db.batch();
      let pendingOps = 0;

      for (const doc of leadsSnap.docs) {
        const data = doc.data();
        const result = buildContactosArrayFromFlat(data);

        if (result === null) {
          counts.yaConContactos++;
          continue;
        }
        if (result.length === 0) {
          counts.sinDatos++;
          continue;
        }
        counts.migrados++;

        if (isDryRun) {
          console.log('Ticket', doc.id, '→ contactos:', JSON.stringify(result));
          continue;
        }

        // Build payload — NUNCA escribir undefined.
        const payload = stripUndefined({
          contactos: result,
          updatedAt: admin.firestore.Timestamp.now(),
        });
        batch.update(doc.ref, payload);
        pendingOps++;

        if (pendingOps >= 400) {
          await batch.commit();
          console.log(`Commit batch: ${pendingOps} tickets`);
          batch = db.batch();
          pendingOps = 0;
        }
      }

      if (pendingOps > 0 && !isDryRun) {
        await batch.commit();
        console.log(`Commit final batch: ${pendingOps} tickets`);
      }

      console.log(`\nResumen:`);
      console.log(`  Migrados: ${counts.migrados}`);
      console.log(`  Ya-con-contactos: ${counts.yaConContactos}`);
      console.log(`  Sin-datos (skip): ${counts.sinDatos}`);
      console.log(`  Total tickets: ${counts.total}`);
    }

    function stripUndefined(obj) {
      const out = {};
      for (const [k, v] of Object.entries(obj)) if (v !== undefined) out[k] = v;
      return out;
    }
    ```

    **Guardarrailes críticos:**
    - **NO tocar** `contacto`, `email`, `telefono` — `syncFlatFromContactos` ya los mantiene sincronizados en writes futuros; borrarlos rompe listas existentes.
    - **NO** usar `null` para email/telefono vacíos dentro del objeto `ContactoTicket` — omitir la key (firebase acepta el doc sin la key, pero RECHAZA el valor `undefined` explícito — de ahí `stripUndefined` + el `if (email) contacto.email = email`).
    - NO leer de `@ags/shared` — el script es JS puro con firebase-admin. Replicar tipos inline.
    - Idempotencia: el `if (existing.length > 0) return null` es el guard que hace que corridas repetidas sean no-ops para tickets ya migrados.

    Output esperado al root de `apps/sistema-modular/` (no escribe mapping.json — no es necesario; solo log a stdout).
  </action>
  <verify>
    <automated>node apps/sistema-modular/scripts/migrate-tickets-contactos.mjs --dry-run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Script corre en `--dry-run` sin errores (o con error esperado de `service-account.json no encontrado` si no hay uno — aceptable; Task 2 lo ejerce con credenciales reales).
    - `grep -c "esPrincipal" apps/sistema-modular/scripts/migrate-tickets-contactos.mjs` ≥ 2 (en el helper y en el comentario o estructura).
    - `grep -c "existing.length > 0" apps/sistema-modular/scripts/migrate-tickets-contactos.mjs` ≥ 1 (idempotency guard presente).
    - NO hay escritura a campos planos `contacto`, `email`, `telefono` (`grep -E "batch.update.*contacto\s*:" apps/sistema-modular/scripts/migrate-tickets-contactos.mjs` debe ser 0 — solo se escribe `contactos:` con 's' final).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ejecutar --dry-run, revisar, ejecutar --run, verificar idempotencia</name>
  <what-built>
    Script de migración idempotente que convierte `contacto/email/telefono` planos a `contactos[]` con principal marcado, para todos los tickets legacy.
  </what-built>
  <how-to-verify>
    1. Confirmar que `apps/sistema-modular/service-account.json` existe (o setear `SERVICE_ACCOUNT_PATH`).
    2. `cd apps/sistema-modular && node scripts/migrate-tickets-contactos.mjs --dry-run`
       - ✔ Imprime "=== DRY RUN ===" + resumen final con las 4 métricas.
       - ✔ Muestra muestras de los arrays `contactos` que propondría.
       - ✔ Firestore NO cambia.
    3. Samplear en Firebase console 3 tickets antes del run real, anotar sus valores de `contacto/email/telefono`.
    4. `node scripts/migrate-tickets-contactos.mjs --run`
       - ✔ Imprime "=== EJECUCIÓN REAL ===" + mismo resumen que `--dry-run`.
    5. Volver a los 3 tickets sampleados:
       - ✔ Ahora tienen `contactos: [{ id: 'legacy-principal', nombre, email, telefono, esPrincipal: true }]`.
       - ✔ Los campos planos `contacto/email/telefono` están idénticos a antes (no borrados).
    6. **Test de idempotencia:** correr `node scripts/migrate-tickets-contactos.mjs --run` por segunda vez.
       - ✔ El resumen muestra `Migrados: 0` y `Ya-con-contactos: N` (todos los que acabamos de migrar).
       - ✔ Firestore NO tuvo cambios en este segundo run (verificar vía `updatedAt` de un ticket migrado: debe ser el timestamp del primer `--run`, no del segundo).
    7. **Test funcional en app:** levantar `pnpm dev:modular`, abrir un ticket migrado en la UI, abrir el "Enviar por mail" modal.
       - ✔ El modal pre-selecciona el destinatario del contacto principal.
       - ✔ No hay errores en consola del browser.
  </how-to-verify>
  <resume-signal>Type "approved" o describir issues (ej: "hay 5 tickets con email con espacio al final que no se trimaron correctamente")</resume-signal>
</task>

</tasks>

<verification>
- Script corre idempotente (2do run = 0 migrados).
- Tickets legacy tienen `contactos[]` persistido post-run.
- Campos planos preservados.
- `getContactoPrincipal()` devuelve non-null para cualquier ticket con contacto previo.
- Firebase console muestra arrays correctos sin `undefined`.
</verification>

<success_criteria>
- Criterio ROADMAP Phase 5 #2: "Los tickets con contactos planos tienen `contactos[]` estructurado con principal marcado — el modal de envío de mail siempre encuentra un destinatario".
- Todas las queries futuras sobre `contactos` (p.ej. FLOW-04 aviso facturación selección de destinatario) funcionan sin depender de hidratación runtime.
- Cero breakage: `syncFlatFromContactos` sigue siendo la fuente de sincronización en writes nuevos; esta migración es additive.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pre-condiciones-migracion-infra/05-02-SUMMARY.md` documentando:
- Conteo final: migrados / ya-con-contactos / sin-datos / total.
- Timestamp del run real.
- Cualquier ticket con edge case (ej: nombre vacío pero email presente → quedó como "(Sin nombre)").
- Confirmación de idempotencia del 2do run.
</output>
