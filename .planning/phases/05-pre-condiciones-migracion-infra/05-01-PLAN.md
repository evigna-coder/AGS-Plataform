---
phase: 05-pre-condiciones-migracion-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/index.ts
  - apps/sistema-modular/scripts/migrate-tickets-clienteid.mjs
  - apps/sistema-modular/src/services/leadsService.ts
  - apps/sistema-modular/src/pages/admin/RevisionClienteIdPage.tsx
  - apps/sistema-modular/src/pages/admin/index.ts
  - apps/sistema-modular/src/App.tsx
  - apps/sistema-modular/src/components/layout/navigation.ts
autonomous: false
requirements:
  - PREC-01

must_haves:
  truths:
    - "El script migrate-tickets-clienteid.mjs corre en --dry-run sin tocar Firestore y produce mapping.json con matched/ambiguous/unmatched"
    - "En --run, tickets con match único reciben clienteId resuelto; tickets ambiguos o sin candidato quedan con pendienteClienteId: true"
    - "Un admin puede abrir /admin/revision-clienteid, ver la lista filtrable de tickets pendientes, elegir un cliente con SearchableSelect y persistir la resolución"
    - "Al resolver manualmente, se escribe clienteId + clienteIdMigradoAt + clienteIdMigradoPor y pendienteClienteId queda false"
    - "Ningún write a Firestore pasa por undefined — todos usan deepCleanForFirestore/cleanFirestoreData"
  artifacts:
    - path: "packages/shared/src/types/index.ts"
      provides: "Campos pendienteClienteId, candidatosPropuestos, clienteIdMigradoAt, clienteIdMigradoPor, revisionDescartada en interface Ticket"
      contains: "pendienteClienteId"
    - path: "apps/sistema-modular/scripts/migrate-tickets-clienteid.mjs"
      provides: "Script de migración batch con --dry-run/--run, normaliza CUIT y razón social, genera mapping.json"
      min_lines: 120
    - path: "apps/sistema-modular/src/pages/admin/RevisionClienteIdPage.tsx"
      provides: "UI admin para resolver manualmente tickets pendientes"
      min_lines: 80
    - path: "apps/sistema-modular/src/services/leadsService.ts"
      provides: "Método resolverClienteIdPendiente(ticketId, clienteId) que setea clienteIdMigradoAt/Por"
      contains: "resolverClienteIdPendiente"
  key_links:
    - from: "apps/sistema-modular/scripts/migrate-tickets-clienteid.mjs"
      to: "Firestore collection 'leads'"
      via: "firebase-admin batch updates con pendienteClienteId/candidatosPropuestos"
      pattern: "pendienteClienteId"
    - from: "apps/sistema-modular/src/pages/admin/RevisionClienteIdPage.tsx"
      to: "apps/sistema-modular/src/services/leadsService.ts"
      via: "leadsService.resolverClienteIdPendiente()"
      pattern: "leadsService\\.resolverClienteIdPendiente"
    - from: "apps/sistema-modular/src/App.tsx"
      to: "apps/sistema-modular/src/pages/admin/RevisionClienteIdPage.tsx"
      via: "ruta /admin/revision-clienteid protegida por rol admin"
      pattern: "revision-clienteid"
---

<objective>
Sanear el legado de tickets con `clienteId: null` para que las derivaciones automáticas de Phases 7-10 no fallen silenciosamente (Pitfall 7-A). Entregamos dos piezas complementarias: un script de migración batch que matchea por CUIT / razón social exacta, y una UI admin que permite resolver manualmente los casos ambiguos o sin candidato.

Purpose: PREC-01 es bloqueante de Phases 7-10. Sin esto, el auto-ticket de FLOW-01 y la derivación a coordinador de FLOW-02 crashan en tickets legacy.
Output: Script `migrate-tickets-clienteid.mjs`, extensión del type `Ticket` en `@ags/shared`, método `resolverClienteIdPendiente` en `leadsService`, página admin `/admin/revision-clienteid`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-pre-condiciones-migracion-infra/05-CONTEXT.md

@CLAUDE.md
@.claude/rules/firestore.md
@.claude/rules/components.md

@apps/sistema-modular/scripts/migrate-establecimientos.js
@apps/sistema-modular/src/services/leadsService.ts
@apps/sistema-modular/src/services/firebase.ts
@apps/sistema-modular/src/pages/admin/ImportacionDatos.tsx
@packages/shared/src/types/index.ts

<interfaces>
<!-- Tipos clave a extender y helpers existentes. Extraídos del codebase. -->

From packages/shared/src/types/index.ts (line 685):
```typescript
export interface Ticket {
  id: string;
  clienteId: string | null;
  razonSocial: string;
  // ... existing fields ...
  // ↓ a AGREGAR en este plan (ver Task 1 para shapes exactos):
  // pendienteClienteId?: boolean;
  // candidatosPropuestos?: Array<{ clienteId: string; razonSocial: string; score: 'cuit' | 'razonSocial' }>;
  // clienteIdMigradoAt?: string | null;
  // clienteIdMigradoPor?: string | null;
  // revisionDescartada?: boolean;
}
/** @deprecated Use Ticket */ export type Lead = Ticket;
```

From apps/sistema-modular/src/services/firebase.ts:
```typescript
export const db: Firestore;
export function cleanFirestoreData<T>(obj: T): Partial<T>;          // Top-level undefined strip
export function deepCleanForFirestore<T>(obj: T): T;                 // Recursive (JSON round-trip)
export function getUpdateTrace(): { updatedAt: Timestamp; updatedBy: string };
export function getCurrentUserTrace(): { uid: string; name: string } | null;
```

From apps/sistema-modular/scripts/migrate-establecimientos.js (pattern to copy):
- Reads `SERVICE_ACCOUNT_PATH` env var (fallback `./service-account.json`)
- Flags `--dry-run` (default) and `--run`
- Usa `admin.firestore()` via dynamic import
- Escribe `mapping.json` + `errors.csv` al root de `apps/sistema-modular/`
- Función `normalizeCuit(s)`: strip non-digits
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extender type Ticket con campos de migración</name>
  <files>packages/shared/src/types/index.ts</files>
  <behavior>
    No TDD (cambio de tipos puros). Criterio: `pnpm type-check` pasa y los callers existentes de `Ticket` / `Lead` no rompen.
  </behavior>
  <action>
    En `packages/shared/src/types/index.ts`, dentro de `interface Ticket` (línea ~685, después de `valorEstimado?: number | null;` y antes de los arrays de IDs), agregar EXACTAMENTE estos campos opcionales con ESTAS shapes (las shapes son contrato consumido por Tasks 2, 3 y 4 — no alterar):

    ```typescript
      /** True si la migración clienteId batch no pudo resolver un único candidato. Visible en /admin/revision-clienteid. Hidratado default `false` en parseLeadDoc cuando el campo falta en Firestore. */
      pendienteClienteId?: boolean;
      /** Candidatos propuestos por el script de migración cuando hay ambigüedad. Array opcional; default `[]` en hidratación si el campo falta. `score` indica por qué criterio matcheó el candidato. */
      candidatosPropuestos?: Array<{ clienteId: string; razonSocial: string; score: 'cuit' | 'razonSocial' }>;
      /** ISO timestamp de cuando se resolvió clienteId (por script o manualmente). `null` si nunca se migró. En Firestore se persiste como Timestamp; se lee como ISO string via .toDate().toISOString(). */
      clienteIdMigradoAt?: string | null;
      /** uid del usuario que resolvió manualmente, o literal "script" si vino del batch. `null` si nunca se migró. */
      clienteIdMigradoPor?: string | null;
      /** Admin marcó el ticket como "no se puede resolver, ignorar de la lista de pendientes". Default `false` en hidratación. */
      revisionDescartada?: boolean;
    ```

    **Notas críticas sobre las shapes (responden al checker B2):**
    - `pendienteClienteId` y `revisionDescartada` NO son `| null` — son booleanos opcionales que en Firestore pueden faltar y en la app se hidratan a `false` por default.
    - `candidatosPropuestos` NO es `| null` — es array opcional; default en hidratación es `[]`. Cada candidato incluye `score: 'cuit' | 'razonSocial'` que indica el criterio de match (usado por la UI para ordenar/badgear).
    - `clienteIdMigradoAt` / `clienteIdMigradoPor` SÍ son `| null` — representan "explícitamente no migrado" cuando el ticket está registrado pero nunca pasó por el script/UI.

    NO modificar el alias `export type Lead = Ticket;` — los cinco campos fluyen automáticamente al alias deprecado. En este plan TODOS los consumidores nuevos importan `Ticket` (NUNCA `Lead`).

    NO tocar la firma de `parseLeadDoc` en `leadsService.ts` en este task — se hace en Task 2.

    Why: Estos campos son el contrato que Task 2 (script), Task 3 (service method) y Task 4 (UI) consumen. Definirlos primero evita scavenger-hunt downstream. Las shapes exactas son lockeadas por el checker — cualquier divergencia entre type declaration y hidratación default causará type errors.
  </action>
  <verify>
    <automated>pnpm type-check</automated>
  </verify>
  <done>
    - `pnpm type-check` pasa en `packages/shared` sin errores.
    - `grep -n "pendienteClienteId" packages/shared/src/types/index.ts` devuelve al menos 2 líneas (definición + comentario o tipo).
    - `grep -n "score: 'cuit'" packages/shared/src/types/index.ts` devuelve al menos 1 línea (shape del candidato).
    - Ni `@deprecated Use Ticket` ni otros exports se alteran.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Script migrate-tickets-clienteid.mjs + hook en leadsService.parseLeadDoc</name>
  <files>apps/sistema-modular/scripts/migrate-tickets-clienteid.mjs, apps/sistema-modular/src/services/leadsService.ts</files>
  <behavior>
    Script es idempotente y observable. Criterios:
    - `node apps/sistema-modular/scripts/migrate-tickets-clienteid.mjs --dry-run` corre sin tocar Firestore y escribe `apps/sistema-modular/mapping-clienteid.json` con arrays `matched`, `ambiguous`, `unmatched`, `skipped`.
    - En `--run`, tickets con match único reciben `clienteId` + `clienteIdMigradoAt` + `clienteIdMigradoPor: "script"`.
    - Tickets ambiguos (>1 candidato) reciben `pendienteClienteId: true` + `candidatosPropuestos: [...]` (con `score` por candidato).
    - Tickets sin candidato reciben `pendienteClienteId: true` + `candidatosPropuestos: []`.
    - Tickets con `clienteId` ya resuelto quedan skipped (idempotente).
    - `parseLeadDoc` hidrata los 5 campos nuevos al leer con defaults alineados a las shapes del type `Ticket` (ver Task 1).
  </behavior>
  <action>
    **PARTE A — Script de migración.** Crear `apps/sistema-modular/scripts/migrate-tickets-clienteid.mjs`. COPIAR el esqueleto de `apps/sistema-modular/scripts/migrate-establecimientos.js`:
    - Reutilizar el bloque de parseo de flags (`--dry-run` / `--run`), import dinámico de `firebase-admin`, carga de `SERVICE_ACCOUNT_PATH`.
    - Reutilizar `normalizeCuit(cuit)` idéntico.
    - Agregar helper `normalizeRazonSocial(s)` que hace trim + lowercase + remove accents (`.normalize('NFD').replace(/[\u0300-\u036f]/g, '')`) + collapse whitespace (`/\s+/g → ' '`).

    Algoritmo principal:
    1. Leer `db.collection('clientes').get()` → construir dos maps: `byCuit: Map<string, {id, razonSocial, cuit}>` y `byRazonSocial: Map<string, Array<...>>` (array porque razón social puede no ser única).
    2. Leer `db.collection('leads').where('clienteId', '==', null).get()`.
    3. Para cada ticket:
       - Si ya tiene `clienteId` no-null (defensive) → push a `mapping.skipped`.
       - Extraer `ticketCuit = normalizeCuit(data.cuit)` (si existe ese campo en el ticket) y `ticketRazon = normalizeRazonSocial(data.razonSocial)`.
       - **Match 1 — CUIT:** si `ticketCuit` y `byCuit.has(ticketCuit)` → resolver con ese cliente único. Al armar el candidato/registro, usar `score: 'cuit'`.
       - **Match 2 — razón social exacta:** si no matcheó por CUIT, buscar `byRazonSocial.get(ticketRazon)`. Si exactamente 1 → resolver. Si >1 → ambiguous. Si 0 → unmatched. Al armar candidatos, usar `score: 'razonSocial'`.
       - Resultados:
         - `matched`: push a array con `{ ticketId, clienteId, via: 'cuit' | 'razonSocial' }`. En `--run`, batch update `clienteId`, `clienteIdMigradoAt: admin.firestore.Timestamp.now()` (se convierte a ISO al leer), `clienteIdMigradoPor: 'script'`, `updatedAt: Timestamp.now()`.
         - `ambiguous`: push a `{ ticketId, candidatos: [{ clienteId, razonSocial, score }, ...] }`. En `--run`, batch update `pendienteClienteId: true`, `candidatosPropuestos: [{ clienteId, razonSocial, score }, ...]`, `updatedAt: Timestamp.now()`. **NOTA:** la shape de cada candidato persistido DEBE matchear `{ clienteId: string, razonSocial: string, score: 'cuit' | 'razonSocial' }` (ver type `Ticket.candidatosPropuestos` en Task 1).
         - `unmatched`: igual que ambiguous pero `candidatosPropuestos: []`.
    4. Commit en batches de 400 (usar `db.batch()` + commit cada 400 ops).
    5. Al final imprimir resumen: `Matcheados: N, ambiguos: N, sin candidato: N, skipped: N`. Escribir `mapping-clienteid.json` en el directorio parent (`apps/sistema-modular/`).

    **CRÍTICO — sin undefined en writes:** ANTES de cada `batch.update(...)`, pasar el payload por un helper local tipo:
    ```js
    function stripUndefined(obj) {
      const out = {};
      for (const [k, v] of Object.entries(obj)) if (v !== undefined) out[k] = v;
      return out;
    }
    ```
    No importar `cleanFirestoreData` del app (es typescript y los scripts son `.mjs` con firebase-admin). Solo replicar la intención.

    **PARTE B — Hidratar los 5 campos en `parseLeadDoc`.** Editar `apps/sistema-modular/src/services/leadsService.ts`, dentro del `return { ... }` de `parseLeadDoc` (línea ~107), agregar ANTES del cierre del objeto (después de `valorEstimado`). Los defaults DEBEN alinearse con las shapes del type `Ticket` (Task 1):

    ```typescript
        // Hidratación de campos de migración clienteId (ver Ticket type en @ags/shared):
        // - `pendienteClienteId` / `revisionDescartada`: boolean (no nullable) — default false
        // - `candidatosPropuestos`: array (no nullable) — default []
        // - `clienteIdMigradoAt` / `clienteIdMigradoPor`: string | null — default null
        pendienteClienteId: data.pendienteClienteId === true,  // coerce truthy a boolean estricto
        candidatosPropuestos: Array.isArray(data.candidatosPropuestos) ? data.candidatosPropuestos : [],
        clienteIdMigradoAt: data.clienteIdMigradoAt?.toDate?.()?.toISOString() ?? (typeof data.clienteIdMigradoAt === 'string' ? data.clienteIdMigradoAt : null),
        clienteIdMigradoPor: typeof data.clienteIdMigradoPor === 'string' ? data.clienteIdMigradoPor : null,
        revisionDescartada: data.revisionDescartada === true,
    ```

    **Por qué `=== true` en vez de `?? false`:** El type dice `pendienteClienteId?: boolean` (no nullable). Si Firestore tuviera `null` por un bug histórico, `?? false` lo dejaría pasar como `null` cuando el slot desambigua con `??`. `=== true` garantiza boolean estricto siempre, alineado con la shape del type.

    NO crear métodos nuevos en `leadsService` en este task (Task 3 lo hace).
  </action>
  <verify>
    <automated>pnpm type-check && node apps/sistema-modular/scripts/migrate-tickets-clienteid.mjs --dry-run 2>&1 | head -40</automated>
  </verify>
  <done>
    - Script corre `--dry-run` sin errores (o con error claro de `service-account.json no encontrado`, si no está presente en la maquina de Claude — eso es esperado y aceptable porque Task 5 es el checkpoint con service account real).
    - `mapping-clienteid.json` se escribe con la estructura `{ matched: [...], ambiguous: [...], unmatched: [...], skipped: [...] }`.
    - `parseLeadDoc` en `leadsService.ts` hidrata los 5 campos nuevos sin romper otros callers.
    - `pnpm type-check` pasa.
    - `grep -c "pendienteClienteId" apps/sistema-modular/scripts/migrate-tickets-clienteid.mjs` devuelve ≥3.
    - `grep -n "score:" apps/sistema-modular/scripts/migrate-tickets-clienteid.mjs` devuelve líneas correspondientes a los candidatos con `score: 'cuit'` o `score: 'razonSocial'`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: leadsService.resolverClienteIdPendiente + descartarRevision + listarPendientes</name>
  <files>apps/sistema-modular/src/services/leadsService.ts</files>
  <behavior>
    Agregar 3 métodos al objeto `leadsService` exportado. Criterios:
    - `resolverClienteIdPendiente(ticketId, clienteId): Promise<void>` escribe `clienteId`, `clienteIdMigradoAt: nowISO`, `clienteIdMigradoPor: uid`, `pendienteClienteId: false`, `updatedAt: Timestamp.now()`. Todo via `deepCleanForFirestore`.
    - `descartarRevisionClienteId(ticketId): Promise<void>` escribe `revisionDescartada: true` + audit trace.
    - `listarPendientesClienteId(): Promise<Ticket[]>` devuelve tickets (tipo `Ticket` de `@ags/shared`, NO `Lead`) leyendo `where('pendienteClienteId', '==', true)` + filtrado client-side de `revisionDescartada`.
  </behavior>
  <action>
    En `apps/sistema-modular/src/services/leadsService.ts`, agregar dentro del objeto `leadsService = { ... }` exportado, al final del objeto (después del último método existente), estos 3 métodos. **Importante: el tipo de retorno de `listarPendientesClienteId` es `Ticket[]`, no `Lead[]`** (el alias `Lead` es deprecated — todos los consumers nuevos usan `Ticket`).

    ```typescript
      async resolverClienteIdPendiente(ticketId: string, clienteId: string): Promise<void> {
        const trace = getCurrentUserTrace();
        const payload = deepCleanForFirestore({
          clienteId,
          clienteIdMigradoAt: Timestamp.now(),
          clienteIdMigradoPor: trace?.uid ?? null,
          pendienteClienteId: false,
          candidatosPropuestos: [],
          ...getUpdateTrace(),
        });
        const ref = doc(db, 'leads', ticketId);
        await updateDoc(ref, payload);
      },

      async descartarRevisionClienteId(ticketId: string): Promise<void> {
        const payload = deepCleanForFirestore({
          revisionDescartada: true,
          pendienteClienteId: false,
          ...getUpdateTrace(),
        });
        const ref = doc(db, 'leads', ticketId);
        await updateDoc(ref, payload);
      },

      async listarPendientesClienteId(): Promise<Ticket[]> {
        // Firestore no soporta `!=` compound eficiente con otro where; filtramos client-side.
        // NOTA (ver sección Notes de este plan): este query usa un único `where` de igualdad sobre
        // un field indexado automáticamente — NO requiere composite index. Si en el futuro se
        // agrega un segundo `where` o un `orderBy` combinado con otro filter, Firestore pedirá
        // crear un composite index via el error mesage del primer run.
        const q = query(collection(db, 'leads'), where('pendienteClienteId', '==', true));
        const snap = await getDocs(q);
        return snap.docs
          .map(d => parseLeadDoc(d))
          .filter(t => t.revisionDescartada !== true);
      },
    ```

    **Sobre el import de `Ticket`:** en el top del archivo, verificar que hay `import type { Ticket } from '@ags/shared';`. Si actualmente el archivo solo importa `Lead` (el alias deprecated), AGREGAR `Ticket` al import. NO remover `Lead` si hay callers internos del módulo que todavía lo referencian — el alias sigue existiendo y no rompe nada. Criterio: el nuevo método `listarPendientesClienteId()` DEBE tipar el retorno como `Promise<Ticket[]>`, no `Promise<Lead[]>`.

    **Reusar exclusivamente:**
    - `getCurrentUserTrace` (ya importado en línea 5).
    - `getUpdateTrace` (ya importado).
    - `deepCleanForFirestore` (ya importado).
    - `Timestamp.now()` (`firebase/firestore`, ya importado).
    - `doc`, `updateDoc`, `query`, `collection`, `where`, `getDocs` (verificar imports en línea 1 — si faltan, añadir al import existente).

    NO crear un nuevo service file. NO componentes React en este task.

    Why: Un solo método por método, nombres verbosos y sin duplicar helpers. `hydrateContactos`/`syncFlatFromContactos` no se tocan. Respeta el budget de 250 líneas — solo agregamos ~40 líneas al archivo.
  </action>
  <verify>
    <automated>pnpm type-check</automated>
  </verify>
  <done>
    - `pnpm type-check` pasa.
    - `grep -n "resolverClienteIdPendiente\|descartarRevisionClienteId\|listarPendientesClienteId" apps/sistema-modular/src/services/leadsService.ts` devuelve las 3 funciones.
    - `grep -n "Promise<Ticket\[\]>" apps/sistema-modular/src/services/leadsService.ts` devuelve al menos 1 línea (el retorno de `listarPendientesClienteId` es `Ticket[]`, no `Lead[]`).
    - Ninguna llamada directa a `setDoc`/`updateDoc` pasa un payload sin `deepCleanForFirestore`.
    - `wc -l apps/sistema-modular/src/services/leadsService.ts` — archivo sigue por debajo de 400 líneas (no violamos el budget de componentes; este no es un componente pero mantenemos higiene).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: RevisionClienteIdPage + ruta /admin/revision-clienteid + link en sidebar Importar</name>
  <files>apps/sistema-modular/src/pages/admin/RevisionClienteIdPage.tsx, apps/sistema-modular/src/pages/admin/index.ts, apps/sistema-modular/src/App.tsx</files>
  <behavior>
    UI admin con criterios:
    - Al entrar, carga `leadsService.listarPendientesClienteId()` y muestra tabla con columnas: razón social, motivo de contacto, candidatos propuestos (badge amber si >0, red si 0), acción.
    - Soporta filtros en URL (con `useUrlFilters`): `soloAmbiguos: boolean`, `search: string` (filtra por razón social client-side).
    - Por cada fila: un `SearchableSelect` para elegir cliente (búsqueda por razón social, opción de usar un candidato propuesto directamente via botón "Usar este") + botón "Descartar (ignorar)".
    - Al resolver: llama `leadsService.resolverClienteIdPendiente()` y quita la fila de la lista.
    - Al descartar: llama `descartarRevisionClienteId()` y quita la fila.
    - Ruta nueva `/admin/revision-clienteid` protegida por rol admin.
    - Componente ≤250 líneas (extraer subcomponentes `RevisionRow` si hace falta).
  </behavior>
  <action>
    **PASO 0 — Pre-implementación: leer shapes reales de servicios a consumir (responde a checker A2).**

    ANTES de escribir cualquier JSX, hacer Read de estos archivos para confirmar exports/firmas reales. NO asumir — los nombres en el código skeleton de abajo pueden ser incorrectos:

    1. `apps/sistema-modular/src/services/clientesService.ts` (primeras ~40 líneas) — confirmar:
       - El nombre exacto del export (¿`clientesService` objeto con métodos, o funciones sueltas?).
       - El método para listar — ¿`list()`, `listAll()`, `getAll()`, `list(filters?)`?
       - La shape del retorno — ¿`Cliente[]`? ¿tiene `id`, `razonSocial`, `cuit` como campos top-level?
       Usar los nombres REALES en los imports y calls de más abajo.

    2. `packages/shared/src/types/index.ts` (buscar `interface Cliente`) — confirmar los campos exactos (`razonSocial`, `cuit`, `id`) para el mapping `cs.map(c => ({ id: c.id, razonSocial: c.razonSocial, cuit: c.cuit ?? null }))`. Si algún campo difiere, ajustar el mapping.

    Si el servicio `clientesService` no existe con el shape esperado (ej: usa react-query hooks en lugar de un service object), adaptar la estrategia: pre-cargar clientes con un hook existente o agregar un método `list()` mínimo al service — pero documentar la decisión en el SUMMARY.

    **PARTE A — Crear `apps/sistema-modular/src/pages/admin/RevisionClienteIdPage.tsx`.**

    Pattern: copiar el shape de `ImportacionDatos.tsx` (tabs mode, colores red/amber, fuente monospace para IDs). Usar atoms existentes:
    - `Card` de `components/ui/Card`
    - `Button` de `components/ui/Button`
    - `SearchableSelect` de `components/ui/SearchableSelect` (búsqueda async por razón social contra `clientesService` — pero reuso mínimo: pasarle como items la lista de clientes pre-cargada una vez al montar).
    - `useUrlFilters` hook (source of truth: skill `list-page-conventions`).

    Estructura del archivo (skeleton — ajustar nombres/firmas según lo leído en PASO 0):
    ```tsx
    // ~150 líneas total target
    import { useEffect, useState } from 'react';
    import { leadsService } from '../../services/leadsService';
    import { clientesService } from '../../services/clientesService'; // ⚠ verificar en PASO 0: nombre exacto del export
    import { useUrlFilters } from '../../hooks/useUrlFilters';
    import { Card } from '../../components/ui/Card';
    import { Button } from '../../components/ui/Button';
    import { SearchableSelect } from '../../components/ui/SearchableSelect';
    import type { Ticket } from '@ags/shared';  // ⚠ NOTA: usar Ticket, NUNCA Lead (alias deprecated)

    interface Filters { soloAmbiguos: boolean; search: string; }

    export default function RevisionClienteIdPage() {
      const [tickets, setTickets] = useState<Ticket[]>([]);
      const [clientes, setClientes] = useState<Array<{id: string; razonSocial: string; cuit?: string|null}>>([]);
      const [loading, setLoading] = useState(true);
      const { filters, setFilter } = useUrlFilters<Filters>({ soloAmbiguos: false, search: '' });

      useEffect(() => {
        (async () => {
          const [ts, cs] = await Promise.all([
            leadsService.listarPendientesClienteId(),
            clientesService.list(),  // ⚠ PASO 0: usar el método REAL del servicio
          ]);
          setTickets(ts);
          setClientes(cs.map(c => ({ id: c.id, razonSocial: c.razonSocial, cuit: c.cuit ?? null })));
          setLoading(false);
        })();
      }, []);

      const visible = tickets.filter(t => {
        if (filters.soloAmbiguos && (t.candidatosPropuestos ?? []).length === 0) return false;
        if (filters.search && !t.razonSocial.toLowerCase().includes(filters.search.toLowerCase())) return false;
        return true;
      });

      async function handleResolver(ticketId: string, clienteId: string) {
        await leadsService.resolverClienteIdPendiente(ticketId, clienteId);
        setTickets(prev => prev.filter(t => t.id !== ticketId));
      }

      async function handleDescartar(ticketId: string) {
        await leadsService.descartarRevisionClienteId(ticketId);
        setTickets(prev => prev.filter(t => t.id !== ticketId));
      }

      // render: Card con título "Revisión de tickets sin cliente"
      // tabla con filtros + filas, cada fila renderiza RevisionRow (subcomponente)
      // badges:
      //   - bg-amber-50 text-amber-800 border-amber-200 para "N candidatos"
      //   - bg-red-50 text-red-800 border-red-200 para "sin candidato"
      //   - labels en uppercase tracking-wide text-[10px] font-mono (Editorial Teal)
      //   - dentro del badge de candidatos, mostrar también el `score` (cuit/razonSocial) en monospace
    }

    function RevisionRow({ ticket, clientes, onResolver, onDescartar }: {...}) {
      // subcomponente por fila. Si la página padre queda >250, mover este a un archivo separado.
      // Al renderizar candidatos propuestos, leer `score` de cada candidato:
      //   candidatosPropuestos?.map(c => <badge>{c.razonSocial} <span className="font-mono text-[10px]">· {c.score}</span></badge>)
    }
    ```

    **Reglas de diseño (Editorial Teal):**
    - Título: `text-xl font-serif` (Newsreader ya cargada).
    - Labels de columna: `font-mono text-[10px] uppercase tracking-wide text-slate-600`.
    - Botón primario: `bg-teal-700 text-white` (atoms ya lo tienen, usar `variant="primary"`).
    - Fila hover: `hover:bg-slate-50`.

    **PARTE B — Registrar en barrel.** En `apps/sistema-modular/src/pages/admin/index.ts`, agregar `export { default as RevisionClienteIdPage } from './RevisionClienteIdPage';`.

    **PARTE C — Ruta en App.tsx.** En `apps/sistema-modular/src/App.tsx`, encontrar la sección donde se registran las rutas admin (grep `/admin/importar`). Agregar:
    ```tsx
    <Route path="/admin/revision-clienteid" element={<AdminGate><RevisionClienteIdPage /></AdminGate>} />
    ```
    Usar el mismo gate/layout que `/admin/importar` existente — copiar el patrón literal.

    **PARTE D — Entrada en sidebar (opcional, dentro del submenú "Importar Datos" o crear separado).** En `apps/sistema-modular/src/components/layout/navigation.ts`, la entrada `Importar Datos` ya existe (línea 60). Agregarle `children` con las dos sub-rutas:
    ```ts
    { name: 'Importar Datos', path: '/admin/importar', icon: '📥', modulo: 'admin',
      children: [
        { name: 'Importar Excel', path: '/admin/importar' },
        { name: 'Revisión clienteId', path: '/admin/revision-clienteid' },
      ],
    },
    ```
    NO cambiar `DESKTOP_MVP_ALLOWED` — `/admin/importar` no estaba en MVP y la revisión tampoco necesita estar (es una herramienta admin, no MVP desktop).

    **Hard rules a respetar:**
    - Componente ≤250 líneas — si `RevisionClienteIdPage` + `RevisionRow` cruzan, mover `RevisionRow` a `apps/sistema-modular/src/pages/admin/components/RevisionClienteIdRow.tsx`.
    - Filtros via `useUrlFilters`, NUNCA `useState` para filtros que deben persistir.
    - No llamar Firestore directamente — solo via `leadsService` y `clientesService`.
  </action>
  <verify>
    <automated>pnpm type-check && pnpm --filter sistema-modular build</automated>
  </verify>
  <done>
    - Build de `sistema-modular` pasa sin errores.
    - `grep -n "revision-clienteid" apps/sistema-modular/src/App.tsx` devuelve la ruta.
    - `wc -l apps/sistema-modular/src/pages/admin/RevisionClienteIdPage.tsx` ≤ 250 (si mayor, `RevisionRow` quedó extraído a `components/`).
    - El barrel `pages/admin/index.ts` exporta `RevisionClienteIdPage`.
    - `grep -c "import type { Ticket }" apps/sistema-modular/src/pages/admin/RevisionClienteIdPage.tsx` ≥ 1 (se importa `Ticket`, no `Lead`).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Correr migración real + validar UI revisión</name>
  <what-built>
    - Script `migrate-tickets-clienteid.mjs` operativo con `--dry-run` / `--run`.
    - Página `/admin/revision-clienteid` funcional con resolver + descartar.
    - Type `Ticket` extendido, `parseLeadDoc` hidrata nuevos campos.
  </what-built>
  <how-to-verify>
    1. Asegurar que `apps/sistema-modular/service-account.json` existe (o setear `SERVICE_ACCOUNT_PATH` env var al path correcto).
    2. `cd apps/sistema-modular && node scripts/migrate-tickets-clienteid.mjs --dry-run`
       - ✔ Debe imprimir "=== DRY RUN ===" y un resumen final "Matcheados: N, ambiguos: N, sin candidato: N, skipped: N".
       - ✔ Debe crear `apps/sistema-modular/mapping-clienteid.json` con las 4 keys.
       - ✔ Firestore NO debe haber cambiado (verificar en Firebase console que los tickets siguen con `clienteId: null` y sin `pendienteClienteId`).
    3. Revisar `mapping-clienteid.json` y confirmar que las resoluciones propuestas tienen sentido (sampling de 5 matched y 5 ambiguous). Los candidatos en `ambiguous` deben tener `score: 'cuit' | 'razonSocial'`.
    4. `node scripts/migrate-tickets-clienteid.mjs --run`
       - ✔ Debe imprimir "=== EJECUCIÓN REAL ===" y el mismo resumen.
       - ✔ En Firebase console, samplear 3 tickets matched: deben tener `clienteId` resuelto + `clienteIdMigradoAt` + `clienteIdMigradoPor: "script"`.
       - ✔ Samplear 3 tickets ambiguous: deben tener `pendienteClienteId: true` + `candidatosPropuestos: [{ clienteId, razonSocial, score }, ...]`.
    5. Levantar la app: `pnpm dev:modular`. Navegar a `http://localhost:5173/admin/revision-clienteid` con un usuario rol `admin`.
       - ✔ La lista muestra los tickets pendientes.
       - ✔ Toggle "solo ambiguos" filtra correctamente (URL refleja `?soloAmbiguos=true`).
       - ✔ Buscar por razón social filtra client-side.
       - ✔ Resolver un ticket con `SearchableSelect`: la fila desaparece, y en Firestore el ticket tiene `clienteId` resuelto + `pendienteClienteId: false` + `clienteIdMigradoPor: <uid admin>`.
       - ✔ Descartar un ticket: la fila desaparece y en Firestore queda `revisionDescartada: true`.
       - ✔ Refrescar la página: los descartados y resueltos ya no aparecen (prueba de persistencia y filtro).
    6. Segundo run del script (`--run`) NO debe modificar nada nuevo (idempotencia sobre los `matched`).
  </how-to-verify>
  <resume-signal>Type "approved" o describir los issues encontrados (p.ej. "hay 3 matches incorrectos por razón social con espacios dobles")</resume-signal>
</task>

</tasks>

<verification>
- `pnpm type-check` pasa.
- `pnpm --filter sistema-modular build` pasa.
- Script migrate corre idempotente (2do `--run` no cambia nada).
- UI revisión resuelve + persiste + filtra por URL.
- Firestore no tiene `undefined` en ningún write (verificado por hook `check-firestore-undefined` al editar `leadsService.ts`).
</verification>

<success_criteria>
- Todos los tickets con `clienteId: null` tienen clienteId resuelto (via script) o están marcados con `pendienteClienteId: true` y visibles en `/admin/revision-clienteid`.
- El modo de resolución manual funciona end-to-end: admin elige cliente, el ticket se actualiza, el registro queda con trazabilidad (`clienteIdMigradoAt` + `clienteIdMigradoPor`).
- El type `Ticket` queda extendido para que Phases 7-10 puedan referenciar `pendienteClienteId` al construir warnings ("cliente no resuelto, mostrar banner en lugar de derivar").
- PREC-01 desbloqueado → FLOW-01, FLOW-02, FLOW-03 pueden implementarse en Phase 8 sin errores silenciosos.
</success_criteria>

## Notes

- **Composite index (addresses checker A3):** El query `listarPendientesClienteId()` usa un único `where('pendienteClienteId', '==', true)` que NO requiere composite index (Firestore indexa single-field automáticamente). Si en el futuro se necesita combinar `pendienteClienteId` con otro `where` o un `orderBy` (p.ej. `orderBy('updatedAt', 'desc')`), Firestore devolverá un error en el primer run con un link para crear el composite index necesario — agregarlo entonces a `firestore.indexes.json` (root del repo, creado en Plan 05-03). No se agrega preventivamente porque el filtrado de `revisionDescartada` se hace client-side (ver comentario en el código de Task 3).

- **Naming `Ticket` vs `Lead`:** El alias `Lead` sigue deprecado pero exportado. NINGÚN consumer nuevo de este plan usa `Lead` — todos importan `Ticket` de `@ags/shared`. Lista de referencias chequeadas:
  - Task 1: type `Ticket` (no `Lead`).
  - Task 3: firma `Promise<Ticket[]>` (no `Promise<Lead[]>`).
  - Task 4: `import type { Ticket } from '@ags/shared'`.

<output>
After completion, create `.planning/phases/05-pre-condiciones-migracion-infra/05-01-SUMMARY.md` documentando:
- Conteos finales del script (`matched`, `ambiguous`, `unmatched`, `skipped`) del run real.
- Decisiones tomadas dentro de Claude's Discretion (naming final de campos, ubicación de sub-ruta).
- Cualquier edge case encontrado (ej: tickets con razón social vacía).
- Link al mapping.json committed o resguardado.
- Shape real encontrada de `clientesService.list()` (PASO 0 de Task 4) y si hubo que adaptar.
</output>
