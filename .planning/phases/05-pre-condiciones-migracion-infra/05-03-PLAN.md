---
phase: 05-pre-condiciones-migracion-infra
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - firebase.json
  - firestore.indexes.json
  - functions/package.json
  - functions/tsconfig.json
  - functions/src/index.ts
  - functions/README.md
  - functions/.gitignore
autonomous: false
requirements:
  - PREC-03

must_haves:
  truths:
    - "El workspace `functions/` existe al root del repo, listado en `pnpm-workspace.yaml` y reconocido por pnpm install"
    - "El package `@ags/functions` compila vía `pnpm --filter @ags/functions build` sin errores TypeScript"
    - "La función HTTP `helloPing` deploya a Firebase en región `southamerica-east1` sin errores"
    - "Un `curl` a la URL de `helloPing` devuelve status 200 con payload `{ ok: true, ts: <number> }`"
  artifacts:
    - path: "functions/package.json"
      provides: "Workspace @ags/functions con firebase-functions + firebase-admin + scripts build/deploy"
      contains: "firebase-functions"
    - path: "functions/src/index.ts"
      provides: "Sample Cloud Function helloPing (onRequest) — placeholder, sin lógica de negocio"
      contains: "helloPing"
    - path: "firebase.json"
      provides: "Config raíz de Firebase declarando el workspace functions + firestore indexes"
      contains: "functions"
    - path: "pnpm-workspace.yaml"
      provides: "Inclusión del workspace functions"
      contains: "functions"
  key_links:
    - from: "firebase.json"
      to: "functions/"
      via: "sección `functions` con `source: functions`, `predeploy: pnpm --filter @ags/functions build`"
      pattern: "\"source\":\\s*\"functions\""
    - from: "functions/src/index.ts"
      to: "Firebase runtime (HTTP onRequest)"
      via: "export helloPing = functions.region('southamerica-east1').https.onRequest(...)"
      pattern: "southamerica-east1"
---

<objective>
Bootstrapear el workspace `functions/` del monorepo con una Cloud Function HTTP de ejemplo (`helloPing`). Este es infra puro: no hay lógica de negocio, es únicamente la base sobre la que Phase 9 construirá `updateResumenStock` (STKP-02) y otras funciones futuras. El criterio de éxito es `firebase deploy --only functions:helloPing` corriendo sin errores + un curl devolviendo 200.

Purpose: Sin este workspace, Phase 9 arranca con 1 día extra de setup antes de poder hacer el trigger real. Lo separamos ahora para que el deploy pipeline esté validado cuando necesitemos lógica real.
Output: Nueva carpeta `functions/` al root con `package.json`, `tsconfig.json`, `src/index.ts`; entrada en `pnpm-workspace.yaml`; `firebase.json` creado al root del repo; función deployable y verificable via HTTP.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-pre-condiciones-migracion-infra/05-CONTEXT.md

@CLAUDE.md

@pnpm-workspace.yaml
@.firebaserc
@package.json

<interfaces>
<!-- Contratos esperados del workspace. No hay código existente — todo se crea desde cero. -->

Estructura final deseada:
```
/
├── firebase.json               # NEW — apunta a functions/, firestore.rules, firestore.indexes.json
├── firestore.indexes.json      # NEW — estructura vacía `{ indexes: [], fieldOverrides: [] }`
├── firestore.rules             # EXISTING — no tocar, solo referenciar en firebase.json
├── pnpm-workspace.yaml         # UPDATE — agregar `'functions'`
├── functions/                  # NEW WORKSPACE
│   ├── package.json            # @ags/functions, firebase-functions, firebase-admin
│   ├── tsconfig.json           # extends monorepo tsconfig base si aplicable
│   ├── .gitignore              # node_modules, lib/, .env
│   ├── src/
│   │   └── index.ts            # export helloPing
│   └── README.md               # cómo buildear + deployar
└── .firebaserc                 # EXISTING — project 'agssop-e7353'
```

Package shape target (`functions/package.json`):
```json
{
  "name": "@ags/functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "pnpm build && firebase emulators:start --only functions",
    "deploy": "pnpm build && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
```

Sample function (`functions/src/index.ts`):
```typescript
import * as functions from 'firebase-functions/v2/https';

const REGION = 'southamerica-east1';

/**
 * HelloPing — función de ejemplo / sanity check del pipeline de deploy.
 * No tiene lógica de negocio. Sirve para validar que el workspace está bien configurado.
 *
 * Usage:
 *   curl https://southamerica-east1-agssop-e7353.cloudfunctions.net/helloPing
 *   → { ok: true, ts: 1729...,  region: 'southamerica-east1' }
 */
export const helloPing = functions.onRequest({ region: REGION }, (req, res) => {
  res.status(200).json({
    ok: true,
    ts: Date.now(),
    region: REGION,
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear workspace functions/ y registrarlo en pnpm-workspace.yaml</name>
  <files>pnpm-workspace.yaml, functions/package.json, functions/tsconfig.json, functions/src/index.ts, functions/README.md, functions/.gitignore</files>
  <behavior>
    Al final de este task:
    - `pnpm install` al root resuelve el workspace y crea `node_modules/@ags/functions` (o symlink).
    - `pnpm --filter @ags/functions build` compila TS → `functions/lib/index.js`.
    - `functions/lib/index.js` exporta `helloPing`.
  </behavior>
  <action>
    **1. `pnpm-workspace.yaml`** — AGREGAR `'functions'` al array. El archivo actual es:
    ```yaml
    packages:
      - 'apps/*'
      - 'packages/*'
    ```
    Resultado:
    ```yaml
    packages:
      - 'apps/*'
      - 'packages/*'
      - 'functions'
    ```
    Nota: usamos el path literal `functions` (no `functions/*`) porque es un único workspace al root, no un directorio contenedor.

    **2. `functions/package.json`** — crear con EL SHAPE EXACTO del `<interfaces>` block de arriba. Versiones específicas:
    - `firebase-functions`: `^6.0.0` (v2 API con soporte nativo de region + Node 20).
    - `firebase-admin`: `^13.0.0` (alineado con runtime Node 20 de functions).
    - `typescript`: `^5.8.0` (mismo major que el monorepo — el root tiene TS 5.8).

    **3. `functions/tsconfig.json`** — config estándar para Cloud Functions v2:
    ```json
    {
      "compilerOptions": {
        "module": "commonjs",
        "target": "es2022",
        "lib": ["es2022"],
        "moduleResolution": "node",
        "outDir": "lib",
        "sourceMap": true,
        "strict": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "resolveJsonModule": true,
        "noImplicitReturns": true
      },
      "compileOnSave": true,
      "include": ["src/**/*"],
      "exclude": ["lib", "node_modules"]
    }
    ```
    NO intentar extender desde un tsconfig base del monorepo (`functions` no puede depender de paths TS que no existan en runtime del deploy — keep it self-contained).

    **4. `functions/src/index.ts`** — crear EXACTO el contenido del `<interfaces>` block. Región hardcoded `southamerica-east1`. Sin imports de `@ags/shared` — mantenerlo self-contained (Phase 9 decidirá si vale la pena importar shared types vía workspace).

    **5. `functions/.gitignore`** — crear con:
    ```
    node_modules/
    lib/
    .env
    .env.local
    *.log
    ```

    **6. `functions/README.md`** — crear con comandos operativos:
    ```markdown
    # @ags/functions

    Workspace de Cloud Functions para el monorepo AGS. Región: `southamerica-east1`.

    ## Scripts

    ```bash
    pnpm --filter @ags/functions build         # Compila TS → lib/
    pnpm --filter @ags/functions build:watch   # Build en watch
    pnpm --filter @ags/functions deploy        # Build + firebase deploy --only functions
    pnpm --filter @ags/functions logs          # Tail de logs de functions
    pnpm --filter @ags/functions serve         # Emulador local
    ```

    ## Funciones actuales

    - `helloPing` — HTTP onRequest, sanity check. Devuelve `{ ok, ts, region }`.

    Futuras funciones (Phase 9 en adelante):
    - `updateResumenStock` — trigger onDocumentWritten para denormalización de stock.

    ## Deploy manual (primera vez)

    ```bash
    cd <repo root>
    firebase login                              # una sola vez
    firebase use agssop-e7353                   # apuntar al proyecto
    pnpm --filter @ags/functions build
    firebase deploy --only functions:helloPing
    ```

    URL resultante (para curl): `https://southamerica-east1-agssop-e7353.cloudfunctions.net/helloPing`
    ```

    **7. Instalar dependencias.** Correr en background:
    ```bash
    pnpm install
    ```
    Esto debe resolver el workspace. Si hay errores de peer deps, documentar pero no bloquear — Task 2 los confirma.

    **Hard rules NO aplican aquí:** `functions/` no es `apps/reportes-ot/`, no es Firestore write (es infra), no tiene componentes React. La regla de `undefined` tampoco aplica (la función no escribe a Firestore en v1).
  </action>
  <verify>
    <automated>pnpm install && pnpm --filter @ags/functions build && ls functions/lib/index.js</automated>
  </verify>
  <done>
    - `pnpm install` completa sin errores nuevos.
    - `functions/lib/index.js` existe post-build.
    - `node -e "console.log(require('./functions/lib/index.js').helloPing)"` imprime un objeto (no undefined) — confirma que `helloPing` está exportado.
    - `pnpm-workspace.yaml` tiene `'functions'` en el array.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear firebase.json + firestore.indexes.json al root</name>
  <files>firebase.json, firestore.indexes.json</files>
  <behavior>
    - `firebase.json` al root declara `functions` con source y predeploy hook, más `firestore.rules` y `firestore.indexes.json` existentes.
    - `firestore.indexes.json` existe con estructura vacía `{ indexes: [], fieldOverrides: [] }`.
    - `firebase deploy --only functions` (dry-run o con confirm) reconoce la config sin warnings.
  </behavior>
  <action>
    **1. Crear `firebase.json` al root del repo.** Shape:
    ```json
    {
      "firestore": {
        "rules": "firestore.rules",
        "indexes": "firestore.indexes.json"
      },
      "functions": [
        {
          "source": "functions",
          "codebase": "default",
          "ignore": [
            "node_modules",
            ".git",
            "firebase-debug.log",
            "firebase-debug.*.log",
            "*.local"
          ],
          "predeploy": [
            "pnpm --filter @ags/functions build"
          ]
        }
      ]
    }
    ```
    Notas:
    - `source: functions` apunta al workspace relativo al root.
    - `predeploy` garantiza que `lib/` está fresco antes de deployar.
    - No declaramos `hosting` — no aplica al monorepo actual (Vercel hostea).
    - No declaramos `emulators` — Phase 11 lo agregará cuando implementemos TEST-01.

    **2. Crear `firestore.indexes.json` al root.** Shape mínimo:
    ```json
    {
      "indexes": [],
      "fieldOverrides": []
    }
    ```
    Este archivo debe existir para que `firebase deploy` no falle al validar indexes. Queda vacío por ahora; las migraciones + fases siguientes agregarán índices según necesidad.

    **3. Verificar que `firestore.rules` ya existe al root.** (ls confirma que está.) NO tocarlo.

    **4. `.firebaserc` ya existe y apunta a `agssop-e7353`.** NO tocarlo.

    **Post-creación** correr:
    ```bash
    firebase --version   # confirmar CLI instalada
    ```
    Si no está instalada (exit code ≠ 0), documentar en la summary: "CLI firebase-tools no instalada en el entorno de Claude — el deploy real se hace en Task 3 (human checkpoint) con `npm i -g firebase-tools` o equivalente".

    NO correr `firebase deploy` en este task — eso requiere login interactivo y es parte del checkpoint de Task 3.
  </action>
  <verify>
    <automated>test -f firebase.json && test -f firestore.indexes.json && cat firebase.json | node -e "const c = JSON.parse(require('fs').readFileSync(0, 'utf8')); if (!c.functions || !c.functions[0] || c.functions[0].source !== 'functions') process.exit(1); console.log('OK')"</automated>
  </verify>
  <done>
    - `firebase.json` existe al root con sección `functions` apuntando a `source: "functions"`.
    - `firestore.indexes.json` existe al root con estructura vacía válida.
    - `firestore.rules` sigue intacto al root.
    - El JSON de `firebase.json` parsea correctamente (node JSON.parse no falla).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy real de helloPing + curl de validación</name>
  <what-built>
    - Workspace `functions/` con sample function `helloPing`.
    - `firebase.json` + `firestore.indexes.json` al root.
    - Predeploy hook que buildea antes de subir.
    - README con comandos documentados.
  </what-built>
  <how-to-verify>
    1. **Prereqs** (si no están):
       ```bash
       npm i -g firebase-tools
       firebase login
       firebase use agssop-e7353
       ```
    2. **Build local** (sanity):
       ```bash
       pnpm --filter @ags/functions build
       ls functions/lib/
       # ✔ Debe listar index.js, index.d.ts, index.js.map
       ```
    3. **Deploy dry-ish** (confirmar que firebase CLI reconoce la config):
       ```bash
       firebase deploy --only functions:helloPing --dry-run 2>&1 | head -20
       ```
       - ✔ Debe mostrar que detecta `helloPing` en `southamerica-east1` sin errores de sintaxis/config.
       - Si dice "Project not selected" o similar → verificar `.firebaserc` y `firebase use`.
    4. **Deploy real:**
       ```bash
       firebase deploy --only functions:helloPing
       ```
       - ✔ Debe completar con "✔  functions[helloPing(southamerica-east1)]: Successful create operation." o similar.
       - ✔ Al final imprime la URL de la función.
    5. **Curl de validación:**
       ```bash
       curl -i https://southamerica-east1-agssop-e7353.cloudfunctions.net/helloPing
       ```
       - ✔ Status code: `200 OK`.
       - ✔ Body JSON: `{ "ok": true, "ts": <number>, "region": "southamerica-east1" }`.
       - ✔ `ts` es un timestamp reciente (última hora).
    6. **Firebase Console:**
       - Abrir https://console.firebase.google.com/project/agssop-e7353/functions
       - ✔ `helloPing` aparece listada, región `southamerica-east1`, runtime `nodejs20`.
    7. **Re-deploy idempotente (opcional):**
       ```bash
       firebase deploy --only functions:helloPing
       ```
       - ✔ Debe decir "Successful update operation" en lugar de crear. Sin errores.
  </how-to-verify>
  <resume-signal>Type "approved" o pegar el error del deploy / output del curl si algo falla</resume-signal>
</task>

</tasks>

<verification>
- `functions/lib/index.js` compilado.
- `firebase deploy --only functions:helloPing` sin errores.
- `curl` a la URL devuelve 200 + JSON esperado.
- `pnpm install` al root resuelve el workspace.
- NO hay impacto en `apps/*` — ningún archivo ahí tocado.
</verification>

<success_criteria>
- Criterio ROADMAP Phase 5 #3: "El workspace `functions/` existe en el monorepo y despliega una función de ejemplo sin errores".
- Phase 9 (STKP-02 `updateResumenStock`) puede arrancar directamente en `functions/src/updateResumenStock.ts` sin setup extra.
- El equipo tiene un runbook mínimo (README) para deploy manual.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pre-condiciones-migracion-infra/05-03-SUMMARY.md` documentando:
- URL exacta de `helloPing` post-deploy.
- Versiones finales instaladas de `firebase-functions` y `firebase-admin`.
- Cualquier warning del deploy (ej: peer deps, Node version mismatch) que haya sido ignorable.
- Confirmación del `curl` devolviendo 200.
- Nota de que Phase 9 (STKP-02) construye encima de este bootstrap sin setup adicional.
</output>
