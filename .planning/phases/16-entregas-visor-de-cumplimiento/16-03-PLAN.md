---
phase: 16-entregas-visor-de-cumplimiento
plan: 03
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - apps/sistema-modular/src/utils/entregasResolver.ts
autonomous: true
requirements: [ENT-01, ENT-02, ENT-03, ENT-04, ENT-05]
must_haves:
  truths:
    - "`computeSemaforo(diasRestantes)` clasifica correctamente verde / amarillo / rojo / sin_eta según los rangos lockeados (>5 = verde, 0..5 = amarillo, <0 = rojo, null = sin_eta)"
    - "`computeSemaforo` con `opts.entregado=true` retorna 'entregado' (override de cualquier otro estado)"
    - "`computeEtaFecha(fechaAceptacionIso, etaDiasEstimados)` retorna ISO date (UTC midnight) o null si falta dato"
    - "`buildEntregaRows(input)` produce una EntregaRow por item de cada presupuesto, con joins correctos por `presupuestoItemId`"
    - "Items sin etaDiasEstimados o ppto sin fechaAceptacion → semaforo='sin_eta', diasRestantes=null (no crash)"
    - "Items con importacion.estado === 'recibido' → semaforo='entregado' (override)"
    - "Comparación de días usa start-of-day para evitar off-by-one por timezone (Pitfall 5 del RESEARCH)"
    - "Wave 0 suite `pnpm --filter @ags/sistema-modular test:entregas` queda GREEN al combinarse con el efecto de 16-02 (no requiere 16-02 para los tests puros que NO leen Firestore — los fixtures ya proveen fechaAceptacion/presupuestoItemId)"
  artifacts:
    - path: "apps/sistema-modular/src/utils/entregasResolver.ts"
      provides: "Pure functions implementadas (replace stubs of 16-01)"
      contains: "if (diasRestantes === null)"
  key_links:
    - from: "apps/sistema-modular/src/utils/entregasResolver.ts"
      to: "apps/sistema-modular/src/__tests__/entregasResolver.test.ts"
      via: "computeSemaforo / computeEtaFecha / buildEntregaRows exports → tests pasan"
      pattern: "computeSemaforo|computeEtaFecha|buildEntregaRows"
---

<objective>
Reemplazar los 3 stubs `NotImplemented` en `apps/sistema-modular/src/utils/entregasResolver.ts` (creados en Plan 16-01) por implementaciones puras que hacen pasar el suite ENT-01..ENT-06 a GREEN.

Purpose: Esta es la lógica de negocio reutilizable — el hook `useEntregas` del Plan 16-05 simplemente carga datos de Firestore y delega a `buildEntregaRows`. Mantener las 3 funciones puras + sin I/O garantiza que el suite siga GREEN sin emulador.

Output: 3 funciones implementadas, Wave 0 suite GREEN end-to-end, archivo ≤ 250 líneas.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-CONTEXT.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-RESEARCH.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/components.md

# Source files
@apps/sistema-modular/src/utils/entregasResolver.ts
@apps/sistema-modular/src/__tests__/entregasResolver.test.ts
@apps/sistema-modular/src/__tests__/fixtures/entregas.ts

<interfaces>
<!-- Signatures from Plan 16-01 (must remain identical): -->
```typescript
export function computeSemaforo(diasRestantes: number | null, opts?: { entregado?: boolean }): Semaforo;
export function computeEtaFecha(fechaAceptacionIso: string | null, etaDiasEstimados: number | null): string | null;
export function buildEntregaRows(input: BuildEntregaRowsInput): EntregaRow[];
```

<!-- Semáforo logic locked in CONTEXT.md + planning_context: -->
- entregado override (opts.entregado=true) → 'entregado'
- diasRestantes === null → 'sin_eta'
- diasRestantes > 5 → 'verde'
- 0 <= diasRestantes <= 5 → 'amarillo'
- diasRestantes < 0 → 'rojo'

<!-- Join logic for buildEntregaRows: -->
- requerimientos by presupuestoItemId: build Map<string, RequerimientoCompra> keyed by `req.presupuestoItemId`.
  Fallback for legacy reqs (presupuestoItemId === null): no join, item shows as if no req.
- ocItem → req: build Map<string, { ocNumero, ocId }> keyed by `ocItem.requerimientoId`, value derived from the parent OC.
- itemImportacion → req: build Map<string, { impId, impNumero, impEstado }> keyed by `itemImp.requerimientoId`.
- 'entregado' override condition: importacion.estado === 'recibido' OR (cantidadRecibida >= cantidadPedida for itemImp).
  Note: OT cierre administrativo also marks 'entregado', but this resolver does NOT load OTs (separate concern, can wire in 16-05 if needed; keep this fn pure on the input shape).

<!-- Date math (Pitfall 5 from RESEARCH): -->
- computeEtaFecha: parse fechaAceptacionIso as UTC, add etaDiasEstimados days, return ISO string of UTC midnight.
- diasRestantes inside buildEntregaRows: use start-of-day in UTC for both eta and now:
  ```typescript
  const startOfDay = (d: Date) => Date.UTC(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate());
  const days = Math.round((startOfDay(eta) - startOfDay(now)) / 86400000);
  ```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar computeSemaforo + computeEtaFecha + buildEntregaRows</name>
  <files>apps/sistema-modular/src/utils/entregasResolver.ts</files>
  <behavior>
    - Mantener intactos: imports, type exports (`Semaforo`, `EntregaRow`, `BuildEntregaRowsInput`), `SEMAFORO_COLORS`, `SEMAFORO_LABELS`.
    - Reemplazar las 3 stubs por implementaciones puras (sin Firestore, sin async, sin Date.now() — usa input.now ?? new Date()).
    - El archivo entero debe quedar ≤ 250 líneas (regla `components.md` aplica a `.tsx` pero la mantenemos como aspirational para `.ts`).
    - Comentar cada función con su intención (1-2 líneas).
  </behavior>
  <action>
**Editar** `apps/sistema-modular/src/utils/entregasResolver.ts`. Mantener todo el preámbulo (imports + types + constants) intacto. Reemplazar los tres stubs `throw new Error('NotImplemented')`:

**1. `computeSemaforo`:**

```typescript
export function computeSemaforo(
  diasRestantes: number | null,
  opts?: { entregado?: boolean },
): Semaforo {
  if (opts?.entregado) return 'entregado';
  if (diasRestantes === null) return 'sin_eta';
  if (diasRestantes > 5) return 'verde';
  if (diasRestantes >= 0) return 'amarillo';
  return 'rojo';
}
```

**2. `computeEtaFecha`:**

```typescript
export function computeEtaFecha(
  fechaAceptacionIso: string | null,
  etaDiasEstimados: number | null,
): string | null {
  if (!fechaAceptacionIso || etaDiasEstimados == null) return null;
  const base = new Date(fechaAceptacionIso);
  if (isNaN(base.getTime())) return null;
  // Sumar días en UTC para evitar drift por DST/timezone.
  const eta = new Date(Date.UTC(
    base.getUTCFullYear(),
    base.getUTCMonth(),
    base.getUTCDate() + etaDiasEstimados,
  ));
  return eta.toISOString();
}
```

**3. `buildEntregaRows`:** (este es el más sustancial — ~70 LOC con joins)

```typescript
/** Comparación a nivel de día completo en UTC (Pitfall 5 del RESEARCH — evita off-by-one). */
function diasEntre(etaIso: string, now: Date): number {
  const eta = new Date(etaIso);
  const etaUtc = Date.UTC(eta.getUTCFullYear(), eta.getUTCMonth(), eta.getUTCDate());
  const nowUtc = Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), now.getUTCDate());
  return Math.round((etaUtc - nowUtc) / 86400000);
}

export function buildEntregaRows(input: BuildEntregaRowsInput): EntregaRow[] {
  const now = input.now ?? new Date();

  // 1) Indexar requerimientos por presupuestoItemId (Plan 16-02 garantiza este campo
  //    para presupuestos aceptados pos-Phase16). Legacy: presupuestoItemId === null → no join.
  const reqByItemId = new Map<string, RequerimientoCompra>();
  for (const req of input.requerimientos) {
    if (req.presupuestoItemId) reqByItemId.set(req.presupuestoItemId, req);
  }

  // 2) Indexar ocItem.requerimientoId → { ocNumero, ocId }.
  const ocByReqId = new Map<string, { ocId: string; ocNumero: string }>();
  for (const oc of input.ordenesCompra) {
    for (const ocItem of oc.items) {
      if (ocItem.requerimientoId) {
        ocByReqId.set(ocItem.requerimientoId, { ocId: oc.id, ocNumero: oc.numero });
      }
    }
  }

  // 3) Indexar itemImportacion.requerimientoId → resumen importación.
  type ImpResumen = {
    impId: string;
    impNumero: string;
    impEstado: string;
    entregado: boolean;       // override semáforo
  };
  const impByReqId = new Map<string, ImpResumen>();
  for (const imp of input.importaciones) {
    for (const itemImp of (imp.items ?? [])) {
      if (!itemImp.requerimientoId) continue;
      const entregado =
        imp.estado === 'recibido' ||
        ((itemImp.cantidadRecibida ?? 0) >= itemImp.cantidadPedida);
      impByReqId.set(itemImp.requerimientoId, {
        impId: imp.id,
        impNumero: imp.numero,
        impEstado: imp.estado as string,
        entregado,
      });
    }
  }

  // 4) Construir filas — una por item de presupuesto activo.
  const rows: EntregaRow[] = [];
  for (const ppto of input.presupuestos) {
    const clienteNombre = input.clienteNombreById.get(ppto.clienteId) ?? '—';
    for (const item of (ppto.items ?? [])) {
      const req = item.id ? reqByItemId.get(item.id) ?? null : null;
      const oc = req ? ocByReqId.get(req.id) ?? null : null;
      const imp = req ? impByReqId.get(req.id) ?? null : null;

      const etaFecha = computeEtaFecha(
        ppto.fechaAceptacion ?? null,
        item.etaDiasEstimados ?? null,
      );
      const diasRestantes = etaFecha ? diasEntre(etaFecha, now) : null;
      const semaforo = computeSemaforo(diasRestantes, { entregado: imp?.entregado === true });

      rows.push({
        presupuestoId: ppto.id,
        presupuestoNumero: ppto.numero,
        itemId: item.id ?? `${ppto.id}::${rows.length}`,
        clienteId: ppto.clienteId,
        clienteNombre,
        descripcion: item.descripcion,
        cantidad: item.cantidad,
        precioUnitario: item.precioUnitario,
        moneda: (item.moneda ?? null) as EntregaRow['moneda'],
        disponibilidad: (item.disponibilidad ?? null) as EntregaRow['disponibilidad'],
        etaDiasEstimados: item.etaDiasEstimados ?? null,
        fechaAceptacion: ppto.fechaAceptacion ?? null,
        etaFecha,
        diasRestantes,
        semaforo,
        otNumeroVinculada: item.otNumeroVinculada ?? null,
        requerimientoId: req?.id ?? null,
        requerimientoNumero: req?.numero ?? null,
        ocNumero: oc?.ocNumero ?? null,
        importacionId: imp?.impId ?? null,
        importacionNumero: imp?.impNumero ?? null,
        importacionEstado: imp?.impEstado ?? null,
      });
    }
  }
  return rows;
}
```

**Verificación post-edición:**
```bash
pnpm --filter @ags/sistema-modular test:entregas
```
Esperado: **6/6 GREEN**. Stdout incluye `[ENT-01]`..`[ENT-06]` con `pass` cada uno.

Si algún test falla, revisar:
- ENT-01 boundary `diasRestantes=5` debe ser `'amarillo'` (no `'verde'`).
- ENT-02 fecha esperada 2026-06-14 — verificar que la suma en UTC no devuelve 2026-06-13 (timezone drift).
- ENT-03 join via presupuestoItemId; verificar `item.id` siempre presente en fixture.
- ENT-05 `entregado` override: el test sobrescribe el item con cantidadRecibida=1 — el override entregado debe ganar sobre rojo.
- ENT-06 item sin req → row debe seguir apareciendo con campos req/oc/imp en null.
  </action>
  <verify>
    <automated>pnpm --filter @ags/sistema-modular test:entregas</automated>
  </verify>
  <done>
    - 3 funciones implementadas (sin throw NotImplemented restantes).
    - Tests `[ENT-01]`..`[ENT-06]` todos GREEN (exit code 0).
    - Archivo ≤ 250 líneas.
    - `pnpm type-check` GREEN.
  </done>
</task>

</tasks>

<verification>
1. Unit suite GREEN:
   ```bash
   pnpm --filter @ags/sistema-modular test:entregas
   ```
2. Confirmar zero residual stubs:
   ```bash
   grep -c "NotImplemented" apps/sistema-modular/src/utils/entregasResolver.ts
   ```
   Esperado: `0`.
3. Smoke contra suites previas (no debe regresionar):
   ```bash
   pnpm --filter @ags/sistema-modular test:entregas && pnpm --filter @ags/sistema-modular test:patron-bom && pnpm --filter @ags/sistema-modular test:venta-loaner
   ```
</verification>

<success_criteria>
- [ ] `apps/sistema-modular/src/utils/entregasResolver.ts` ya no tiene `throw new Error('NotImplemented')`.
- [ ] 6/6 tests de `test:entregas` GREEN.
- [ ] Otras suites (`test:patron-bom`, `test:venta-loaner`, etc.) siguen GREEN (no regresión).
- [ ] `pnpm type-check` GREEN.
- [ ] Archivo ≤ 250 líneas.
</success_criteria>

<output>
After completion, create `.planning/phases/16-entregas-visor-de-cumplimiento/16-03-SUMMARY.md` con:
- Confirmación 6/6 GREEN (copy-paste stdout líneas `[ENT-XX]`).
- Confirmación de no-regresión en suites existentes.
- Próximo paso: 16-04 (editor de presupuestos — UI fields para `disponibilidad` + `etaDiasEstimados`).
</output>
