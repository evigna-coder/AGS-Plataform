---
phase: 16-entregas-visor-de-cumplimiento
plan: 05
type: execute
wave: 3
depends_on: ["16-03", "16-04"]
files_modified:
  - apps/sistema-modular/src/pages/entregas/index.tsx
  - apps/sistema-modular/src/pages/entregas/EntregasList.tsx
  - apps/sistema-modular/src/pages/entregas/EntregasFilters.tsx
  - apps/sistema-modular/src/pages/entregas/EntregaRow.tsx
  - apps/sistema-modular/src/hooks/useEntregas.ts
  - apps/sistema-modular/src/components/layout/navigation.ts
  - apps/sistema-modular/src/components/layout/TabContentManager.tsx
autonomous: true
requirements: [ENT-03]
must_haves:
  truths:
    - "La ruta `/entregas` existe y monta `EntregasList`. Está bajo `Stock > Compras` en el sidebar (vía navigation.ts)."
    - "`useEntregas()` carga presupuestos aceptados + reqs + OCs + importaciones + clientes via `Promise.all` y delega a `buildEntregaRows` (del Plan 16-03) para producir las filas."
    - "EntregasList renderiza la lista con PageHeader + filtros + tabla resizable + SortableHeader (patrón list-page-conventions del repo)."
    - "Filtros usan `useUrlFilters` (NO useState): clienteId, semaforo, estadoImportacion, search."
    - "Columnas visibles: Cliente, Item, Cantidad, Valor unit + moneda, Presupuesto#, OT# (editable inline), OC#, IMP# + estado, ETA, Días restantes (con semáforo)."
    - "Edición inline de OT#: input en la fila, blur o Enter guarda via `presupuestosService.update(pres.id, { items: [...updatedItems] })` con `deepCleanForFirestore`."
    - "Items sin ETA muestran badge gris 'Sin ETA' (no rompen rendering — Pitfall 1 del RESEARCH)."
    - "El componente EntregasList ≤ 250 LOC (extracción de EntregasFilters + EntregaRow sub-componentes)."
  artifacts:
    - path: "apps/sistema-modular/src/pages/entregas/EntregasList.tsx"
      provides: "List page principal con PageHeader, filtros, tabla, sort"
      contains: "useUrlFilters"
    - path: "apps/sistema-modular/src/pages/entregas/EntregasFilters.tsx"
      provides: "Sub-componente con SearchableSelect cliente + select semaforo + select estado imp + search"
    - path: "apps/sistema-modular/src/pages/entregas/EntregaRow.tsx"
      provides: "Fila editable con input inline para OT# + badge semáforo + badges disponibilidad"
    - path: "apps/sistema-modular/src/hooks/useEntregas.ts"
      provides: "Hook que orquesta Promise.all + clienteNombreById Map + delega a buildEntregaRows + handlers de update OT#"
      exports: ["useEntregas"]
    - path: "apps/sistema-modular/src/pages/entregas/index.tsx"
      provides: "Barrel re-export"
      exports: ["EntregasList"]
    - path: "apps/sistema-modular/src/components/layout/navigation.ts"
      provides: "Sidebar: Stock > Compras > Entregas link"
      contains: "/entregas"
    - path: "apps/sistema-modular/src/components/layout/TabContentManager.tsx"
      provides: "Route /entregas montado con ProtectedRoute"
      contains: "/entregas"
  key_links:
    - from: "apps/sistema-modular/src/hooks/useEntregas.ts"
      to: "apps/sistema-modular/src/utils/entregasResolver.ts"
      via: "buildEntregaRows() call"
      pattern: "buildEntregaRows"
    - from: "apps/sistema-modular/src/hooks/useEntregas.ts"
      to: "Firestore (4 colecciones en paralelo)"
      via: "Promise.all([presupuestosService, requerimientosService, ordenesCompraService, importacionesService, clientesService])"
      pattern: "Promise\\.all"
    - from: "apps/sistema-modular/src/pages/entregas/EntregasList.tsx"
      to: "apps/sistema-modular/src/hooks/useEntregas.ts"
      via: "useEntregas() hook call"
      pattern: "useEntregas"
    - from: "apps/sistema-modular/src/components/layout/navigation.ts"
      to: "apps/sistema-modular/src/components/layout/TabContentManager.tsx"
      via: "path /entregas matches route registration"
      pattern: "'/entregas'"
    - from: "apps/sistema-modular/src/pages/entregas/EntregaRow.tsx"
      to: "apps/sistema-modular/src/services/presupuestosService.ts"
      via: "presupuestosService.update on blur / Enter"
      pattern: "presupuestosService\\.update"
---

<objective>
Construir la vista `/entregas` end-to-end: hook `useEntregas` que orquesta el resolver (delegando la lógica pura al Plan 16-03), página `EntregasList` siguiendo el patrón list-page-conventions del repo, sidebar entry bajo `Stock > Compras > Entregas`, ruta protegida en `TabContentManager.tsx`, edición inline de OT#.

Purpose: Cerrar el loop visual. La coordinadora abre `/entregas`, ve la planilla con semáforo, filtra por cliente/semaforo, escribe OT# en línea cuando lo sabe. Sin auto-cosecha, sin notificaciones — visibilidad operativa pura.

Output: Página funcional con datos reales en Firestore, filtros en URL, edición inline. Listo para el UAT del Plan 16-06.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-CONTEXT.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-RESEARCH.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-03-SUMMARY.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-04-SUMMARY.md
@CLAUDE.md
@.claude/rules/components.md
@.claude/rules/firestore.md
@.claude/skills/list-page-conventions/SKILL.md

# Referencia perfecta de list-page
@apps/sistema-modular/src/pages/stock/ImportacionesList.tsx
@apps/sistema-modular/src/hooks/useImportaciones.ts

# Resolver puro (Plan 16-03) — consumido por el hook
@apps/sistema-modular/src/utils/entregasResolver.ts

# Sidebar + route registration patterns
@apps/sistema-modular/src/components/layout/navigation.ts
@apps/sistema-modular/src/components/layout/TabContentManager.tsx

# Services consumidos (read-only references)
@apps/sistema-modular/src/services/presupuestosService.ts
@apps/sistema-modular/src/services/importacionesService.ts
@apps/sistema-modular/src/services/clientesService.ts

<interfaces>
<!-- Resolver (from Plan 16-03): -->
```typescript
export function buildEntregaRows(input: BuildEntregaRowsInput): EntregaRow[];
export interface BuildEntregaRowsInput {
  presupuestos: Array<Pick<Presupuesto, 'id' | 'numero' | 'clienteId' | 'estado' | 'items' | 'fechaAceptacion'>>;
  requerimientos: RequerimientoCompra[];
  ordenesCompra: Array<{ id: string; numero: string; items: { id: string; requerimientoId?: string | null }[] }>;
  importaciones: Array<Pick<Importacion, 'id' | 'numero' | 'estado' | 'items'>>;
  clienteNombreById: Map<string, string>;
  now?: Date;
}
```

<!-- Services to call: -->
```typescript
presupuestosService.getAll(filters?: { clienteId?: string; estado?: Presupuesto['estado'] }): Promise<Presupuesto[]>
requerimientosService.getAll(filters?: { origen?: string; presupuestoId?: string }): Promise<RequerimientoCompra[]>
ordenesCompraService.getAll(filters?: { estado?: string; tipo?: string; proveedorId?: string }): Promise<OrdenCompra[]>
importacionesService.getAll(filters?: { estado?: EstadoImportacion }): Promise<Importacion[]>
clientesService.getAll(): Promise<Cliente[]>
presupuestosService.update(id: string, patch: Partial<Presupuesto>): Promise<void>
```

<!-- Hook contract: -->
```typescript
// apps/sistema-modular/src/hooks/useEntregas.ts
export function useEntregas(): {
  rows: EntregaRow[];
  loading: boolean;
  error: Error | null;
  reload: () => Promise<void>;
  /** Actualiza otNumeroVinculada en el item del presupuesto. */
  updateOtNumero: (presupuestoId: string, itemId: string, otNumero: string | null) => Promise<void>;
};
```

<!-- Route + sidebar paths (locked): -->
- Route path: `/entregas`
- Sidebar parent: `Stock > Compras` (after `Importaciones`)
- ProtectedRoute roles: `['admin', 'admin_soporte', 'administracion']` (mirror ImportacionesList in TabContentManager.tsx line 161)

<!-- FILTER_SCHEMA (from list-page-conventions skill + research): -->
```typescript
const FILTER_SCHEMA = {
  clienteId:  { type: 'string' as const, default: '' },
  semaforo:   { type: 'string' as const, default: '' },
  estadoImp:  { type: 'string' as const, default: '' },
  search:     { type: 'string' as const, default: '' },
  sortField:  { type: 'string' as const, default: 'diasRestantes' },
  sortDir:    { type: 'string' as const, default: 'asc' },
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hook useEntregas — orquesta Promise.all + delega al resolver puro + updateOtNumero</name>
  <files>apps/sistema-modular/src/hooks/useEntregas.ts</files>
  <behavior>
    - Carga en paralelo: presupuestos (estados ['aceptado', 'en_ejecucion', 'finalizado']), requerimientos, ordenesCompra, importaciones, clientes.
    - Construye `clienteNombreById: Map<string, string>` a partir de `clientesService.getAll()`.
    - **NO usa serviceCache** (esta vista necesita datos frescos — feedback del repo sobre staleness en planning).
    - Llama `buildEntregaRows(...)` con los datos cargados.
    - `updateOtNumero(presupuestoId, itemId, otNumero)`: lee el ppto desde Firestore, modifica el item por id, escribe con `deepCleanForFirestore({ items: newItems })`. Tras éxito → llamar `reload()`.
    - Maneja errors graceful (setError + console.error). Re-throw en `updateOtNumero` para que el caller pueda mostrar toast.
    - Sin cache local (la coordinadora puede tener mil pestañas) — `reload` es público para refresh on focus si se quiere.
    - **CRITICAL**: usar `presupuestosService.update` (que ya envuelve writes desde `'./firebase'`) — NO importar `updateDoc` directo (regla del repo, bug Electron post-write keyboard router).
  </behavior>
  <action>
Crear `apps/sistema-modular/src/hooks/useEntregas.ts`:

```typescript
import { useCallback, useEffect, useState } from 'react';
import type { Presupuesto, PresupuestoItem, RequerimientoCompra, Importacion } from '@ags/shared';
import { presupuestosService } from '../services/presupuestosService';
import {
  requerimientosService,
  ordenesCompraService,
  importacionesService,
} from '../services/importacionesService';
import { clientesService } from '../services/clientesService';
import { buildEntregaRows, type EntregaRow } from '../utils/entregasResolver';

type EstadoPresupuestoActivo = 'aceptado' | 'en_ejecucion' | 'finalizado';
const ESTADOS_ACTIVOS: EstadoPresupuestoActivo[] = ['aceptado', 'en_ejecucion', 'finalizado'];

interface UseEntregasReturn {
  rows: EntregaRow[];
  loading: boolean;
  error: Error | null;
  reload: () => Promise<void>;
  updateOtNumero: (presupuestoId: string, itemId: string, otNumero: string | null) => Promise<void>;
}

export function useEntregas(): UseEntregasReturn {
  const [rows, setRows] = useState<EntregaRow[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const load = useCallback(async () => {
    setLoading(true);
    setError(null);
    try {
      // Cargar presupuestos por estado en paralelo, luego flatten.
      const [pptosBuckets, reqs, ocs, imps, clientes] = await Promise.all([
        Promise.all(ESTADOS_ACTIVOS.map(e => presupuestosService.getAll({ estado: e }))),
        requerimientosService.getAll().catch(() => [] as RequerimientoCompra[]),
        ordenesCompraService.getAll().catch(() => [] as any[]),
        importacionesService.getAll().catch(() => [] as Importacion[]),
        clientesService.getAll().catch(() => [] as any[]),
      ]);
      const presupuestos = pptosBuckets.flat() as Presupuesto[];

      const clienteNombreById = new Map<string, string>(
        clientes.map((c: any) => [c.id as string, (c.razonSocial as string) ?? c.nombre ?? c.id]),
      );

      // Normalizar OC shape para el resolver (id + numero + items[{id, requerimientoId}])
      const ocsForResolver = ocs.map((oc: any) => ({
        id: oc.id as string,
        numero: oc.numero as string,
        items: (oc.items ?? []).map((it: any) => ({
          id: it.id as string,
          requerimientoId: (it.requerimientoId ?? null) as string | null,
        })),
      }));

      const built = buildEntregaRows({
        presupuestos,
        requerimientos: reqs as RequerimientoCompra[],
        ordenesCompra: ocsForResolver,
        importaciones: imps as Importacion[],
        clienteNombreById,
      });

      setRows(built);
    } catch (err) {
      const e = err instanceof Error ? err : new Error(String(err));
      console.error('[useEntregas] load failed', e);
      setError(e);
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => { void load(); }, [load]);

  const updateOtNumero = useCallback(async (presupuestoId: string, itemId: string, otNumero: string | null) => {
    const pres = await presupuestosService.getById(presupuestoId);
    if (!pres) throw new Error('Presupuesto no encontrado');
    const newItems = (pres.items ?? []).map((it: PresupuestoItem) =>
      it.id === itemId ? { ...it, otNumeroVinculada: otNumero ?? null } : it,
    );
    // presupuestosService.update internamente usa deepCleanForFirestore y writes desde './firebase'
    // (verificar — si no lo hace, envolver acá). Patrón del repo es pasar el patch limpio.
    await presupuestosService.update(presupuestoId, { items: newItems } as Partial<Presupuesto>);
    await load();   // refresh para que la fila refleje el cambio
  }, [load]);

  return { rows, loading, error, reload: load, updateOtNumero };
}
```

**Verificación**:
- `pnpm type-check` GREEN.
- En dev: abrir DevTools → en la consola del componente que monta el hook (ver Task 2), inspeccionar `rows.length > 0` si hay presupuestos aceptados.

NOTA: Si `presupuestosService.update` NO envuelve con `deepCleanForFirestore` (verificar grep en presupuestosService.ts), envolver acá:
```typescript
import { deepCleanForFirestore } from '../services/firebase';
await presupuestosService.update(presupuestoId, deepCleanForFirestore({ items: newItems }));
```
  </action>
  <verify>
    <automated>pnpm type-check && wc -l apps/sistema-modular/src/hooks/useEntregas.ts | awk '{ if ($1 > 150) print "Hook getting large ("$1" LOC) — consider split"; else print "OK ("$1" LOC)" }'</automated>
  </verify>
  <done>
    - Hook creado, exporta `useEntregas`.
    - `pnpm type-check` GREEN.
    - ≤ 150 LOC.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EntregasFilters + EntregaRow sub-componentes (extraidos antes de tener problemas de tamaño)</name>
  <files>apps/sistema-modular/src/pages/entregas/EntregasFilters.tsx, apps/sistema-modular/src/pages/entregas/EntregaRow.tsx</files>
  <behavior>
    - **EntregasFilters**: render del bloque de filtros (4 controles + clear). Props: `filters`, `setFilter`, `clienteOptions: Array<{value, label}>`. ≤ 120 LOC.
    - **EntregaRow**: render de una fila de la tabla. Props: `row: EntregaRow`, `onUpdateOtNumero: (otNumero: string | null) => Promise<void>`. Edición inline del input OT# con debounce (300ms) o blur/Enter handler. Renderiza badges para semáforo + disponibilidad + estado importación. ≤ 150 LOC.
    - Atoms reutilizados: `SearchableSelect`, badge classes del skill list-page-conventions.
  </behavior>
  <action>
**(a) Crear `apps/sistema-modular/src/pages/entregas/EntregasFilters.tsx`:**

```tsx
import React from 'react';
import { SearchableSelect } from '../../components/ui/SearchableSelect';
import type { Semaforo } from '../../utils/entregasResolver';
import { SEMAFORO_LABELS } from '../../utils/entregasResolver';
import type { EstadoImportacion } from '@ags/shared';
import { ESTADO_IMPORTACION_LABELS } from '@ags/shared';

interface Filters {
  clienteId: string;
  semaforo: string;
  estadoImp: string;
  search: string;
}

interface Props {
  filters: Filters;
  setFilter: <K extends keyof Filters>(k: K, v: Filters[K]) => void;
  clienteOptions: Array<{ value: string; label: string }>;
}

const SEMAFOROS: Semaforo[] = ['verde', 'amarillo', 'rojo', 'entregado', 'sin_eta'];
const ESTADOS_IMP: EstadoImportacion[] = ['preparacion', 'embarcado', 'en_transito', 'en_aduana', 'despachado', 'recibido', 'cancelado'];

export const EntregasFilters: React.FC<Props> = ({ filters, setFilter, clienteOptions }) => {
  const hasAdvanced = filters.clienteId || filters.semaforo || filters.estadoImp || filters.search;

  return (
    <div className="flex items-center gap-2 flex-wrap">
      <input
        type="text"
        value={filters.search}
        onChange={(e) => setFilter('search', e.target.value)}
        placeholder="Buscar item, presupuesto, OT, OC..."
        className="border border-slate-200 rounded-lg px-3 py-1.5 text-xs placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-teal-500 w-64"
        data-testid="entregas-search"
      />
      <div className="min-w-[180px]">
        <SearchableSelect
          value={filters.clienteId}
          onChange={(v: string) => setFilter('clienteId', v)}
          options={[{ value: '', label: 'Cliente: Todos' }, ...clienteOptions]}
          placeholder="Cliente"
        />
      </div>
      <select
        value={filters.semaforo}
        onChange={(e) => setFilter('semaforo', e.target.value)}
        className="text-xs border border-slate-300 rounded-lg px-2.5 py-1.5 bg-white text-slate-700 focus:outline-none focus:ring-2 focus:ring-teal-500"
      >
        <option value="">Semáforo: Todos</option>
        {SEMAFOROS.map(s => <option key={s} value={s}>{SEMAFORO_LABELS[s]}</option>)}
      </select>
      <select
        value={filters.estadoImp}
        onChange={(e) => setFilter('estadoImp', e.target.value)}
        className="text-xs border border-slate-300 rounded-lg px-2.5 py-1.5 bg-white text-slate-700 focus:outline-none focus:ring-2 focus:ring-teal-500"
      >
        <option value="">Estado IMP: Todos</option>
        {ESTADOS_IMP.map(e => <option key={e} value={e}>{ESTADO_IMPORTACION_LABELS[e]}</option>)}
      </select>
      {hasAdvanced && (
        <button
          type="button"
          onClick={() => { setFilter('clienteId', ''); setFilter('semaforo', ''); setFilter('estadoImp', ''); setFilter('search', ''); }}
          className="text-[11px] text-slate-500 hover:text-slate-700 px-2 py-1 rounded hover:bg-slate-100"
        >Limpiar</button>
      )}
    </div>
  );
};
```

**(b) Crear `apps/sistema-modular/src/pages/entregas/EntregaRow.tsx`:**

```tsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';
import { ESTADO_IMPORTACION_COLORS, ESTADO_IMPORTACION_LABELS, DISPONIBILIDAD_COLORS, DISPONIBILIDAD_LABELS } from '@ags/shared';
import type { EstadoImportacion } from '@ags/shared';
import type { EntregaRow as Row } from '../../utils/entregasResolver';
import { SEMAFORO_COLORS, SEMAFORO_LABELS } from '../../utils/entregasResolver';

interface Props {
  row: Row;
  onUpdateOtNumero: (otNumero: string | null) => Promise<void>;
}

const formatDate = (iso: string | null): string => {
  if (!iso) return '—';
  try { return new Date(iso).toLocaleDateString('es-AR', { day: '2-digit', month: '2-digit', year: '2-digit' }); }
  catch { return '—'; }
};

const formatMoney = (n: number, m: string | null): string => {
  const symbol = m === 'ARS' ? '$' : m === 'EUR' ? '€' : '$';
  return `${symbol} ${n.toLocaleString('es-AR', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
};

export const EntregaRowComponent: React.FC<Props> = ({ row, onUpdateOtNumero }) => {
  const [otDraft, setOtDraft] = useState(row.otNumeroVinculada ?? '');
  const [saving, setSaving] = useState(false);

  const commitOt = async () => {
    const trimmed = otDraft.trim();
    const next = trimmed === '' ? null : trimmed;
    if (next === (row.otNumeroVinculada ?? null)) return;   // no change
    setSaving(true);
    try {
      await onUpdateOtNumero(next);
    } catch (err) {
      console.error('[EntregaRow] OT save failed', err);
      setOtDraft(row.otNumeroVinculada ?? '');  // revert
    } finally {
      setSaving(false);
    }
  };

  return (
    <tr className="border-b border-slate-100 hover:bg-slate-50">
      <td className="px-3 py-2 text-xs font-semibold text-teal-700 truncate max-w-[160px]">
        {row.clienteNombre}
      </td>
      <td className="px-3 py-2 text-xs text-slate-600 truncate max-w-[220px]" title={row.descripcion}>
        {row.descripcion}
      </td>
      <td className="px-3 py-2 text-xs text-slate-600 text-right font-mono">{row.cantidad}</td>
      <td className="px-3 py-2 text-xs text-slate-600 text-right font-mono whitespace-nowrap">
        {formatMoney(row.precioUnitario, row.moneda)}
      </td>
      <td className="px-3 py-2 text-xs">
        <Link to={`/presupuestos/${row.presupuestoId}`} className="text-teal-700 hover:underline font-mono">
          {row.presupuestoNumero}
        </Link>
      </td>
      <td className="px-3 py-2 text-xs">
        <input
          type="text"
          value={otDraft}
          onChange={(e) => setOtDraft(e.target.value)}
          onBlur={commitOt}
          onKeyDown={(e) => { if (e.key === 'Enter') (e.target as HTMLInputElement).blur(); }}
          placeholder="OT#"
          disabled={saving}
          className="w-20 text-xs font-mono border border-slate-200 rounded px-1.5 py-0.5 focus:outline-none focus:ring-1 focus:ring-teal-500"
          data-testid={`ot-input-${row.itemId}`}
        />
      </td>
      <td className="px-3 py-2 text-xs text-slate-600 font-mono">{row.ocNumero ?? '—'}</td>
      <td className="px-3 py-2 text-xs">
        {row.importacionNumero ? (
          <div className="flex items-center gap-1.5">
            <span className="font-mono text-slate-600">{row.importacionNumero}</span>
            {row.importacionEstado && (
              <span className={`text-[10px] font-medium px-1.5 py-0.5 rounded-full ${ESTADO_IMPORTACION_COLORS[row.importacionEstado as EstadoImportacion] ?? ''}`}>
                {ESTADO_IMPORTACION_LABELS[row.importacionEstado as EstadoImportacion] ?? row.importacionEstado}
              </span>
            )}
          </div>
        ) : <span className="text-slate-300">—</span>}
      </td>
      <td className="px-3 py-2 text-xs">
        {row.disponibilidad ? (
          <span className={`text-[10px] font-medium px-1.5 py-0.5 rounded-full ${DISPONIBILIDAD_COLORS[row.disponibilidad]}`}>
            {DISPONIBILIDAD_LABELS[row.disponibilidad]}
          </span>
        ) : <span className="text-slate-300 text-[10px]">—</span>}
      </td>
      <td className="px-3 py-2 text-[10px] text-slate-500 whitespace-nowrap font-mono">
        {formatDate(row.etaFecha)}
      </td>
      <td className="px-3 py-2 text-xs whitespace-nowrap">
        <span className={`font-mono ${SEMAFORO_COLORS[row.semaforo]}`}>
          {row.semaforo === 'sin_eta' ? 'Sin ETA' : (row.diasRestantes != null ? `${row.diasRestantes}d` : '—')}
        </span>
        <span className="ml-1.5 text-[10px] text-slate-400">{SEMAFORO_LABELS[row.semaforo]}</span>
      </td>
    </tr>
  );
};
```

NOTE: SearchableSelect signature can vary — verify against the existing one (`apps/sistema-modular/src/components/ui/SearchableSelect.tsx`) and adjust prop names if needed.
  </action>
  <verify>
    <automated>pnpm type-check && wc -l apps/sistema-modular/src/pages/entregas/EntregasFilters.tsx apps/sistema-modular/src/pages/entregas/EntregaRow.tsx | awk 'NR<=2 && $1>200 { print "OVER BUDGET: "$2" ("$1" LOC)"; exit 1 } END { print "OK" }'</automated>
  </verify>
  <done>
    - 2 sub-componentes creados.
    - Cada uno ≤ 200 LOC.
    - `pnpm type-check` GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: EntregasList + sidebar + route + barrel index</name>
  <files>apps/sistema-modular/src/pages/entregas/EntregasList.tsx, apps/sistema-modular/src/pages/entregas/index.tsx, apps/sistema-modular/src/components/layout/navigation.ts, apps/sistema-modular/src/components/layout/TabContentManager.tsx</files>
  <behavior>
    - **EntregasList.tsx**: orchestración — usa `useEntregas()`, `useUrlFilters(FILTER_SCHEMA)`, `useResizableColumns('entregas-list')`, `SortableHeader`, `sortByField`. Renderiza `PageHeader` + `EntregasFilters` + tabla con `EntregaRow`. Aplica filtros + sort in-memory. ≤ 250 LOC.
    - **index.tsx**: barrel `export { EntregasList } from './EntregasList';`.
    - **navigation.ts**: agregar `{ name: 'Entregas', path: '/entregas' }` al array `Compras.children` (después de `'Importaciones'`).
    - **TabContentManager.tsx**: agregar import + route con `ProtectedRoute allowedRoles={['admin', 'admin_soporte', 'administracion']}`.
  </behavior>
  <action>
**(a) Crear `apps/sistema-modular/src/pages/entregas/EntregasList.tsx`:**

```tsx
import React, { useMemo } from 'react';
import { useEntregas } from '../../hooks/useEntregas';
import { useUrlFilters } from '../../hooks/useUrlFilters';
import { useResizableColumns } from '../../hooks/useResizableColumns';
import { SortableHeader, sortByField, toggleSort, type SortDir } from '../../components/ui/SortableHeader';
import { PageHeader } from '../../components/ui/PageHeader';
import { EntregasFilters } from './EntregasFilters';
import { EntregaRowComponent } from './EntregaRow';

const FILTER_SCHEMA = {
  clienteId:  { type: 'string' as const, default: '' },
  semaforo:   { type: 'string' as const, default: '' },
  estadoImp:  { type: 'string' as const, default: '' },
  search:     { type: 'string' as const, default: '' },
  sortField:  { type: 'string' as const, default: 'diasRestantes' },
  sortDir:    { type: 'string' as const, default: 'asc' },
};

const thClass = 'text-left text-[11px] font-medium text-slate-400 tracking-wider py-2 px-3';

export const EntregasList: React.FC = () => {
  const { rows, loading, updateOtNumero } = useEntregas();
  const [filters, setFilter] = useUrlFilters(FILTER_SCHEMA);
  const { tableRef } = useResizableColumns('entregas-list');

  const clienteOptions = useMemo(() => {
    const seen = new Map<string, string>();
    rows.forEach(r => { if (r.clienteId && !seen.has(r.clienteId)) seen.set(r.clienteId, r.clienteNombre); });
    return Array.from(seen.entries()).map(([value, label]) => ({ value, label })).sort((a, b) => a.label.localeCompare(b.label));
  }, [rows]);

  const filtered = useMemo(() => {
    const term = filters.search.trim().toLowerCase();
    return rows.filter(r => {
      if (filters.clienteId && r.clienteId !== filters.clienteId) return false;
      if (filters.semaforo && r.semaforo !== filters.semaforo) return false;
      if (filters.estadoImp && r.importacionEstado !== filters.estadoImp) return false;
      if (term) {
        const hay = `${r.descripcion} ${r.presupuestoNumero} ${r.otNumeroVinculada ?? ''} ${r.ocNumero ?? ''} ${r.importacionNumero ?? ''} ${r.clienteNombre}`.toLowerCase();
        if (!hay.includes(term)) return false;
      }
      return true;
    });
  }, [rows, filters.clienteId, filters.semaforo, filters.estadoImp, filters.search]);

  const sorted = useMemo(
    () => sortByField(filtered as any[], filters.sortField, filters.sortDir as SortDir),
    [filtered, filters.sortField, filters.sortDir],
  );

  const handleSort = (f: string) => {
    const s = toggleSort(f, filters.sortField, filters.sortDir as SortDir);
    setFilter('sortField', s.field);
    setFilter('sortDir', s.dir);
  };

  return (
    <div className="h-full flex flex-col bg-slate-50">
      <PageHeader
        title="Entregas"
        subtitle="Visor de cumplimiento de entregas comprometidas"
        count={sorted.length}
      >
        <EntregasFilters filters={filters} setFilter={setFilter} clienteOptions={clienteOptions} />
      </PageHeader>

      <div className="flex-1 min-h-0 px-5 pb-4">
        <div className="bg-white rounded-xl border border-slate-200 shadow-sm overflow-y-auto h-full">
          {loading ? (
            <div className="text-center py-12 text-xs text-slate-400">Cargando...</div>
          ) : sorted.length === 0 ? (
            <div className="text-center py-12 text-xs text-slate-400">No hay items para mostrar</div>
          ) : (
            <table ref={tableRef} className="w-full">
              <thead className="sticky top-0 z-10 bg-slate-50">
                <tr className="border-b border-slate-200">
                  <SortableHeader field="clienteNombre"        currentField={filters.sortField} dir={filters.sortDir as SortDir} onSort={handleSort} className={thClass}>Cliente</SortableHeader>
                  <SortableHeader field="descripcion"          currentField={filters.sortField} dir={filters.sortDir as SortDir} onSort={handleSort} className={thClass}>Item</SortableHeader>
                  <SortableHeader field="cantidad"             currentField={filters.sortField} dir={filters.sortDir as SortDir} onSort={handleSort} className={`${thClass} text-right`}>Cantidad</SortableHeader>
                  <SortableHeader field="precioUnitario"       currentField={filters.sortField} dir={filters.sortDir as SortDir} onSort={handleSort} className={`${thClass} text-right`}>Valor unit.</SortableHeader>
                  <SortableHeader field="presupuestoNumero"    currentField={filters.sortField} dir={filters.sortDir as SortDir} onSort={handleSort} className={thClass}>Presupuesto</SortableHeader>
                  <th className={thClass}>OT#</th>
                  <SortableHeader field="ocNumero"             currentField={filters.sortField} dir={filters.sortDir as SortDir} onSort={handleSort} className={thClass}>OC#</SortableHeader>
                  <SortableHeader field="importacionNumero"    currentField={filters.sortField} dir={filters.sortDir as SortDir} onSort={handleSort} className={thClass}>Importación</SortableHeader>
                  <SortableHeader field="disponibilidad"       currentField={filters.sortField} dir={filters.sortDir as SortDir} onSort={handleSort} className={thClass}>Disp.</SortableHeader>
                  <SortableHeader field="etaFecha"             currentField={filters.sortField} dir={filters.sortDir as SortDir} onSort={handleSort} className={thClass}>ETA</SortableHeader>
                  <SortableHeader field="diasRestantes"        currentField={filters.sortField} dir={filters.sortDir as SortDir} onSort={handleSort} className={thClass}>Días</SortableHeader>
                </tr>
              </thead>
              <tbody>
                {sorted.map((row: any) => (
                  <EntregaRowComponent
                    key={`${row.presupuestoId}::${row.itemId}`}
                    row={row}
                    onUpdateOtNumero={(otNumero) => updateOtNumero(row.presupuestoId, row.itemId, otNumero)}
                  />
                ))}
              </tbody>
            </table>
          )}
        </div>
      </div>
    </div>
  );
};
```

NOTE: SortableHeader API may use different prop names (e.g., `currentSort`, `currentDir`). Verify against `apps/sistema-modular/src/components/ui/SortableHeader.tsx` and adapt. If `useResizableColumns` returns more shape (colWidths, etc.), thread the props through `<th>` like ImportacionesList does.

**(b) Crear `apps/sistema-modular/src/pages/entregas/index.tsx`:**

```typescript
export { EntregasList } from './EntregasList';
```

**(c) Editar `apps/sistema-modular/src/components/layout/navigation.ts`** — agregar UNA línea en `Compras.children` (alrededor de línea 64-69, después de `Importaciones`):

```typescript
      {
        name: 'Compras', path: '#stock-compras', icon: '🛒',
        children: [
          { name: 'Requerimientos', path: '/stock/requerimientos' },
          { name: 'Planificación', path: '/stock/planificacion' },
          { name: 'Ordenes de Compra', path: '/stock/ordenes-compra' },
          { name: 'Importaciones', path: '/stock/importaciones' },
          { name: 'Entregas', path: '/entregas' },     // <-- ADD THIS LINE
        ],
      },
```

**(d) Editar `apps/sistema-modular/src/components/layout/TabContentManager.tsx`:**

1. Agregar al import (línea 20 ya tiene un import largo de `'../../pages/stock'`; agregar import separado para entregas):
   ```typescript
   import { EntregasList } from '../../pages/entregas';
   ```

2. Agregar ruta cerca de la registración de `/stock/importaciones` (línea 161 pattern; mismo patrón de roles):
   ```tsx
   <Route path="/entregas" element={<ProtectedRoute allowedRoles={['admin', 'admin_soporte', 'administracion']}><EntregasList /></ProtectedRoute>} />
   ```

**Smoke en dev**:
1. `pnpm dev:modular`.
2. Login como admin.
3. Sidebar: `Stock → Compras → Entregas` debe estar visible.
4. Click → abrir `/entregas` → ver la planilla con presupuestos aceptados.
5. Aplicar filtros → URL debe cambiar (`?clienteId=...&semaforo=verde`).
6. Editar OT# en una fila → blur → verificar (a) loading state, (b) reload tras éxito, (c) F5 → valor persiste.
7. Sort por días restantes (click en header).
  </action>
  <verify>
    <automated>pnpm type-check && pnpm --filter @ags/sistema-modular test:entregas && wc -l apps/sistema-modular/src/pages/entregas/EntregasList.tsx | awk '{ if ($1 > 250) { print "OVER BUDGET ("$1" LOC) — extract more sub-components"; exit 1 } else print "OK ("$1" LOC)"; }'</automated>
  </verify>
  <done>
    - 7 archivos creados/editados.
    - Ruta `/entregas` visible en sidebar bajo Stock > Compras.
    - `pnpm type-check` GREEN; `test:entregas` sigue GREEN.
    - EntregasList ≤ 250 LOC.
    - Smoke en dev confirma: filtros en URL, sort por días restantes, edición inline de OT# persistente tras F5.
  </done>
</task>

</tasks>

<verification>
1. Type-check + Wave 0 suite:
   ```bash
   pnpm type-check && pnpm --filter @ags/sistema-modular test:entregas
   ```
2. Smoke manual en dev:
   - Login → sidebar Stock > Compras > Entregas visible.
   - Apertura de `/entregas` muestra filas de presupuestos aceptados.
   - Filtros: cliente, semáforo, estado imp, search libre → URL refleja state.
   - Sort: click headers cambia orden.
   - Edición OT#: input → blur → reload → valor persiste en Firestore (verificable en DevTools).
3. Componente size budget:
   ```bash
   wc -l apps/sistema-modular/src/pages/entregas/*.tsx apps/sistema-modular/src/hooks/useEntregas.ts
   ```
   Todos ≤ 250.
</verification>

<success_criteria>
- [ ] 5 archivos nuevos: `pages/entregas/{EntregasList,EntregasFilters,EntregaRow,index}.tsx` + `hooks/useEntregas.ts`.
- [ ] 2 archivos editados: `layout/navigation.ts` (entrada sidebar) + `layout/TabContentManager.tsx` (route mount).
- [ ] `pnpm type-check` GREEN.
- [ ] `pnpm --filter @ags/sistema-modular test:entregas` GREEN (sin regresión Wave 0).
- [ ] Smoke en dev pasa (todos los puntos de Verification #2).
- [ ] Componentes ≤ 250 LOC.
- [ ] Filtros usan `useUrlFilters` (verificable con grep: cero matches de `useState` para filters en `EntregasList.tsx`).
</success_criteria>

<output>
After completion, create `.planning/phases/16-entregas-visor-de-cumplimiento/16-05-SUMMARY.md` con:
- Lista de archivos + LOC de cada uno.
- URL ejemplo del sidebar y de la ruta con filtros aplicados.
- Confirmación de edición inline funcional (smoke trace).
- Próximo paso: 16-06 UAT manual + release note.
</output>
