---
phase: 16-entregas-visor-de-cumplimiento
plan: 02
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - apps/sistema-modular/src/services/presupuestosService.ts
autonomous: true
requirements: [ENT-03]
must_haves:
  truths:
    - "Al aceptar un presupuesto, se persiste `Presupuesto.fechaAceptacion: <ISO>` dentro de la runTransaction"
    - "Al aceptar un presupuesto con items de importación, cada `RequerimientoCompra` creado lleva `presupuestoItemId: <id del item>` en su payload"
    - "Presupuestos ya aceptados pre-Phase16 NO se re-procesan (idempotencia respetada — early return cuando `estado === 'aceptado'`)"
    - "El audit chain existente (updatedBy/updatedByName) sigue funcionando — NO se rompe Phase 8 FLOW-03"
  artifacts:
    - path: "apps/sistema-modular/src/services/presupuestosService.ts"
      provides: "aceptarConRequerimientos() escribe fechaAceptacion + presupuestoItemId"
      contains: "fechaAceptacion"
  key_links:
    - from: "apps/sistema-modular/src/services/presupuestosService.ts"
      to: "packages/shared/src/types/index.ts"
      via: "import type RequerimientoCompra (con presupuestoItemId opcional)"
      pattern: "presupuestoItemId"
    - from: "apps/sistema-modular/src/services/presupuestosService.ts"
      to: "Firestore presupuestos doc"
      via: "tx.update con fechaAceptacion en el patch"
      pattern: "fechaAceptacion:"
---

<objective>
Patch backend behavior de `presupuestosService.aceptarConRequerimientos()`: agregar **dos writes** dentro del `runTransaction` ya existente — (1) `fechaAceptacion: Timestamp.now()` en el update del presupuesto, (2) `presupuestoItemId: item.id` en el payload de cada `RequerimientoCompra` creado.

Purpose: Sin estos dos campos, el resolver de la planilla `/entregas` (Plan 16-03 + 16-05) no puede (a) computar ETA, (b) join items↔reqs de forma robusta. Es la base del modelo de datos para esta phase.

Output: `aceptarConRequerimientos()` con los dos writes adicionales. Smoke manual: aceptar un presupuesto en dev y verificar en Firestore que `fechaAceptacion` y `presupuestoItemId` quedan persistidos.

Constraint: ZERO cambios funcionales además de estos dos writes. NO tocar las ramas de syncFromPresupuesto, spawn-T_n, _appendPendingAction, auto-ticket coordinación. La idempotencia (`if pp.estado === 'aceptado' return;`) debe seguir intacta.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-CONTEXT.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-RESEARCH.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/firestore.md

# Source file to edit
@apps/sistema-modular/src/services/presupuestosService.ts

<interfaces>
<!-- Function signature (DOES NOT CHANGE) -->
```typescript
async aceptarConRequerimientos(
  presupuestoId: string,
  actor?: { uid: string; name?: string },
): Promise<{ requerimientosIds: string[] }>;
```

<!-- Existing runTransaction in presupuestosService.ts lines 960-1015 (verified): -->
```typescript
await runTransaction(db, async (tx) => {
  const presRef = doc(db, 'presupuestos', presupuestoId);
  const presSnap = await tx.get(presRef);
  if (!presSnap.exists()) throw new Error('Presupuesto no encontrado');
  const pp = presSnap.data() as Presupuesto;
  if (pp.estado === 'aceptado') return;   // idempotency — KEEP

  itemsImport.forEach((item: any, idx: number) => {
    const reqRef = doc(collection(db, 'requerimientos_compra'));
    newReqIds.push(reqRef.id);
    const articulo = articulosData.get(item.stockArticuloId) || null;
    const payload = deepCleanForFirestore({
      // ... existing fields ...
      presupuestoId,
      presupuestoNumero: pp.numero,
      // ADD HERE: presupuestoItemId: item.id ?? null,
      proveedorSugeridoId: articulo?.proveedorIds?.[0] ?? null,
      // ... rest unchanged ...
    });
    tx.set(reqRef, payload);
  });

  tx.update(presRef, deepCleanForFirestore({
    estado: 'aceptado',
    // ADD HERE: fechaAceptacion: nowIso,
    updatedAt: Timestamp.now(),
    updatedBy: actor?.uid ?? null,
    updatedByName: actor?.name ?? null,
  }));
});
```

<!-- Imports already in file (verify; do NOT re-add): -->
```typescript
import { db, deepCleanForFirestore, /* ... */ } from './firebase';
import { runTransaction, doc, collection, Timestamp /* ... */ } from 'firebase/firestore';
import type { Presupuesto, PresupuestoItem, RequerimientoCompra } from '@ags/shared';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Patch aceptarConRequerimientos — escribir fechaAceptacion + presupuestoItemId</name>
  <files>apps/sistema-modular/src/services/presupuestosService.ts</files>
  <behavior>
    - Dentro de la `runTransaction` ya existente en lines ~960-1015 (NO crear una segunda transacción):
      - En el `payload` que se pasa a `tx.set(reqRef, ...)` (around line 977-1004), agregar `presupuestoItemId: item.id ?? null` justo después de `presupuestoNumero: pp.numero,`.
      - En el patch que se pasa a `tx.update(presRef, ...)` (around line 1009-1014), agregar `fechaAceptacion: nowIso` (capturado UNA vez fuera del tx callback como `const nowIso = new Date().toISOString();`) justo después de `estado: 'aceptado',`.
    - `deepCleanForFirestore` ya envuelve ambos payloads → null values son safe (no se stripean valores escalares non-undefined; sólo strip undefined). Mantener el wrapper.
    - NO modificar el bloque de pre-reserva de números REQ (lines 938-950).
    - NO modificar los post-commit side-effects (paso 5 — leadsService.syncFromPresupuesto, spawn-T_n, auto-ticket coordinación) — están fuera del tx por diseño.
    - NO cambiar la firma pública del método.
  </behavior>
  <action>
**Localizar** en `apps/sistema-modular/src/services/presupuestosService.ts`:

1. La función `aceptarConRequerimientos` (línea ~921).
2. Dentro del bloque `await runTransaction(db, async (tx) => { ... })` (líneas ~963-1015):

**Cambios mínimos quirúrgicos:**

**(A) Antes del `await runTransaction(...)` (línea ~961, después de `const newReqIds: string[] = [];`)** — capturar el ISO de aceptación UNA vez (consistente entre presupuesto y reqs):

```typescript
    // (Phase 16) Capturar fechaAceptacion ISO antes del tx para que quede consistente
    // entre el update del presupuesto y los reqs que se crean en la misma tx.
    const nowIso = new Date().toISOString();
```

**(B) En el payload del requerimiento (línea ~989, justo después de `presupuestoNumero: pp.numero,`)** — agregar:

```typescript
          presupuestoNumero: pp.numero,
          presupuestoItemId: item.id ?? null,    // (Phase 16) join key para visor de entregas
```

**(C) En el patch del update del presupuesto (línea ~1010, justo después de `estado: 'aceptado',`)** — agregar:

```typescript
      tx.update(presRef, deepCleanForFirestore({
        estado: 'aceptado',
        fechaAceptacion: nowIso,                  // (Phase 16) base para computar ETA por item
        updatedAt: Timestamp.now(),
        updatedBy: actor?.uid ?? null,
        updatedByName: actor?.name ?? null,
      }));
```

**No tocar nada más.** No re-numerar comentarios, no extraer helpers, no mover imports. Cirugía estrictamente local. El handle Electron/keyboard-router del repo writes desde `./firebase` ya está respetado (este archivo importa `db, deepCleanForFirestore` desde `./firebase` — verificar con grep).

**Smoke local en dev (ANTES del commit):**
```bash
pnpm --filter @ags/sistema-modular dev
```
1. Crear un presupuesto `partes` con un item de stock cuyo articulo tenga `itemRequiereImportacion: true`.
2. Aceptarlo (botón "Aceptar" en `EditPresupuestoModal`).
3. En la consola del navegador, leer el doc:
   ```js
   const { db } = await import('/src/services/firebase.ts');
   const { doc, getDoc } = await import('firebase/firestore');
   const snap = await getDoc(doc(db, 'presupuestos', '<id>'));
   console.log(snap.data().fechaAceptacion);   // ISO string non-null
   ```
4. Buscar el `RequerimientoCompra` creado y confirmar `presupuestoItemId === <item.id>`.

NO ejecutar en prod — solo dev. El smoke es para validar antes del PR/commit.
  </action>
  <verify>
    <automated>pnpm type-check && grep -E "fechaAceptacion: nowIso|presupuestoItemId: item\.id" apps/sistema-modular/src/services/presupuestosService.ts | wc -l | grep -E "^2$"</automated>
  </verify>
  <done>
    - `pnpm type-check` GREEN (la nueva referencia a `nowIso` y `item.id` compila).
    - Grep encuentra exactamente 2 líneas nuevas: `fechaAceptacion: nowIso,` y `presupuestoItemId: item.id ?? null,`.
    - `presupuestosService.aceptarConRequerimientos` cuando se ejecute, escribe los dos campos en Firestore (verificable en dev con un presupuesto de prueba).
    - Idempotencia conservada (`if (pp.estado === 'aceptado') return;` intacto).
    - Audit chain conservado (`updatedAt/updatedBy/updatedByName` siguen presentes).
  </done>
</task>

</tasks>

<verification>
1. Type-check:
   ```bash
   pnpm type-check
   ```
2. Grep evidence:
   ```bash
   grep -n "fechaAceptacion: nowIso\|presupuestoItemId: item\.id" apps/sistema-modular/src/services/presupuestosService.ts
   ```
   Esperado: 2 matches.
3. Smoke manual en dev (ver Task 1 action).
4. Idempotencia: aceptar dos veces el mismo presupuesto debe seguir siendo no-op en el segundo intento.
</verification>

<success_criteria>
- [ ] Sólo 1 archivo modificado: `apps/sistema-modular/src/services/presupuestosService.ts`.
- [ ] Diff total ≤ ~6 líneas agregadas (declaración `nowIso` + 2 líneas dentro del payload del req + 1 línea dentro del patch del presupuesto + comentarios cortos `// (Phase 16)`).
- [ ] `pnpm type-check` GREEN.
- [ ] Smoke en dev confirma que `Presupuesto.fechaAceptacion` y `RequerimientoCompra.presupuestoItemId` se persisten correctamente.
- [ ] La idempotencia y el audit chain previos siguen intactos.
</success_criteria>

<output>
After completion, create `.planning/phases/16-entregas-visor-de-cumplimiento/16-02-SUMMARY.md` con:
- Diff resumido (3 cambios atómicos dentro del runTransaction existente).
- Comando del smoke en dev (copy-pasteable).
- Confirmación de idempotencia conservada.
- Próximo paso: 16-03 (pure-function impls — turns Wave 0 tests GREEN end-to-end al combinarse con este plan).
</output>
