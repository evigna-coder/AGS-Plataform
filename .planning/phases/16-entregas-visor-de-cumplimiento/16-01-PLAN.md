---
phase: 16-entregas-visor-de-cumplimiento
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/shared/src/types/index.ts
  - apps/sistema-modular/src/utils/entregasResolver.ts
  - apps/sistema-modular/src/__tests__/entregasResolver.test.ts
  - apps/sistema-modular/src/__tests__/fixtures/entregas.ts
  - apps/sistema-modular/scripts/test-entregas.ts
  - apps/sistema-modular/package.json
autonomous: true
requirements: [ENT-W0]
must_haves:
  truths:
    - "El comando `pnpm --filter @ags/sistema-modular test:entregas` corre Node test runner contra el archivo de tests"
    - "El archivo de tests carga las 3 pure functions desde entregasResolver.ts (stubs presentes que tiran NotImplemented)"
    - "@ags/shared exporta PresupuestoItem.disponibilidad, PresupuestoItem.etaDiasEstimados, PresupuestoItem.otNumeroVinculada, Presupuesto.fechaAceptacion, RequerimientoCompra.presupuestoItemId — todos opcionales (backwards-compat)"
    - "Existen fixtures de ppto aceptado + req + OC + importacion para los 6 escenarios ENT-01..ENT-06"
  artifacts:
    - path: "packages/shared/src/types/index.ts"
      provides: "Type extensions for PresupuestoItem (disponibilidad, etaDiasEstimados, otNumeroVinculada), Presupuesto (fechaAceptacion), RequerimientoCompra (presupuestoItemId)"
      contains: "disponibilidad?"
    - path: "apps/sistema-modular/src/utils/entregasResolver.ts"
      provides: "Pure-function stubs (computeSemaforo, computeEtaFecha, buildEntregaRows) — throw NotImplemented until Plan 16-03"
      exports: ["computeSemaforo", "computeEtaFecha", "buildEntregaRows", "EntregaRow", "Disponibilidad", "Semaforo"]
    - path: "apps/sistema-modular/src/__tests__/entregasResolver.test.ts"
      provides: "6 unit tests covering ENT-01..ENT-06 (RED — fail until 16-02 + 16-03 land)"
      contains: "ENT-01"
    - path: "apps/sistema-modular/src/__tests__/fixtures/entregas.ts"
      provides: "Test fixtures (presupuesto/requerimiento/ordenCompra/importacion samples)"
    - path: "apps/sistema-modular/scripts/test-entregas.ts"
      provides: "Entry point que importa el test file (mismo patrón que test-patron-bom.ts)"
    - path: "apps/sistema-modular/package.json"
      provides: "Script test:entregas wired"
      contains: "test:entregas"
  key_links:
    - from: "apps/sistema-modular/package.json"
      to: "apps/sistema-modular/scripts/test-entregas.ts"
      via: "scripts.test:entregas → tsx scripts/test-entregas.ts"
      pattern: "tsx scripts/test-entregas.ts"
    - from: "apps/sistema-modular/scripts/test-entregas.ts"
      to: "apps/sistema-modular/src/__tests__/entregasResolver.test.ts"
      via: "side-effect import"
      pattern: "import.*entregasResolver.test"
    - from: "apps/sistema-modular/src/__tests__/entregasResolver.test.ts"
      to: "apps/sistema-modular/src/utils/entregasResolver.ts"
      via: "named imports (computeSemaforo, computeEtaFecha, buildEntregaRows)"
      pattern: "from.*entregasResolver"
    - from: "apps/sistema-modular/src/utils/entregasResolver.ts"
      to: "@ags/shared"
      via: "type imports for PresupuestoItem.disponibilidad/etaDiasEstimados, Presupuesto.fechaAceptacion, RequerimientoCompra.presupuestoItemId"
      pattern: "from '@ags/shared'"
---

<objective>
Establish Wave 0 baseline for Phase 16: extend shared types (PresupuestoItem, Presupuesto, RequerimientoCompra) with the new opcionales, create pure-function STUBS in `apps/sistema-modular/src/utils/entregasResolver.ts` (throwing NotImplemented), build the test fixture set covering ENT-01..ENT-06, and wire `pnpm --filter @ags/sistema-modular test:entregas`.

Purpose: Lock the contract. Tests RED at end-of-plan (correct GREEN signal). Plan 16-02 (backend) + Plan 16-03 (resolver) turn them green. Plan 16-04+ consume the same types.

Output: Type extensions in `@ags/shared` compiling project-wide; failing test suite that downstream plans will turn green; `test:entregas` script registered.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-CONTEXT.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-RESEARCH.md
@.planning/phases/16-entregas-visor-de-cumplimiento/16-VALIDATION.md
@CLAUDE.md
@.claude/rules/firestore.md
@.claude/rules/components.md
@.claude/skills/list-page-conventions/SKILL.md

# Reference implementations (Wave 0 pattern from Phases 14 + 15)
@apps/sistema-modular/scripts/test-patron-bom.ts
@apps/sistema-modular/scripts/test-venta-loaner.ts
@apps/sistema-modular/src/__tests__/patronBom.test.ts
@apps/sistema-modular/src/services/__tests__/ventaLoaner.test.ts

# Files touched (read before editing)
@packages/shared/src/types/index.ts
@apps/sistema-modular/package.json

<interfaces>
<!-- Current shapes that this plan EXTENDS (do not break) -->

From packages/shared/src/types/index.ts (lines 898-948 verified in research):
```typescript
export interface PresupuestoItem {
  id: string;
  codigoProducto?: string | null;
  descripcion: string;
  cantidad: number;
  unidad: string;
  precioUnitario: number;
  descuento?: number;
  categoriaPresupuestoId?: string;
  subtotal: number;
  stockArticuloId?: string | null;
  // ... + sistemaId/moduloId/grupo/subItem/moneda/sectorNombre/itemNotasAdicionales/esSinCargo
  itemRequiereImportacion?: boolean;   // last existing field
  // ADD THESE (this plan):
  disponibilidad?: Disponibilidad | null;       // NEW
  etaDiasEstimados?: number | null;             // NEW
  otNumeroVinculada?: string | null;            // NEW (manual ref, no FK)
}

From packages/shared/src/types/index.ts (lines 1284+):
```typescript
export interface Presupuesto {
  id: string;
  numero: string;
  // ... existing fields
  preEmbarque?: boolean;
  // ADD THIS (this plan):
  fechaAceptacion?: string | null;   // ISO; written by aceptarConRequerimientos (16-02)
}
```

From packages/shared/src/types/index.ts (lines 3314-3357):
```typescript
export interface RequerimientoCompra {
  id: string;
  numero: string;
  // ... existing fields
  presupuestoId?: string | null;
  presupuestoNumero?: string | null;
  // ADD THIS (this plan):
  presupuestoItemId?: string | null;   // NEW — opcional; written by aceptarConRequerimientos (16-02)
  condicional?: boolean;
  // ...
}
```

New union/enum types to ADD in @ags/shared (near PresupuestoItem definition, before line 898):
```typescript
export type Disponibilidad = 'stock' | 'post_facturacion' | 'a_importar' | 'en_transito';

export const DISPONIBILIDAD_LABELS: Record<Disponibilidad, string> = {
  stock:            'Stock',
  post_facturacion: 'Post-facturación',
  a_importar:       'A importar',
  en_transito:      'En tránsito',
};

export const DISPONIBILIDAD_COLORS: Record<Disponibilidad, string> = {
  stock:            'bg-emerald-100 text-emerald-700',
  post_facturacion: 'bg-slate-100 text-slate-700',
  a_importar:       'bg-amber-100 text-amber-700',
  en_transito:      'bg-blue-100 text-blue-700',
};
```

The resolver exports (stubs in this plan; impls in 16-03):
```typescript
export type Semaforo = 'verde' | 'amarillo' | 'rojo' | 'entregado' | 'sin_eta';

export interface EntregaRow {
  presupuestoId: string;
  presupuestoNumero: string;
  itemId: string;
  clienteId: string;
  clienteNombre: string;
  descripcion: string;
  cantidad: number;
  precioUnitario: number;
  moneda: 'USD' | 'ARS' | 'EUR' | null;
  disponibilidad: Disponibilidad | null;
  etaDiasEstimados: number | null;
  fechaAceptacion: string | null;     // from Presupuesto
  etaFecha: string | null;            // computed
  diasRestantes: number | null;       // computed
  semaforo: Semaforo;                 // computed
  otNumeroVinculada: string | null;
  requerimientoId: string | null;
  requerimientoNumero: string | null;
  ocNumero: string | null;
  importacionId: string | null;
  importacionNumero: string | null;
  importacionEstado: string | null;   // EstadoImportacion as string
}

export function computeSemaforo(diasRestantes: number | null, opts?: { entregado?: boolean }): Semaforo;
export function computeEtaFecha(fechaAceptacionIso: string | null, etaDiasEstimados: number | null): string | null;
export function buildEntregaRows(input: {
  presupuestos: Array<Pick<Presupuesto, 'id' | 'numero' | 'clienteId' | 'estado' | 'items' | 'fechaAceptacion'>>;
  requerimientos: RequerimientoCompra[];
  ordenesCompra: Array<{ id: string; numero: string; items: { id: string; requerimientoId?: string | null }[] }>;
  importaciones: Array<Pick<Importacion, 'id' | 'numero' | 'estado' | 'items'>>;
  clienteNombreById: Map<string, string>;
  now?: Date;
}): EntregaRow[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend @ags/shared types (PresupuestoItem, Presupuesto, RequerimientoCompra) + Disponibilidad union</name>
  <files>packages/shared/src/types/index.ts</files>
  <behavior>
    - All new fields are OPCIONALES (`?:`) — presupuestos / reqs legacy siguen compilando.
    - `Disponibilidad` exported as a union type with 4 literals.
    - `DISPONIBILIDAD_LABELS` + `DISPONIBILIDAD_COLORS` exported (Editorial Teal classes).
    - `PresupuestoItem` gains `disponibilidad?: Disponibilidad | null`, `etaDiasEstimados?: number | null`, `otNumeroVinculada?: string | null`.
    - `Presupuesto` gains `fechaAceptacion?: string | null` (ISO).
    - `RequerimientoCompra` gains `presupuestoItemId?: string | null`.
    - `pnpm type-check` passes from repo root (no break in sistema-modular / reportes-ot / portal-ingeniero consumers).
  </behavior>
  <action>
Edit `packages/shared/src/types/index.ts`:

1. **Insert near the top of the Presupuestos section (just before `// --- Item de Presupuesto ---` around line 897)**, the new union + label maps:
   ```typescript
   // --- Disponibilidad de Items (Phase 16 — Entregas) ---
   export type Disponibilidad = 'stock' | 'post_facturacion' | 'a_importar' | 'en_transito';

   export const DISPONIBILIDAD_LABELS: Record<Disponibilidad, string> = {
     stock:            'Stock',
     post_facturacion: 'Post-facturación',
     a_importar:       'A importar',
     en_transito:      'En tránsito',
   };

   export const DISPONIBILIDAD_COLORS: Record<Disponibilidad, string> = {
     stock:            'bg-emerald-100 text-emerald-700',
     post_facturacion: 'bg-slate-100 text-slate-700',
     a_importar:       'bg-amber-100 text-amber-700',
     en_transito:      'bg-blue-100 text-blue-700',
   };
   ```

2. **Extend `PresupuestoItem` (around line 948, right after `itemRequiereImportacion?: boolean;`)** with:
   ```typescript
     /**
      * (Phase 16) Origen de la entrega prometida. Default al agregar item:
      * `'stock'` si ATP > 0, `'a_importar'` si ATP = 0. Para items de contrato
      * (servicios sin stockArticuloId), default `'post_facturacion'`.
      * Editable manualmente. Cambios quedan en auditUpdate del presupuesto.
      */
     disponibilidad?: Disponibilidad | null;
     /**
      * (Phase 16) Días estimados desde `Presupuesto.fechaAceptacion` para entregar este item.
      * ETA = fechaAceptacion + etaDiasEstimados (computado en cliente, no persistido).
      * null = "Sin ETA" (presupuestos legacy o items sin compromiso).
      */
     etaDiasEstimados?: number | null;
     /**
      * (Phase 16) Número de OT manual asociada a este item (texto libre, NO FK validada).
      * Editable inline desde la vista /entregas. NO genera ni modifica una OT.
      */
     otNumeroVinculada?: string | null;
   ```

3. **Extend `Presupuesto` (around line 1359, right after `preEmbarque?: boolean;`)** with:
   ```typescript
     /**
      * (Phase 16) Timestamp ISO en que el presupuesto pasó a `aceptado`.
      * Escrito dentro del runTransaction de `aceptarConRequerimientos`.
      * Distinto de `updatedAt` (que se pisa en cada update). Base para computar ETA.
      * Legacy: presupuestos aceptados pre-Phase16 quedan en null → vista entregas muestra "Sin ETA".
      */
     fechaAceptacion?: string | null;
   ```

4. **Extend `RequerimientoCompra` (around line 3357, right after `codigoComponente?: string | null;`)** with:
   ```typescript
     /**
      * (Phase 16) FK opcional al `PresupuestoItem.id` que originó este requerimiento.
      * Permite join O(1) en el resolver del visor de entregas.
      * null cuando origen != 'presupuesto' o cuando se creó pre-Phase16.
      */
     presupuestoItemId?: string | null;
   ```

CRITICAL: NO tocar consumidores en esta plan; el plan que escribe `presupuestoItemId` + `fechaAceptacion` es 16-02. Solo los TIPOS aquí.

Verificación local antes de commit:
```bash
pnpm type-check
```
debe pasar (los tres workspaces compilan).
  </action>
  <verify>
    <automated>pnpm type-check</automated>
  </verify>
  <done>
    - 5 nuevos campos opcionales / tipos exportados en `packages/shared/src/types/index.ts` (verifiable con grep: `disponibilidad?: Disponibilidad`, `etaDiasEstimados?:`, `otNumeroVinculada?:`, `fechaAceptacion?:`, `presupuestoItemId?: string`).
    - `Disponibilidad` union + `DISPONIBILIDAD_LABELS` + `DISPONIBILIDAD_COLORS` exportados.
    - `pnpm type-check` GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure-function STUBS en entregasResolver.ts + EntregaRow type + Semaforo union</name>
  <files>apps/sistema-modular/src/utils/entregasResolver.ts</files>
  <behavior>
    - Archivo nuevo exporta `computeSemaforo`, `computeEtaFecha`, `buildEntregaRows` con firmas correctas.
    - Las 3 funciones LANZAN `Error('NotImplemented — Plan 16-03')` (stubs).
    - `EntregaRow` interface exportada con TODOS los campos del Interface block (16 campos en total).
    - `Semaforo` union type exportado (`'verde' | 'amarillo' | 'rojo' | 'entregado' | 'sin_eta'`).
    - `SEMAFORO_COLORS` map exportado (tailwind classes para el render).
    - `pnpm type-check` pasa — el archivo compila aunque las impls tiren en runtime.
  </behavior>
  <action>
Crear `apps/sistema-modular/src/utils/entregasResolver.ts` con stubs:

```typescript
/**
 * Phase 16 — Pure-function resolver para el visor de entregas.
 *
 * 3 funciones puras testeables sin Firestore:
 *   - computeSemaforo(diasRestantes, opts) — clasifica el semáforo
 *   - computeEtaFecha(fechaAceptacionIso, etaDiasEstimados) — calcula la fecha ETA
 *   - buildEntregaRows(input) — joins en memoria de la cadena ppto→req→oc→imp
 *
 * Plan 16-01 (Wave 0): STUBS — funciones tiran NotImplemented.
 * Plan 16-03 (Wave 1): impls. Tests turn GREEN.
 */
import type {
  Presupuesto,
  PresupuestoItem,
  RequerimientoCompra,
  Importacion,
  ItemImportacion,
  Disponibilidad,
} from '@ags/shared';

export type Semaforo = 'verde' | 'amarillo' | 'rojo' | 'entregado' | 'sin_eta';

export const SEMAFORO_COLORS: Record<Semaforo, string> = {
  verde:     'text-emerald-600',
  amarillo:  'text-amber-500',
  rojo:      'text-red-600',
  entregado: 'text-slate-400',
  sin_eta:   'text-slate-300',
};

export const SEMAFORO_LABELS: Record<Semaforo, string> = {
  verde:     'En plazo',
  amarillo:  'Próximo',
  rojo:      'Vencido',
  entregado: 'Entregado',
  sin_eta:   'Sin ETA',
};

export interface EntregaRow {
  presupuestoId: string;
  presupuestoNumero: string;
  itemId: string;
  clienteId: string;
  clienteNombre: string;
  descripcion: string;
  cantidad: number;
  precioUnitario: number;
  moneda: 'USD' | 'ARS' | 'EUR' | null;
  disponibilidad: Disponibilidad | null;
  etaDiasEstimados: number | null;
  fechaAceptacion: string | null;
  etaFecha: string | null;
  diasRestantes: number | null;
  semaforo: Semaforo;
  otNumeroVinculada: string | null;
  requerimientoId: string | null;
  requerimientoNumero: string | null;
  ocNumero: string | null;
  importacionId: string | null;
  importacionNumero: string | null;
  importacionEstado: string | null;
}

export interface BuildEntregaRowsInput {
  presupuestos: Array<Pick<Presupuesto, 'id' | 'numero' | 'clienteId' | 'estado' | 'items' | 'fechaAceptacion'>>;
  requerimientos: RequerimientoCompra[];
  ordenesCompra: Array<{ id: string; numero: string; items: Array<{ id: string; requerimientoId?: string | null }> }>;
  importaciones: Array<Pick<Importacion, 'id' | 'numero' | 'estado' | 'items'>>;
  clienteNombreById: Map<string, string>;
  /** Inyectable para tests; default = new Date() */
  now?: Date;
}

/**
 * STUB — Plan 16-03 implementará.
 *
 * Returns:
 *   - 'sin_eta' si diasRestantes === null
 *   - 'entregado' si opts.entregado === true
 *   - 'verde' si diasRestantes > 5
 *   - 'amarillo' si 0 <= diasRestantes <= 5
 *   - 'rojo' si diasRestantes < 0
 */
export function computeSemaforo(
  _diasRestantes: number | null,
  _opts?: { entregado?: boolean },
): Semaforo {
  throw new Error('NotImplemented — Plan 16-03');
}

/**
 * STUB — Plan 16-03 implementará.
 *
 * Returns ISO date (YYYY-MM-DD) of fechaAceptacion + etaDiasEstimados, or null si falta dato.
 */
export function computeEtaFecha(
  _fechaAceptacionIso: string | null,
  _etaDiasEstimados: number | null,
): string | null {
  throw new Error('NotImplemented — Plan 16-03');
}

/**
 * STUB — Plan 16-03 implementará.
 *
 * Toma presupuestos + requerimientos + OCs + importaciones + clienteNombreById y
 * produce una lista plana de EntregaRow (una por item de cada presupuesto activo).
 *
 * Joins:
 *   - presupuestoItem ↔ requerimiento via `req.presupuestoItemId === item.id`
 *     (fallback: req.presupuestoId + articuloId match si presupuestoItemId no existe — legacy)
 *   - requerimiento ↔ ocItem via `ocItem.requerimientoId === req.id`
 *   - requerimiento ↔ itemImportacion via `itemImp.requerimientoId === req.id`
 */
export function buildEntregaRows(_input: BuildEntregaRowsInput): EntregaRow[] {
  throw new Error('NotImplemented — Plan 16-03');
}
```

NOTA: NO importar `Importacion` desde `@ags/shared` si no está exportado del barrel — verificar con grep. Si solo está exportado como type, ajustar a `import type`.

Verificación pre-commit:
```bash
pnpm --filter @ags/sistema-modular exec tsc --noEmit
```
debe pasar (compila aunque las impls tiren en runtime).
  </action>
  <verify>
    <automated>pnpm --filter @ags/sistema-modular exec tsc --noEmit</automated>
  </verify>
  <done>
    - `apps/sistema-modular/src/utils/entregasResolver.ts` existe con 3 funciones stub + `EntregaRow` + `Semaforo` + `SEMAFORO_COLORS` + `SEMAFORO_LABELS`.
    - Compila TS sin errores.
    - Llamar a `computeSemaforo(0)` desde Node lanza `Error('NotImplemented — Plan 16-03')` (verificable en Task 4).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fixtures para ENT-01..ENT-06 (presupuesto + req + OC + importacion mock data)</name>
  <files>apps/sistema-modular/src/__tests__/fixtures/entregas.ts</files>
  <behavior>
    - Exporta builders / factory functions que devuelven instancias mínimas de `Presupuesto`, `RequerimientoCompra`, OC stub, `Importacion` para los 6 escenarios.
    - Cada fixture cubre EXACTAMENTE el caso del test ENT-XX correspondiente.
    - Sin Firestore — solo objetos JS literales con shape correcto.
  </behavior>
  <action>
Crear `apps/sistema-modular/src/__tests__/fixtures/entregas.ts`:

```typescript
/**
 * Phase 16 — Fixtures para test:entregas.
 * Patrón: builders puros que devuelven shapes válidos del modelo @ags/shared.
 * Sin Firestore, sin Date.now() — fechas hardcoded para tests determinísticos.
 */
import type {
  Presupuesto,
  PresupuestoItem,
  RequerimientoCompra,
  Importacion,
} from '@ags/shared';

// Fecha base "hoy" para tests determinísticos: 2026-06-01
export const FIXTURE_NOW = new Date('2026-06-01T12:00:00.000Z');

// ENT-01 / ENT-02 helpers — fechaAceptacion = 2026-05-15 (17 días antes de "hoy")
// Si etaDiasEstimados = 30 → etaFecha = 2026-06-14 → diasRestantes = 13 → verde
// Si etaDiasEstimados = 20 → etaFecha = 2026-06-04 →  3 → amarillo
// Si etaDiasEstimados = 10 → etaFecha = 2026-05-25 → -7 → rojo

export function makePresupuestoBase(overrides: Partial<Presupuesto> = {}): Presupuesto {
  return {
    id: 'PPTO-001',
    numero: 'PRE-0001',
    tipo: 'partes',
    moneda: 'USD',
    clienteId: 'CLI-001',
    estado: 'aceptado',
    items: [],
    subtotal: 0,
    total: 0,
    ordenesCompraIds: [],
    adjuntos: [],
    validezDias: 15,
    fechaAceptacion: '2026-05-15T10:00:00.000Z',
    createdAt: '2026-05-10T10:00:00.000Z',
    updatedAt: '2026-05-15T10:00:00.000Z',
    ...overrides,
  } as Presupuesto;
}

export function makeItem(overrides: Partial<PresupuestoItem> = {}): PresupuestoItem {
  return {
    id: 'ITEM-1',
    descripcion: 'Columna HPLC Eclipse XDB-C18',
    cantidad: 1,
    unidad: 'unidad',
    precioUnitario: 1200,
    subtotal: 1200,
    moneda: 'USD',
    disponibilidad: 'a_importar',
    etaDiasEstimados: 30,
    ...overrides,
  } as PresupuestoItem;
}

export function makeRequerimiento(overrides: Partial<RequerimientoCompra> = {}): RequerimientoCompra {
  return {
    id: 'REQ-001',
    numero: 'REQ-0001',
    articuloId: 'ART-1',
    articuloCodigo: 'G1312-60067',
    articuloDescripcion: 'Columna HPLC Eclipse XDB-C18',
    cantidad: 1,
    unidadMedida: 'unidad',
    motivo: 'Auto',
    origen: 'presupuesto',
    estado: 'pendiente',
    solicitadoPor: 'Sistema',
    fechaSolicitud: '2026-05-15T10:00:00.000Z',
    presupuestoId: 'PPTO-001',
    presupuestoItemId: 'ITEM-1',     // ENT-03 critical: join key
    createdAt: '2026-05-15T10:00:00.000Z',
    updatedAt: '2026-05-15T10:00:00.000Z',
    ...overrides,
  } as RequerimientoCompra;
}

export function makeOC(overrides: { id?: string; numero?: string; items?: Array<{ id: string; requerimientoId?: string | null }> } = {}) {
  return {
    id: 'OC-001',
    numero: 'OC-2025-0042',
    items: [{ id: 'OCITEM-1', requerimientoId: 'REQ-001' }],
    ...overrides,
  };
}

export function makeImportacion(overrides: Partial<Importacion> = {}): Importacion {
  return {
    id: 'IMP-001',
    numero: 'IMP-0001',
    estado: 'en_transito',
    ordenCompraId: 'OC-001',
    ordenCompraNumero: 'OC-2025-0042',
    proveedorId: 'PROV-1',
    proveedorNombre: 'Agilent',
    items: [
      {
        id: 'IMPITEM-1',
        itemOCId: 'OCITEM-1',
        articuloId: 'ART-1',
        articuloCodigo: 'G1312-60067',
        descripcion: 'Columna HPLC',
        cantidadPedida: 1,
        cantidadRecibida: 0,
        unidadMedida: 'unidad',
        requerimientoId: 'REQ-001',
      },
    ],
    createdAt: '2026-05-16T10:00:00.000Z',
    updatedAt: '2026-05-16T10:00:00.000Z',
  } as unknown as Importacion;
}

export const CLIENTE_NOMBRE_BY_ID = new Map<string, string>([
  ['CLI-001', 'Bayer S.A.'],
]);
```

NOTE: If `Importacion` requires fields not provided by `makeImportacion` (verify exact shape in `@ags/shared`), add minimal stubs as `null`/`undefined`-safe defaults. The `as unknown as Importacion` cast is OK for fixture purposes — if compile fails, expand the literal to satisfy required fields.
  </action>
  <verify>
    <automated>pnpm --filter @ags/sistema-modular exec tsc --noEmit</automated>
  </verify>
  <done>
    - Archivo creado, exporta 5 builders + `CLIENTE_NOMBRE_BY_ID` + `FIXTURE_NOW`.
    - TS compila (cast a unknown si hace falta para campos legacy de Importacion).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Test suite ENT-01..ENT-06 (RED baseline) + test-entregas.ts entry + package.json script</name>
  <files>apps/sistema-modular/src/__tests__/entregasResolver.test.ts, apps/sistema-modular/scripts/test-entregas.ts, apps/sistema-modular/package.json</files>
  <behavior>
    - `entregasResolver.test.ts` define 6 tests con `node:test` + `node:assert/strict` (mismo patrón que `patronBom.test.ts`).
    - Cada test loguea `[ENT-XX label]` para grep stdout.
    - `pnpm --filter @ags/sistema-modular test:entregas` ejecuta el runner; al landearse este plan, todos los tests FALLAN con `NotImplemented — Plan 16-03` o assertion mismatch (RED esperado).
    - El comando devuelve exit code != 0 (eso es el GREEN signal de "Wave 0 RED baseline establecido").
  </behavior>
  <action>
**(a) Crear `apps/sistema-modular/src/__tests__/entregasResolver.test.ts`:**

```typescript
/**
 * Phase 16 — Entregas Resolver unit suite (Wave 0 RED baseline).
 *
 * Run with: pnpm --filter @ags/sistema-modular test:entregas
 *
 * Uses node:test + node:assert/strict — no framework install.
 *
 * === RED baseline ===
 * Tests fail until:
 *   - 16-03 implements computeSemaforo, computeEtaFecha, buildEntregaRows
 *   - 16-02 ensures Presupuesto.fechaAceptacion + RequerimientoCompra.presupuestoItemId
 *     are written by aceptarConRequerimientos (only affects ENT-03 integration via fixtures)
 *
 * Each test logs [ENT-XX label] for stdout grep:
 *   pnpm --filter @ags/sistema-modular test:entregas 2>&1 | grep '\[ENT-'
 */
import { test } from 'node:test';
import assert from 'node:assert/strict';

import {
  computeSemaforo,
  computeEtaFecha,
  buildEntregaRows,
} from '../utils/entregasResolver.ts';
import {
  FIXTURE_NOW,
  makePresupuestoBase,
  makeItem,
  makeRequerimiento,
  makeOC,
  makeImportacion,
  CLIENTE_NOMBRE_BY_ID,
} from './fixtures/entregas.ts';

test('[ENT-01] computeSemaforo classifies verde/amarillo/rojo/sin_eta correctly', () => {
  assert.equal(computeSemaforo(10), 'verde',     'diasRestantes=10 → verde (>5)');
  assert.equal(computeSemaforo(6),  'verde',     'diasRestantes=6  → verde (>5)');
  assert.equal(computeSemaforo(5),  'amarillo',  'diasRestantes=5  → amarillo (0..5)');
  assert.equal(computeSemaforo(0),  'amarillo',  'diasRestantes=0  → amarillo (boundary)');
  assert.equal(computeSemaforo(-1), 'rojo',      'diasRestantes=-1 → rojo (<0)');
  assert.equal(computeSemaforo(null), 'sin_eta', 'diasRestantes=null → sin_eta');
});

test('[ENT-02] computeEtaFecha computes fechaAceptacion + etaDiasEstimados correctly', () => {
  // 2026-05-15 + 30 = 2026-06-14
  const eta = computeEtaFecha('2026-05-15T10:00:00.000Z', 30);
  assert.ok(eta && eta.startsWith('2026-06-14'), `expected 2026-06-14, got ${eta}`);

  // null inputs → null
  assert.equal(computeEtaFecha(null, 30), null, 'fechaAceptacion null → null');
  assert.equal(computeEtaFecha('2026-05-15T10:00:00.000Z', null), null, 'etaDiasEstimados null → null');
});

test('[ENT-03] buildEntregaRows resolves ppto→req→oc→imp chain via presupuestoItemId', () => {
  const ppto = makePresupuestoBase({
    items: [makeItem({ id: 'ITEM-1', etaDiasEstimados: 30 })],
  });
  const req = makeRequerimiento({ presupuestoItemId: 'ITEM-1', ordenCompraId: 'OC-001', ordenCompraNumero: 'OC-2025-0042' });
  const oc = makeOC();
  const imp = makeImportacion();

  const rows = buildEntregaRows({
    presupuestos: [ppto],
    requerimientos: [req],
    ordenesCompra: [oc],
    importaciones: [imp],
    clienteNombreById: CLIENTE_NOMBRE_BY_ID,
    now: FIXTURE_NOW,
  });

  assert.equal(rows.length, 1, 'exactly 1 row per item');
  const r = rows[0];
  assert.equal(r.presupuestoNumero, 'PRE-0001');
  assert.equal(r.clienteNombre, 'Bayer S.A.', 'clienteNombre resolved from map');
  assert.equal(r.requerimientoId, 'REQ-001', 'req joined by presupuestoItemId');
  assert.equal(r.ocNumero, 'OC-2025-0042', 'oc joined via requerimientoId on ocItem');
  assert.equal(r.importacionId, 'IMP-001');
  assert.equal(r.importacionNumero, 'IMP-0001');
  assert.equal(r.importacionEstado, 'en_transito');
  assert.ok(r.etaFecha && r.etaFecha.startsWith('2026-06-14'), `etaFecha computed: ${r.etaFecha}`);
  assert.equal(r.diasRestantes, 13, 'diasRestantes from FIXTURE_NOW (2026-06-01) to 2026-06-14');
  assert.equal(r.semaforo, 'verde', '13 días → verde');
});

test('[ENT-04] items sin etaDiasEstimados → semaforo = sin_eta (no crash)', () => {
  const ppto = makePresupuestoBase({
    items: [makeItem({ id: 'ITEM-X', etaDiasEstimados: null })],
  });
  const rows = buildEntregaRows({
    presupuestos: [ppto],
    requerimientos: [],
    ordenesCompra: [],
    importaciones: [],
    clienteNombreById: CLIENTE_NOMBRE_BY_ID,
    now: FIXTURE_NOW,
  });
  assert.equal(rows.length, 1);
  assert.equal(rows[0].etaFecha, null);
  assert.equal(rows[0].diasRestantes, null);
  assert.equal(rows[0].semaforo, 'sin_eta');
});

test('[ENT-05] item con importacion.estado=recibido → semaforo = entregado', () => {
  const ppto = makePresupuestoBase({
    items: [makeItem({ id: 'ITEM-1', etaDiasEstimados: 10 /* would be rojo without entregado override */ })],
  });
  const req = makeRequerimiento({ presupuestoItemId: 'ITEM-1' });
  const oc = makeOC();
  const imp = makeImportacion({ estado: 'recibido', items: [
    { id: 'IMPITEM-1', itemOCId: 'OCITEM-1', articuloId: 'ART-1', articuloCodigo: 'G1312-60067',
      descripcion: 'Columna', cantidadPedida: 1, cantidadRecibida: 1, unidadMedida: 'unidad', requerimientoId: 'REQ-001' },
  ] });

  const rows = buildEntregaRows({
    presupuestos: [ppto],
    requerimientos: [req],
    ordenesCompra: [oc],
    importaciones: [imp],
    clienteNombreById: CLIENTE_NOMBRE_BY_ID,
    now: FIXTURE_NOW,
  });
  assert.equal(rows[0].importacionEstado, 'recibido');
  assert.equal(rows[0].semaforo, 'entregado', 'recibido → entregado (no rojo)');
});

test('[ENT-06] item sin requerimiento (stock available) sigue mostrando row (sin cadena req/oc/imp)', () => {
  const ppto = makePresupuestoBase({
    items: [makeItem({ id: 'ITEM-S', disponibilidad: 'stock', etaDiasEstimados: 2 })],
  });
  const rows = buildEntregaRows({
    presupuestos: [ppto],
    requerimientos: [],
    ordenesCompra: [],
    importaciones: [],
    clienteNombreById: CLIENTE_NOMBRE_BY_ID,
    now: FIXTURE_NOW,
  });
  assert.equal(rows.length, 1);
  assert.equal(rows[0].disponibilidad, 'stock');
  assert.equal(rows[0].requerimientoId, null);
  assert.equal(rows[0].ocNumero, null);
  assert.equal(rows[0].importacionId, null);
  // etaDiasEstimados=2, fechaAceptacion=2026-05-15 → eta=2026-05-17 → diasRestantes=-15 → rojo
  assert.equal(rows[0].semaforo, 'rojo');
});
```

**(b) Crear `apps/sistema-modular/scripts/test-entregas.ts`:**

```typescript
// Entry point for pnpm --filter @ags/sistema-modular test:entregas
// Pattern: mirror scripts/test-patron-bom.ts (Phase 14) — side-effect import dispara
// node:test runner. RED baseline lands en 16-01; turns GREEN en 16-03.
import '../src/__tests__/entregasResolver.test.ts';
```

**(c) Editar `apps/sistema-modular/package.json`:**

Agregar **una sola línea** dentro de `"scripts"` después de `"test:venta-loaner"`:
```json
    "test:entregas": "tsx scripts/test-entregas.ts",
```

Verificación:
```bash
pnpm --filter @ags/sistema-modular test:entregas
```
Esperado: FALLA con `NotImplemented — Plan 16-03` o `AssertionError` en cada test. Exit code != 0. **Eso es el GREEN signal de "Wave 0 baseline establecido"**.

NO marcar el plan como fallido por esto — la convención del repo (Phases 14/15) es Wave 0 RED + verificación de RED.

Para evitar bloquear CI futuro, NO se agrega `test:entregas` al script `test` agregado de root (no existe `pnpm test` agregado por ahora). Cada plan downstream sigue corriendo el suite explícitamente.
  </action>
  <verify>
    <automated>pnpm --filter @ags/sistema-modular test:entregas; if [ $? -ne 0 ]; then echo 'RED baseline OK'; exit 0; else echo 'unexpected GREEN — stubs should fail'; exit 1; fi</automated>
  </verify>
  <done>
    - Tres archivos creados/editados (test file + entry + package.json).
    - `pnpm --filter @ags/sistema-modular test:entregas` corre, ejecuta los 6 tests y todos FALLAN con NotImplemented / AssertionError (RED baseline).
    - Stdout contiene 6 líneas `[ENT-XX ...]` (grep-friendly).
  </done>
</task>

</tasks>

<verification>
1. Type-check global (no break en otros workspaces):
   ```bash
   pnpm type-check
   ```
2. Test runner registered:
   ```bash
   pnpm --filter @ags/sistema-modular test:entregas
   ```
   Esperado: 6 tests, todos en RED. Stdout contiene `[ENT-01]`..`[ENT-06]`.
3. Grep evidence de tipos extendidos:
   ```bash
   grep -E "disponibilidad\?|etaDiasEstimados\?|otNumeroVinculada\?|fechaAceptacion\?|presupuestoItemId\?" packages/shared/src/types/index.ts
   ```
   Esperado: 5 matches (uno por field nuevo).
4. Verify stubs exist:
   ```bash
   grep -E "computeSemaforo|computeEtaFecha|buildEntregaRows" apps/sistema-modular/src/utils/entregasResolver.ts
   ```
</verification>

<success_criteria>
- [ ] `packages/shared/src/types/index.ts` extendido con `Disponibilidad` union + 5 nuevos campos opcionales (compila project-wide).
- [ ] `apps/sistema-modular/src/utils/entregasResolver.ts` exporta `EntregaRow`, `Semaforo`, `SEMAFORO_COLORS`, `SEMAFORO_LABELS`, `computeSemaforo`, `computeEtaFecha`, `buildEntregaRows` (stubs).
- [ ] `apps/sistema-modular/src/__tests__/fixtures/entregas.ts` exporta los 5 builders + map de cliente nombre.
- [ ] `apps/sistema-modular/src/__tests__/entregasResolver.test.ts` define 6 tests etiquetados `[ENT-01]`..`[ENT-06]`.
- [ ] `apps/sistema-modular/scripts/test-entregas.ts` re-exporta el suite.
- [ ] `apps/sistema-modular/package.json` tiene script `"test:entregas": "tsx scripts/test-entregas.ts"`.
- [ ] `pnpm type-check` GREEN.
- [ ] `pnpm --filter @ags/sistema-modular test:entregas` corre y devuelve exit != 0 con 6 fallos `[ENT-XX]` (RED baseline confirmado).
</success_criteria>

<output>
After completion, create `.planning/phases/16-entregas-visor-de-cumplimiento/16-01-SUMMARY.md` con:
- Lista de campos agregados en `@ags/shared`.
- Lista de exports del stub `entregasResolver.ts`.
- Comando exacto del script `test:entregas` (copy-pasteable).
- Confirmación de RED baseline (6/6 fallos esperados).
- Próximos pasos: 16-02 (backend writes) + 16-03 (resolver impls — turns GREEN).
</output>
