---
phase: 07-presupuesto-per-incident
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/sistema-modular/src/components/presupuestos/PresupuestoItemsTable.tsx
  - apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx
  - apps/sistema-modular/src/components/presupuestos/pdf/PresupuestoPDFEstandar.tsx
  - apps/sistema-modular/src/pages/presupuestos/PresupuestoNew.tsx
autonomous: false
requirements:
  - PTYP-01
  - FMT-01
user_setup: []

must_haves:
  truths:
    - "Un vendedor puede crear un presupuesto tipo 'servicio' desde un ticket (LeadDetail → CreatePresupuestoModal) con clienteId/contacto/sistema prellenados, agregar ítems desde el catálogo ConceptoServicio con valorBase como referencia editable, y guardarlo como borrador"
    - "Un vendedor puede crear un presupuesto tipo 'servicio' standalone desde /presupuestos/nuevo o desde /presupuestos (botón + Nuevo Presupuesto) sin origen de ticket"
    - "Cuando tipo==='servicio' la tabla flat PresupuestoItemsTable se renderiza (no la jerárquica de contrato) y el selector de equipo/módulo del AddItemModal NO aparece (solo aparece para 'contrato')"
    - "El PDF del presupuesto tipo 'servicio' se genera con PresupuestoPDFEstandar — muestra header AGS + razón social + CUIT + dirección establecimiento + tabla ítems (descripción/cant/unidad/precio/subtotal) + totales por moneda (MIXTA soportado) + bloque de validez prominente + condiciones comerciales en Page 2"
    - "PresupuestosList filtra correctamente por tipo='servicio' vía useUrlFilters; la columna Tipo muestra el badge 'Servicio' con el color TIPO_PRESUPUESTO_COLORS.servicio"
  artifacts:
    - path: "apps/sistema-modular/src/components/presupuestos/PresupuestoItemsTable.tsx"
      provides: "Tabla flat de ítems que no pasa 'sistemas' al AddItemModal cuando tipoPresupuesto!=='contrato'"
      contains: "tipoPresupuesto === 'contrato'"
    - path: "apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx"
      provides: "Modal con el panel 'Vincular a equipo' gated por tipoPresupuesto==='contrato'"
      contains: "tipoPresupuesto === 'contrato'"
    - path: "apps/sistema-modular/src/components/presupuestos/pdf/PresupuestoPDFEstandar.tsx"
      provides: "PDF Editorial Teal con validez prominente y sin header 'NOTAS TÉCNICAS' huérfano"
      contains: "Oferta válida por"
    - path: "apps/sistema-modular/src/pages/presupuestos/PresupuestoNew.tsx"
      provides: "Wizard standalone que crea presupuesto con tipo:'servicio' explícito"
      contains: "tipo: 'servicio'"
  key_links:
    - from: "apps/sistema-modular/src/components/presupuestos/EditPresupuestoModal.tsx"
      to: "apps/sistema-modular/src/components/presupuestos/PresupuestoItemsTable.tsx"
      via: "tipoPresupuesto prop"
      pattern: "tipoPresupuesto=\\{form\\.tipo\\}"
    - from: "apps/sistema-modular/src/components/presupuestos/PresupuestoItemsTable.tsx"
      to: "apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx"
      via: "sistemas prop gated by tipoPresupuesto"
      pattern: "sistemas=\\{tipoPresupuesto === 'contrato'"
    - from: "apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx"
      to: "Panel 'Vincular a equipo'"
      via: "condicional por tipoPresupuesto"
      pattern: "tipoPresupuesto === 'contrato' && sistemas"
---

<objective>
Audit + fix del flow end-to-end del presupuesto tipo `'servicio'` (alias interno de per_incident), con polish del PDF estándar que servirá como template base para `partes`, `mixto` y `ventas` en Phase 10.

Purpose: El código ya existe al ~70% (tipo `'servicio'` en enum desde Phase anterior, tabla flat, wizards de creación, PDF estándar reutilizable). El trabajo aquí es cerrar huecos del audit: aislar el selector de equipo/módulo del `AddItemModal` para que NO aparezca en `'servicio'` (hoy se muestra siempre que el cliente tiene sistemas, lo cual contamina la UX del tipo `'servicio'`); pulir el `PresupuestoPDFEstandar` (remover header "NOTAS TÉCNICAS" huérfano, promover bloque de validez como protección contractual); y verificar que los dos caminos de creación (desde ticket vía `CreatePresupuestoModal`, standalone vía `PresupuestoNew`) funcionan end-to-end.

Output: Tabla flat limpia para `'servicio'`, PDF estándar pulido, wizards que crean el tipo correctamente.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-presupuesto-per-incident/07-CONTEXT.md

@CLAUDE.md
@.claude/rules/firestore.md
@.claude/rules/components.md

@apps/sistema-modular/src/components/presupuestos/EditPresupuestoModal.tsx
@apps/sistema-modular/src/components/presupuestos/PresupuestoItemsTable.tsx
@apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx
@apps/sistema-modular/src/components/presupuestos/pdf/PresupuestoPDFEstandar.tsx
@apps/sistema-modular/src/components/presupuestos/pdf/generatePresupuestoPDF.tsx
@apps/sistema-modular/src/pages/presupuestos/PresupuestoNew.tsx
@apps/sistema-modular/src/pages/presupuestos/PresupuestosList.tsx
@apps/sistema-modular/src/components/presupuestos/CreatePresupuestoModal.tsx
@apps/sistema-modular/src/hooks/useCreatePresupuestoForm.ts
@apps/sistema-modular/src/hooks/usePresupuestoEdit.ts
@apps/sistema-modular/src/services/presupuestosService.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Do NOT re-explore the codebase for these — use as-is. -->

From packages/shared/src/types/index.ts (line 753+):
```typescript
export type TipoPresupuesto = 'servicio' | 'partes' | 'ventas' | 'contrato' | 'mixto';
export type PresupuestoEstado = 'borrador' | 'enviado' | 'aceptado' | 'en_ejecucion' | 'anulado' | 'finalizado';
export const TIPO_PRESUPUESTO_LABELS: Record<TipoPresupuesto, string> = {
  servicio: 'Servicio', partes: 'Partes', ventas: 'Ventas', contrato: 'Contrato', mixto: 'Mixto',
};

export interface Presupuesto {
  id: string;
  numero: string;              // PRE-XXXX.NN
  tipo: TipoPresupuesto;       // 'servicio' for per_incident
  moneda: MonedaPresupuesto;   // 'USD' | 'ARS' | 'EUR' | 'MIXTA'
  clienteId: string;
  establecimientoId?: string | null;
  sistemaId?: string | null;
  contactoId?: string | null;
  estado: PresupuestoEstado;
  items: PresupuestoItem[];
  subtotal: number;
  total: number;
  validezDias?: number;        // default 15
  fechaEnvio?: string | null;
  notasTecnicas?: string | null;
  condicionesComerciales?: string | null;
  // ... (see types file)
}
```

From apps/sistema-modular/src/components/presupuestos/PresupuestoItemsTable.tsx (lines 19-38):
```typescript
interface PresupuestoItemsTableProps {
  items: PresupuestoItem[];
  categoriasPresupuesto: CategoriaPresupuesto[];
  conceptosServicio: ConceptoServicio[];
  moneda: MonedaPresupuesto;
  totals: PresupuestoTotals;
  notasTecnicas: string;
  condicionesComerciales: string;
  onAddItem: (item: PresupuestoItem) => void;
  onUpdateItem: (itemId: string, field: keyof PresupuestoItem, value: any) => void;
  onRemoveItem: (itemId: string) => void;
  onNotasTecnicasChange: (v: string) => void;
  onCondicionesChange: (v: string) => void;
  calculateItemTaxes: (item: PresupuestoItem) => { iva; ganancias; iibb; totalImpuestos };
  tipoPresupuesto?: string;
  sistemas?: Sistema[];
  loadModulos?: (sistemaId: string) => Promise<ModuloSistema[]>;
  itemsByGrupo?: GrupoSistema[];
  getGrupo?: (sistemaId: string | null | undefined) => number;
}
```

From apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx (lines 14-28):
```typescript
interface AddItemModalProps {
  newItem: Partial<PresupuestoItem>;
  setNewItem: (v: Partial<PresupuestoItem>) => void;
  categoriasPresupuesto: CategoriaPresupuesto[];
  conceptosServicio: ConceptoServicio[];
  moneda?: string;
  onAdd: () => void;
  onClose: () => void;
  sistemas?: Sistema[];
  loadModulos?: (sistemaId: string) => Promise<ModuloSistema[]>;
  tipoPresupuesto?: string;  // <- ALREADY receives it but DOES NOT use it in the equipo panel gate
}
```

Service pattern (apps/sistema-modular/src/services/presupuestosService.ts line 196):
```typescript
// create() already defaults to 'servicio' if not provided:
tipo: presupuestoData.tipo || 'servicio',
```
</interfaces>

<audit_findings>
<!-- Concrete issues found during task-level investigation. -->
<!-- Reference these when executing — each task below addresses one or more. -->

**FINDING-1 (BLOCKING for PTYP-01):** `PresupuestoItemsTable.tsx` line 188-207 pasa `sistemas={clienteSistemas}` al `AddItemModal` incluso cuando `tipo !== 'contrato'`. `AddItemModal.tsx` renderiza el panel azul "Vincular a equipo" (lines 144-202) cuando `sistemas && sistemas.length > 0`, SIN gate por `tipoPresupuesto`. Resultado: el vendedor crea un presupuesto `'servicio'` y ve un panel de vinculación de equipos que no debería estar. El prop `tipoPresupuesto` ya existe en las props pero no se usa en la condicional.

**FINDING-2 (Polish for FMT-01):** `PresupuestoPDFEstandar.tsx` line 401-403 renderiza un header "NOTAS TÉCNICAS:" en Page 1 con un espacio vacío debajo, aunque las notas técnicas reales se renderizan en Page 2 via `PDFCondiciones`. Este header huérfano es ruido visual.

**FINDING-3 (Polish for FMT-01):** `PresupuestoPDFEstandar.tsx` line 236-238 tiene un `Text style={S.validez}` con "Oferta válida por N días" como un línea minor. Para tipo `'servicio'` sin snapshot de precio (decisión CONTEXT), ESTE bloque es la protección contractual — debe ser MÁS prominente (bordeado, con color primary, posicionado antes del disclaimer).

**FINDING-4 (Polish for FMT-01):** `PresupuestoPDFEstandar.tsx` line 69 `<Text style={S.titleIcon}>X</Text>` es un placeholder legacy. Reemplazar por nada (vacío) o un símbolo neutro consistente con la identidad Editorial Teal.

**FINDING-5 (PTYP-01 — standalone flow):** `PresupuestoNew.tsx` (line 87-98) crea sin campo `tipo` explícito. Depende del fallback en `presupuestosService.create()` (línea 196: `tipo: presupuestoData.tipo || 'servicio'`). Funciona pero es implícito — setearlo explícito para que sea auditable.

**FINDING-6 (VERIFY, no code change):** `CreatePresupuestoModal` (desde ticket) ya funciona — `useCreatePresupuestoForm` línea 27 default es `tipo: 'servicio'`, el header tiene dropdown de tipo. Flow probado desde `LeadDetail.tsx` línea 365-378.

**FINDING-7 (VERIFY, no code change):** `PresupuestosList.tsx` línea 109-111 filtra por `p.tipo !== filters.tipo`. El filtro de tipo usa `TIPO_PRESUPUESTO_LABELS`. El badge renderiza `TIPO_PRESUPUESTO_COLORS[p.tipo || 'servicio']` con fallback a `'servicio'` — funciona para presupuestos legacy sin campo tipo. OK.

**NON-FINDING (out of scope):** `PresupuestoCondicionesEditor` + plantillas rich-text → Phase 3 diferida. Ya hay campos freeform funcionando.
</audit_findings>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gate equipo panel del AddItemModal por tipoPresupuesto === 'contrato'</name>
  <files>
    - apps/sistema-modular/src/components/presupuestos/PresupuestoItemsTable.tsx
    - apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx
  </files>
  <action>
Cierra FINDING-1.

**En `PresupuestoItemsTable.tsx`:**
- Línea 167 (llamada a `<AddItemModal ... />`): cambiar `sistemas={sistemas}` y `loadModulos={loadModulos}` para que sólo se pasen cuando `tipoPresupuesto === 'contrato'`.

Diff conceptual:
```tsx
// ANTES:
<AddItemModal newItem={newItem} setNewItem={setNewItem} categoriasPresupuesto={categoriasPresupuesto}
  conceptosServicio={conceptosServicio} moneda={moneda} onAdd={handleAdd} onClose={() => setShowModal(false)}
  tipoPresupuesto={tipoPresupuesto} sistemas={sistemas} loadModulos={loadModulos} />

// DESPUÉS:
<AddItemModal newItem={newItem} setNewItem={setNewItem} categoriasPresupuesto={categoriasPresupuesto}
  conceptosServicio={conceptosServicio} moneda={moneda} onAdd={handleAdd} onClose={() => setShowModal(false)}
  tipoPresupuesto={tipoPresupuesto}
  sistemas={tipoPresupuesto === 'contrato' ? sistemas : undefined}
  loadModulos={tipoPresupuesto === 'contrato' ? loadModulos : undefined} />
```

**En `AddItemModal.tsx`:**
- Línea 144: cambiar la condición del panel "Vincular a equipo" de `{sistemas && sistemas.length > 0 && (` a `{tipoPresupuesto === 'contrato' && sistemas && sistemas.length > 0 && (`. Asegurar que `tipoPresupuesto` esté en la lista de props destructurada (línea 30) — ya está en la interfaz pero falta en el destructure. Agregarlo.

Diff conceptual en el destructure (línea 30):
```tsx
// ANTES:
export const AddItemModal = ({ newItem, setNewItem, categoriasPresupuesto, conceptosServicio, moneda, onAdd, onClose, sistemas, loadModulos }: AddItemModalProps) => {

// DESPUÉS:
export const AddItemModal = ({ newItem, setNewItem, categoriasPresupuesto, conceptosServicio, moneda, onAdd, onClose, sistemas, loadModulos, tipoPresupuesto }: AddItemModalProps) => {
```

Y la condición en línea 144:
```tsx
// ANTES:
{sistemas && sistemas.length > 0 && (
  <div className="bg-blue-50 p-3 rounded-lg space-y-2">

// DESPUÉS:
{tipoPresupuesto === 'contrato' && sistemas && sistemas.length > 0 && (
  <div className="bg-blue-50 p-3 rounded-lg space-y-2">
```

**Justificación:** El panel "Vincular a equipo" exista solo tiene sentido semántico para presupuestos de contrato (que organizan ítems por sistema/módulo). Para `'servicio'`, `'partes'`, `'mixto'`, `'ventas'` el ítem no se vincula a un equipo específico (eso se maneja a nivel presupuesto via `sistemaId`). Al ocultarlo, la UX de `'servicio'` queda limpia y no duplica la selección de sistema que ya está a nivel presupuesto.

**Regla firestore:** no hay writes en esta task — solo UI rendering.
**Regla componentes:** `AddItemModal.tsx` tiene 257 líneas hoy. Agregar un condicional no aumenta LOC de forma notable. Si queda >250 post-cambio, extraer el panel "Vincular a equipo" en `<EquipoLinkPanel />` subcomponente; verificar con `wc -l` post-edit.
  </action>
  <verify>
    <automated>pnpm --filter @ags/sistema-modular type-check</automated>
    <manual>
1. `pnpm dev:modular` en terminal
2. Navegar a `/presupuestos` → "+ Nuevo Presupuesto" → seleccionar cliente con sistemas vinculados → Tipo: "Servicio" → "+ Agregar" ítem
3. Verificar que el modal "Agregar item" NO muestra el panel azul "Vincular a equipo"
4. Cambiar Tipo a "Contrato" → "+ Agregar" ítem → verificar que el panel azul SÍ aparece
    </manual>
  </verify>
  <done>
- `tipoPresupuesto === 'contrato'` es la condición gate tanto en `PresupuestoItemsTable.tsx` (pass sistemas prop) como en `AddItemModal.tsx` (render equipo panel)
- `pnpm type-check` pasa sin errores en sistema-modular
- Verificación manual: modal limpio para `'servicio'`, panel presente para `'contrato'`
- `AddItemModal.tsx` sigue <250 LOC (verificar con `wc -l`)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Polish PresupuestoPDFEstandar — validez prominente, remove header huérfano, cleanup titleIcon</name>
  <files>
    - apps/sistema-modular/src/components/presupuestos/pdf/PresupuestoPDFEstandar.tsx
  </files>
  <action>
Cierra FINDING-2, FINDING-3, FINDING-4.

**Cambio 1 — Remover header "NOTAS TÉCNICAS" huérfano en Page 1 (FINDING-2):**
- Líneas 400-404: eliminar el bloque:
```tsx
<View style={S.separator} />
<Text style={{ fontSize: 7, fontWeight: 'bold', color: COLORS.primary, marginBottom: 4 }}>
  NOTAS TÉCNICAS:
</Text>
```
Las notas técnicas reales se renderizan en Page 2 via `<PDFCondiciones>` con su propio título. Dejar solo `<PDFFooter />`.

**Cambio 2 — Promover bloque de validez como protección contractual (FINDING-3):**
- Líneas 235-238 (`<Text style={S.validez}>...</Text>`): reemplazar por un bloque más prominente — card con borde teal, texto en negrita, fondo teal-50 equivalente. Recordar que es react-pdf (no Tailwind), usar inline styles con `COLORS.primary`, `COLORS.primaryLight` (o equivalente).

Diff conceptual:
```tsx
// ANTES:
<Text style={S.validez}>
  Oferta válida por {presupuesto.validezDias || 15} días.
</Text>

// DESPUÉS:
<View style={{
  borderWidth: 0.8,
  borderColor: COLORS.primary,
  backgroundColor: '#F0F9F9',  // teal-50 equivalent; add to COLORS if not present
  paddingVertical: 5,
  paddingHorizontal: 8,
  marginVertical: 6,
  borderRadius: 3,
}}>
  <Text style={{ fontSize: 7.5, color: COLORS.primary, fontWeight: 700, textAlign: 'center' }}>
    OFERTA VÁLIDA POR {presupuesto.validezDias || 15} DÍAS desde la fecha de emisión
  </Text>
</View>
```

Si `COLORS` no tiene una variante "light teal" para fondo, verificar el archivo `pdfStyles.ts` y reusar `COLORS.sectionBg` o agregar una constante nueva. NO inventar un hex arbitrario sin chequear — leer primero `apps/sistema-modular/src/components/presupuestos/pdf/pdfStyles.ts` (grep ya confirmó que existe `COLORS.sectionBg`, `COLORS.primary`, `COLORS.primaryLight`).

**Cambio 3 — Cleanup titleIcon placeholder (FINDING-4):**
- Línea 69: `<Text style={S.titleIcon}>X</Text>` → cambiar a `<Text style={S.titleIcon}>$</Text>` (símbolo monetario neutro, consistente con la semántica "presupuesto"). Alternativamente, si el styling del titleIcon no se ve bien con `$`, remover el `<Text>` entero y ajustar el layout del titleBox. Elegir la opción menos invasiva — ver cómo luce renderizado.

**Importante:** NO tocar la lógica de `isMixta`, los grupos por sistema (`hasGrupos`), los cuotas, ni nada del soporte MIXTA/contrato. Solo polish visual puntual.

**Regla reportes-ot:** este archivo NO está en `apps/reportes-ot/` — es el PDF de sistema-modular. No afecta la frozen surface.
  </action>
  <verify>
    <automated>pnpm --filter @ags/sistema-modular type-check</automated>
    <manual>
1. `pnpm dev:modular`
2. Abrir un presupuesto existente tipo `'servicio'` (o crear uno rápido con 2 ítems de catálogo)
3. Click "Descargar PDF" → abrir el PDF
4. Verificar Page 1:
   - Header AGS + título "PRESUPUESTO" (sin placeholder X raro)
   - Datos cliente
   - Tabla de ítems con descripcion/cant/precio/subtotal
   - Totales por moneda
   - **Bloque "OFERTA VÁLIDA POR N DÍAS"** prominente (bordeado/teal)
   - NO aparece el header suelto "NOTAS TÉCNICAS:" al final
5. Verificar Page 2: condiciones del presupuesto + firma
6. Verificar Page 3: bloque de conformidad + firma detallada
7. Probar MIXTA: crear presupuesto con moneda MIXTA, ítems en USD + ARS, descargar PDF → totales por moneda se muestran OK
    </manual>
  </verify>
  <done>
- Page 1 del PDF NO tiene header "NOTAS TÉCNICAS:" huérfano
- Bloque "OFERTA VÁLIDA POR N DÍAS" aparece prominente entre condPago/disclaimer y totals
- `titleIcon` placeholder "X" reemplazado (o el Text removido)
- MIXTA sigue funcionando (totales por moneda en el box de totals)
- `pnpm type-check` pasa
- Render visual confirmado en PDF generado
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: PresupuestoNew setea tipo:'servicio' explícito + verificar standalone flow</name>
  <files>
    - apps/sistema-modular/src/pages/presupuestos/PresupuestoNew.tsx
  </files>
  <action>
Cierra FINDING-5.

**En `PresupuestoNew.tsx` línea 87-98 (`handleSubmit`):** agregar `tipo: 'servicio'` explícito al objeto `presupuestoData`. Hoy depende del fallback del service que hace `tipo: presupuestoData.tipo || 'servicio'` (línea 196 de `presupuestosService.ts`). Hacerlo explícito elimina ambigüedad y hace auditable el comportamiento default del wizard standalone.

Diff conceptual:
```tsx
// ANTES:
const presupuestoData = {
  numero: '',
  clienteId: formData.clienteId,
  sistemaId: formData.sistemaId || null,
  contactoId: formData.contactoId || null,
  estado: 'borrador' as const,
  items: [],
  subtotal: 0,
  total: 0,
  ordenesCompraIds: [],
  ...(origenTipo ? { origenTipo, origenId, origenRef } : {}),
};

// DESPUÉS:
const presupuestoData = {
  numero: '',
  tipo: 'servicio' as const,  // per_incident por default — el wizard standalone crea servicio; contrato se inicia desde otro flow
  moneda: 'USD' as const,
  clienteId: formData.clienteId,
  sistemaId: formData.sistemaId || null,
  contactoId: formData.contactoId || null,
  estado: 'borrador' as const,
  items: [],
  subtotal: 0,
  total: 0,
  ordenesCompraIds: [],
  ...(origenTipo ? { origenTipo, origenId, origenRef } : {}),
};
```

**Regla firestore:** el objeto se pasa a `presupuestosService.create()` que ya usa `deepCleanForFirestore` internamente — no hace falta limpiar aquí.

**Verificación del flow completo (sin código nuevo):** después del cambio, validar los 2 caminos de creación:
- **Path A (desde ticket):** `/leads/{id}` (LeadDetail) → botón "Crear presupuesto" → abre `CreatePresupuestoModal` con prefill `clienteId`, `sistemaId`, `origenTipo: 'lead'` → usuario setea Tipo=Servicio (default) → agrega 1 item del catálogo → guarda → queda en `borrador` con `tipo: 'servicio'`.
- **Path B (standalone):** `/presupuestos/nuevo` (`PresupuestoNew`) → selecciona cliente → click "Crear Presupuesto" → redirige a `/presupuestos/{id}` (abre `EditPresupuestoModal` con `tipo: 'servicio'`) → agrega items → guarda.
- **Path C (list page):** `/presupuestos` → botón "+ Nuevo Presupuesto" → abre `CreatePresupuestoModal` (sin prefill) → crea tipo=Servicio por default.
  </action>
  <verify>
    <automated>pnpm --filter @ags/sistema-modular type-check</automated>
    <manual>
1. `pnpm dev:modular`
2. **Path A (desde ticket):** ir a `/leads` → abrir un lead con clienteId resuelto → "Crear presupuesto" → verificar prefill clienteId + Tipo=Servicio → agregar 1 ítem del catálogo (dropdown "Catalogo de servicios") → guardar → en `/presupuestos` aparece con badge "Servicio" + estado "Borrador"
3. **Path B (standalone):** `/presupuestos/nuevo` → seleccionar cliente → "Crear Presupuesto" → se abre el EditPresupuestoModal → verificar badge Tipo "Servicio" en metadata strip → agregar 1 ítem → guardar
4. **Path C (list):** `/presupuestos` → "+ Nuevo Presupuesto" → seleccionar cliente + Tipo=Servicio → agregar ítem → "Crear presupuesto" → aparece en la lista
5. Verificar en Firestore Console (o en la lista filtrando por Tipo=Servicio) que los 3 presupuestos tienen `tipo: 'servicio'` en el documento
    </manual>
  </verify>
  <done>
- `PresupuestoNew.tsx` setea `tipo: 'servicio'` y `moneda: 'USD'` explícito en el payload de creación
- `pnpm type-check` pasa
- 3 paths de creación probados end-to-end — todos resultan en presupuestos con `tipo: 'servicio'`
- List page los filtra correctamente por tipo (usando el dropdown "Tipo: Servicio")
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — Validar flow 'servicio' end-to-end</name>
  <files>(no file edits — verification-only checkpoint)</files>
  <action>Pausar ejecución. Ver bloque <how-to-verify> abajo para instrucciones detalladas. El humano ejecuta los tests manualmente y responde con el resume-signal.</action>
  <verify>
    <automated>true</automated>
    <manual>Ver <how-to-verify> abajo — tests manuales end-to-end</manual>
  </verify>
  <done>El humano valida via resume-signal — comportamientos observados coinciden con los descritos en <what-built></done>
  <what-built>
- Selector de equipo/módulo del AddItemModal solo visible para tipo 'contrato'
- PDF estándar pulido (bloque de validez prominente, header huérfano removido, titleIcon cleanup)
- PresupuestoNew setea tipo:'servicio' explícito
- Los 3 paths de creación (desde ticket, standalone, list button) producen presupuestos tipo 'servicio' correctos
  </what-built>
  <how-to-verify>
Test end-to-end completo del flow `'servicio'` (tiempo estimado: 8-10 min):

1. **Setup:** `pnpm dev:modular` con un cliente de prueba que tenga al menos 1 sistema y 1 contacto. Al menos 1 `ConceptoServicio` activo en `/presupuestos/conceptos-servicio`.

2. **Path A — Crear desde ticket:**
   - Ir a `/leads` → abrir o crear un ticket con clienteId
   - Click "Crear presupuesto" → verificar que el modal abre con:
     - Tipo por default = "Servicio"
     - Cliente prellenado
     - Contacto prellenado (si el ticket tiene contacto)
   - Agregar 1 ítem desde el catálogo: seleccionar del dropdown "Catalogo de servicios" → se autopopulan descripción + precio desde `valorBase * factorActualizacion`
   - Editar el precio unitario manualmente (+10%) — verificar que el subtotal se recalcula
   - Guardar → verificar redirect a `/presupuestos` y badge "Servicio" visible

3. **Path B — Crear standalone:**
   - `/presupuestos/nuevo` → seleccionar cliente + sistema + contacto → "Crear Presupuesto"
   - Se abre `EditPresupuestoModal` → verificar Tipo="Servicio" en el metadata strip
   - Click "+ Agregar" ítem → **verificar que el panel azul "Vincular a equipo" NO aparece** (key check de FINDING-1)
   - Agregar ítem manual con descripción + cantidad + precio → guardar
   - Abrir el presupuesto de nuevo → confirmar que se persistió

4. **Path C — Crear desde list:**
   - `/presupuestos` → "+ Nuevo Presupuesto" → cliente + Tipo="Servicio" + 1 ítem del catálogo + condiciones comerciales texto libre → "Crear presupuesto"

5. **PDF check (todos los presupuestos creados):**
   - Abrir presupuesto → "Descargar PDF"
   - Verificar Page 1:
     - Header AGS con título "PRESUPUESTO" (sin `X` placeholder raro)
     - Cliente + dirección + contacto
     - Tabla de ítems con números correctos
     - Totales (por moneda si MIXTA)
     - **Bloque "OFERTA VÁLIDA POR N DÍAS" prominente con borde teal**
     - **SIN** header suelto "NOTAS TÉCNICAS:" al final de Page 1
   - Page 2: condiciones comerciales (si se agregaron) + firma
   - Page 3: bloque de conformidad + firma detallada

6. **Contrato check (regression):**
   - `/presupuestos` → crear tipo="Contrato" → "+ Agregar" ítem → **verificar que el panel "Vincular a equipo" SÍ aparece** (el gate funciona en ambas direcciones)
   - Guardar y descargar PDF → verificar que el `PresupuestoPDFContrato` se usa (no el estándar)

7. **List filter:**
   - `/presupuestos` → filtro "Tipo" → seleccionar "Servicio" → verificar que solo presupuestos tipo `'servicio'` aparecen
  </how-to-verify>
  <resume-signal>
Type "approved" si el flow completo funciona. Si encontrás issues, describilos (ej: "PDF no muestra bloque validez" o "Path A prefill no funciona para contacto") y el plan se revisará en una iteración de revision mode.
  </resume-signal>
</task>

</tasks>

<verification>
**Phase-level checks (correr después del checkpoint):**
- `pnpm --filter @ags/sistema-modular type-check` → sin errores
- `pnpm --filter @ags/sistema-modular build` → build completo exitoso
- `pnpm lint:ast` → no nuevas violaciones de `no-firestore-undefined` (no tocamos writes en este plan, debería quedar igual)
- Line counts post-edit:
  - `AddItemModal.tsx` < 250 LOC
  - `PresupuestoItemsTable.tsx` < 250 LOC
  - `PresupuestoPDFEstandar.tsx` — sin límite estricto (es PDF template, pero evitar explosión de LOC)
</verification>

<success_criteria>
**PTYP-01 (cerrar flow 'servicio' end-to-end):**
- [x] Tipo `'servicio'` renderiza tabla flat (`PresupuestoItemsTable`), no jerárquica
- [x] `AddItemModal` NO muestra panel de equipo cuando `tipo !== 'contrato'`
- [x] Los 3 paths de creación (desde ticket, standalone, list button) producen documentos con `tipo: 'servicio'`
- [x] Selector de `ConceptoServicio` (catálogo) prellenrea descripción + precio del ítem
- [x] Precios editables manualmente (decisión "100% manual" de CONTEXT)
- [x] Soporte MIXTA no se rompe
- [x] Filtro de tipo en `PresupuestosList` funciona

**FMT-01 (PDF Editorial Teal para 'servicio'):**
- [x] `PresupuestoPDFEstandar` se genera para tipo `'servicio'` (dispatcher en `generatePresupuestoPDF.tsx:122-125`)
- [x] Header con logo + razón social + CUIT cliente (ya funcionaba)
- [x] Tabla de ítems con descripción/cant/unidad/precio/descuento/subtotal
- [x] Totales por moneda con soporte MIXTA
- [x] Bloque "Oferta válida por N días" **prominente** (polish visual)
- [x] Sin header "NOTAS TÉCNICAS" huérfano (cleanup)
- [x] Condiciones comerciales en Page 2
- [x] Numeración `PRE-XXXX.NN` visible en header
- [x] Render OK en Electron + navegador (react-pdf es agnóstico del host)

**Documented NOT addressed in this plan (por scope / CONTEXT deferred):**
- Token-first order en envío por mail → **Plan 07-02**
- Snapshot de precio / tipoCambio / estado `oc_recibida` → descartado (CONTEXT)
- Plantillas rich-text para condiciones → Phase 3 diferida
- Auto-ticket / auto-OT desde `'servicio'` → Phase 8
</success_criteria>

<output>
After completion, create `.planning/phases/07-presupuesto-per-incident/07-01-SUMMARY.md` with:
- Files modified (+ line counts pre/post)
- Audit findings closed (FINDING-1 through FINDING-5)
- Audit findings NOT addressed (FINDING-6, FINDING-7 — verify-only, sin código)
- Pending for plan 07-02: token-first order, resto de polish PDF si queda
- Screenshots/notes del checkpoint (opcional pero útil)
</output>
