---
phase: 04-presupuestos-anexo-consumibles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/index.ts
autonomous: true
requirements: [ANXC-01]
must_haves:
  truths:
    - "Un servicio de plantilla puede declarar requiereAnexoConsumibles=true sin romper plantillas existentes"
    - "Un documento ConsumiblesPorModulo modela un código de módulo con su lista de consumibles informativos"
    - "Los tipos compilan en strict mode y no introducen undefined obligatorio (Firestore-safe)"
  artifacts:
    - path: "packages/shared/src/types/index.ts"
      provides: "TipoEquipoServicio.requiereAnexoConsumibles + ConsumibleModulo + ConsumiblesPorModulo interfaces exportados"
      contains: "requiereAnexoConsumibles"
  key_links:
    - from: "TipoEquipoServicio.requiereAnexoConsumibles"
      to: "tiposEquipoService writes"
      via: "default false (omitido) si no se setea — back-compat con plantillas existentes"
      pattern: "requiereAnexoConsumibles\\?: boolean"
    - from: "ConsumiblesPorModulo"
      to: "consumiblesPorModuloService (plan 04-02)"
      via: "doc shape consumido por CRUD service y por builder de anexo"
      pattern: "interface ConsumiblesPorModulo"
---

<objective>
Extender `@ags/shared` con los tipos foundation que el resto de la fase consume:

1. Flag opcional `requiereAnexoConsumibles?: boolean` en `TipoEquipoServicio` (default omitido = false). Schema-flexible — se puede tildar en cualquier `tipo`.
2. `ConsumibleModulo` interface (codigo / descripcion / cantidad) — fila individual del listado.
3. `ConsumiblesPorModulo` interface (doc shape de la nueva colección Firestore `consumibles_por_modulo`): `codigoModulo` único, `descripcion?`, `consumibles[]`, `activo`, audit fields nullables.

Purpose: Sin estos tipos no compila ningún plan de Wave 2 (service, admin CRUD, builder de anexos, PDF template). Es el cuello de botella foundational.

Output: `packages/shared/src/types/index.ts` extendido con 3 cambios. `pnpm type-check` pasa. Plantillas existentes (`HPLC 1100`, `1200`, etc.) en Firestore siguen siendo válidas — el flag es opcional y se trata como `false` cuando está ausente.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-presupuestos-anexo-consumibles/04-CONTEXT.md
@packages/shared/src/types/index.ts
@CLAUDE.md
@.claude/rules/firestore.md

<interfaces>
<!-- Tipos existentes relevantes — tomados directamente del repo. Usarlos como referencia exacta. -->

From packages/shared/src/types/index.ts (lines 1354-1395):
```typescript
export type TipoServicioPlantilla = 'mantenimiento' | 'regulatorio' | 'consumible' | 'otro';

export interface TipoEquipoComponente {
  id: string;
  orden: number;
  codigo: string;                   // Part number Agilent (ej: "G1322A")
  descripcion: string;
  servicioCode?: string | null;
}

export interface TipoEquipoServicio {
  id: string;
  orden: number;
  servicioCode: string;
  descripcion: string;
  cantidadDefault: number;
  tipo: TipoServicioPlantilla;
  precioDefault?: number | null;
  // ⬅ requiereAnexoConsumibles?: boolean — agregar acá
}

export interface TipoEquipoPlantilla {
  id: string;
  nombre: string;
  descripcion?: string | null;
  activo: boolean;
  componentes: TipoEquipoComponente[];
  servicios: TipoEquipoServicio[];
  createdAt: string;
  updatedAt: string;
  createdBy?: string | null;
  updatedBy?: string | null;
}
```

Audit-field convention used elsewhere in this file (`PlantillaTextoPresupuesto`, `CondicionPago`, `Articulo`):
```typescript
createdAt: string;
updatedAt: string;
createdBy?: string | null;     // null, no undefined (regla Firestore)
createdByName?: string | null;
updatedBy?: string | null;
updatedByName?: string | null;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extender TipoEquipoServicio con flag requiereAnexoConsumibles</name>
  <files>packages/shared/src/types/index.ts</files>
  <behavior>
    - El flag es opcional (`?:`) — plantillas existentes (sin el campo) siguen siendo válidas.
    - Schema-flexible: cualquier `tipo` (`'mantenimiento' | 'regulatorio' | 'consumible' | 'otro'`) puede llevarlo. NO restringir por tipo.
    - JSDoc explica que dispara generación de anexo PDF al cotizar el servicio.
    - El campo es `boolean` (no string union, no enum) — es un flag binario.
  </behavior>
  <action>
    En `packages/shared/src/types/index.ts`, dentro de la interface `TipoEquipoServicio` (línea ~1366), agregar el flag:

    ```typescript
    export interface TipoEquipoServicio {
      id: string;
      orden: number;
      servicioCode: string;
      descripcion: string;
      cantidadDefault: number;
      tipo: TipoServicioPlantilla;
      precioDefault?: number | null;
      /**
       * (Phase 4 / ANXC-01) Si true, al incluir este servicio en un presupuesto se debe generar
       * un PDF anexo con el listado de consumibles por módulo del sistema seleccionado y adjuntarlo
       * al email de envío. Operacionalmente se tilda en servicios MPCC ("Mantenimiento Preventivo
       * Con Consumibles") pero el flag es ortogonal al campo `tipo` — schema-flexible.
       *
       * Default: omitido (tratar como `false`) — plantillas previas a Phase 4 NO requieren migración.
       */
      requiereAnexoConsumibles?: boolean;
    }
    ```

    NO modificar `TipoServicioPlantilla` ni `TipoEquipoComponente` — el flag vive solo en servicios.

    NO escribir migración para plantillas existentes — el flag es opcional. `tiposEquipoService.hydrate()` (en plan 04-03) leerá `data.requiereAnexoConsumibles ?? false` para soportar lecturas viejas.
  </action>
  <verify>
    <automated>cd packages/shared &amp;&amp; pnpm type-check 2>&amp;1 | grep -i "error" || echo "OK"</automated>
  </verify>
  <done>
    - `TipoEquipoServicio.requiereAnexoConsumibles` aparece en `packages/shared/src/types/index.ts` con JSDoc.
    - `pnpm type-check` desde el root pasa (no errores TS en `packages/shared` ni en consumers existentes).
    - Búsqueda `grep -n "requiereAnexoConsumibles" packages/shared/src/types/index.ts` devuelve al menos 1 línea (la prop) + JSDoc.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Agregar tipos ConsumibleModulo + ConsumiblesPorModulo</name>
  <files>packages/shared/src/types/index.ts</files>
  <behavior>
    - `ConsumibleModulo` modela una fila del listado: `{ codigo: string, descripcion: string, cantidad: number }`. SIN precio. SIN periodicidad.
    - `ConsumiblesPorModulo` modela el doc Firestore: `{ id, codigoModulo, descripcion?, consumibles[], activo, audit fields }`.
    - `codigoModulo` es la clave lógica única (ej: `"G7129A"`). Misma colección, una entrada por código de módulo, reusable entre tipos de equipo.
    - Audit fields siguen la convención del repo: `createdAt: string`, `updatedAt: string`, `createdBy/updatedBy: string | null`. NUNCA `undefined` (regla Firestore-undefined).
    - JSDoc apunta al phase context.
  </behavior>
  <action>
    En `packages/shared/src/types/index.ts`, INMEDIATAMENTE DESPUÉS del bloque de `TipoEquipoPlantilla` y `TIPO_SERVICIO_PLANTILLA_LABELS` (línea ~1395, justo antes del comment `// --- Facturación ---`), agregar el bloque:

    ```typescript
    // =============================================
    // --- Anexo de Consumibles por Módulo (Phase 4) ---
    // Catálogo declarativo: cada módulo (por part number Agilent) lleva una lista
    // informativa de consumibles. Cuando un presupuesto incluye un servicio con
    // `requiereAnexoConsumibles=true`, se matchea el código del módulo del sistema
    // contra esta colección para generar el PDF anexo.
    // =============================================

    /** Fila de un consumible en el anexo. Sin precio (queda implícito en el ítem MPCC del PDF principal). */
    export interface ConsumibleModulo {
      codigo: string;        // Part number del consumible (ej: "5061-3361")
      descripcion: string;   // "Vial 2ml ámbar con tapa"
      cantidad: number;      // Cantidad informativa por evento de mantenimiento
    }

    /**
     * Doc Firestore en la colección `consumibles_por_modulo` (snake_case).
     * Una entrada por `codigoModulo` único — reusable entre todas las plantillas de
     * tipo de equipo que incluyan ese módulo (ej: G7129A puede aparecer en HPLC 1260
     * y HPLC 1290; se declara una sola vez).
     *
     * `consumibles: []` (array vacío) significa "este módulo no lleva consumibles
     * declarados intencionalmente" → skip silencioso al generar el anexo (NO warning).
     */
    export interface ConsumiblesPorModulo {
      id: string;
      codigoModulo: string;          // Part number del módulo (ej: "G7129A"). UNIQUE en la colección.
      descripcion?: string | null;   // Descripción legible del módulo ("Inyector Iso Pump") — opcional, fallback al hidratar.
      consumibles: ConsumibleModulo[];
      activo: boolean;
      createdAt: string;
      updatedAt: string;
      createdBy?: string | null;
      createdByName?: string | null;
      updatedBy?: string | null;
      updatedByName?: string | null;
    }
    ```

    Reglas:
    - NO usar `Record<string, ...>` ni Maps — preferir arrays para mantener orden estable de consumibles.
    - NO agregar campos `precio` ni `periodicidad` (decisión de CONTEXT.md). Si en el futuro hay que extender, se puede agregar opcional sin romper consumers.
    - NO exportar un default — los consumers usan los tipos por nombre.
  </action>
  <verify>
    <automated>cd packages/shared &amp;&amp; pnpm type-check 2>&amp;1 | grep -i "error" || echo "OK"</automated>
  </verify>
  <done>
    - `ConsumibleModulo` y `ConsumiblesPorModulo` aparecen en `packages/shared/src/types/index.ts` con JSDoc.
    - `grep -n "ConsumiblesPorModulo" packages/shared/src/types/index.ts` devuelve al menos la declaración + bloque `interface`.
    - `pnpm type-check` pasa sin errores.
    - Ambos tipos exportados (no `interface ... {}` sin `export`).
  </done>
</task>

</tasks>

<verification>
**Phase-level checks tras este plan:**

```bash
# Type-check del workspace shared
cd packages/shared && pnpm type-check
# Esperado: sin errores

# Verificar que los 3 cambios están presentes
grep -n "requiereAnexoConsumibles" packages/shared/src/types/index.ts
grep -n "interface ConsumibleModulo" packages/shared/src/types/index.ts
grep -n "interface ConsumiblesPorModulo" packages/shared/src/types/index.ts

# Verificar que apps/sistema-modular sigue compilando (consumers no se rompen)
cd ../../apps/sistema-modular && pnpm type-check 2>&1 | grep -E "error TS" | head -20
# Esperado: vacío o solo errores pre-existentes no relacionados con los nuevos tipos
```
</verification>

<success_criteria>
- 3 extensiones a `packages/shared/src/types/index.ts` (1 flag + 2 interfaces) sin romper compilación.
- Plantillas existentes en Firestore (sin `requiereAnexoConsumibles`) siguen siendo válidas.
- Plans 04-02, 04-03, 04-04 quedan desbloqueados — pueden importar `ConsumiblesPorModulo`, `ConsumibleModulo`, y leer `requiereAnexoConsumibles` desde `TipoEquipoServicio`.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/04-presupuestos-anexo-consumibles/04-01-SUMMARY.md` siguiendo `.claude/get-shit-done/templates/summary.md`. Documentar:
- Líneas exactas donde quedó cada tipo (para que plans Wave 2 referencien sin buscar).
- Confirmar que `pnpm type-check` pasa.
- Cualquier decisión de naming desviada de CONTEXT.md.
</output>
