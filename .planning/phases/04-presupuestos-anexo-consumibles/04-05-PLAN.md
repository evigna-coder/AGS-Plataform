---
phase: 04-presupuestos-anexo-consumibles
plan: 05
type: execute
wave: 3
depends_on: [04-02, 04-03, 04-04]
files_modified:
  - apps/sistema-modular/src/hooks/useEnviarPresupuesto.ts
  - apps/sistema-modular/src/components/presupuestos/EnviarPresupuestoModal.tsx
  - apps/sistema-modular/src/components/presupuestos/EnviarAnexosSection.tsx
autonomous: false
requirements: [ANXC-06]
must_haves:
  truths:
    - "Cuando un presupuesto tiene items con flag, EnviarPresupuestoModal muestra checkbox 'Adjuntar anexos de consumibles' default ON"
    - "Si el operador confirma sin tildar, el mail se envía solo con el PDF principal"
    - "Si el operador confirma con tilde, el mail incluye N adjuntos (1 por item con flag), filename 'Anexo Consumibles - {numero} - {sistema}.pdf'"
    - "Botón 'Ver anexos' muestra preview de cada uno (apertura en nueva pestaña — uno por uno con dropdown si N > 1)"
    - "Caso terminal (sistema sin módulos ni plantilla) muestra banner amarillo no-bloqueante con opción de enviar igual sin anexo"
    - "Si la generación de anexos falla pero el principal está OK, el send se aborta con error claro (no se envía mail incompleto)"
    - "El estado del presupuesto NO transiciona a 'enviado' si el mail falla — token-first order preservado de Phase 7"
  artifacts:
    - path: "apps/sistema-modular/src/hooks/useEnviarPresupuesto.ts"
      provides: "Hook extendido: pre-carga catálogos, computa anexos, agrega N attachments al sendGmail"
    - path: "apps/sistema-modular/src/components/presupuestos/EnviarPresupuestoModal.tsx"
      provides: "Modal con checkbox + botón preview + banner warnings (≤250 líneas)"
    - path: "apps/sistema-modular/src/components/presupuestos/EnviarAnexosSection.tsx"
      provides: "Sub-componente: checkbox + preview button + warnings + lista de filenames"
      min_lines: 80
  key_links:
    - from: "useEnviarPresupuesto"
      to: "buildAnexosFromPresupuesto + generateAnexoConsumiblesPDF"
      via: "pre-carga catálogos en stage 'preparing_anexos' y produce blobs base64 antes del sendGmail"
      pattern: "buildAnexosFromPresupuesto"
    - from: "EnviarPresupuestoModal"
      to: "EnviarAnexosSection"
      via: "render gated por hasItemsConFlag"
      pattern: "EnviarAnexosSection"
    - from: "sendGmail attachments[]"
      to: "[principal, ...anexos]"
      via: "spread N anexos en el array (gmailService ya soporta N adjuntos)"
      pattern: "attachments: \\[.*\\.\\.\\."
---

<objective>
Cierre del feature: integrar la generación y adjunte de anexos al flujo de envío por email del presupuesto. Esta es la única plan con checkpoint de verificación humana porque el flujo final involucra OAuth + render PDF + envío real, y los warnings/banner UX necesitan visual confirmación.

Purpose: Sin esto los plans 04-01 a 04-04 son infraestructura sin output al cliente. El cliente recibe el mail con N+1 adjuntos (principal + N anexos).

Output:
1. `useEnviarPresupuesto` hook extendido con stages `preparing_anexos` y producción de N attachments.
2. `EnviarAnexosSection` sub-componente nuevo (checkbox + preview + warnings).
3. `EnviarPresupuestoModal` integra el sub-componente, gated por presencia de items con flag.
4. Token-first order de Phase 7 PRESERVADO — el estado solo transiciona a `enviado` después del send exitoso.
5. Checkpoint humano final: visual + functional smoke en un ppto real con 1 y 2 sistemas con flag.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-presupuestos-anexo-consumibles/04-CONTEXT.md
@CLAUDE.md
@.claude/rules/firestore.md
@.claude/rules/components.md

@apps/sistema-modular/src/hooks/useEnviarPresupuesto.ts
@apps/sistema-modular/src/components/presupuestos/EnviarPresupuestoModal.tsx
@apps/sistema-modular/src/services/gmailService.ts
@apps/sistema-modular/src/services/equiposService.ts
@apps/sistema-modular/src/components/presupuestos/pdf/index.ts

<interfaces>
<!-- APIs de plan 04-04 (esperar a que esté commited): -->
```typescript
// from '../components/presupuestos/pdf':
export interface AnexoConsumiblesData { /* ... */ }
export interface AnexoBuildResult { itemId: string; sistemaId: string | null; data: AnexoConsumiblesData; filename: string; }
export interface AnexoBuildWarning {
  tipo: 'modulo_sin_codigo' | 'codigo_no_en_catalogo' | 'sistema_sin_modulos_ni_plantilla';
  itemId: string; sistemaNombre: string | null; detalle: string;
}
export interface BuildAnexosInput {
  presupuesto: Presupuesto; cliente: Cliente | null;
  sistemas: Record<string, Sistema | null>;
  modulosBySistema: Record<string, ModuloSistema[]>;
  plantillas: TipoEquipoPlantilla[];
}
export async function buildAnexosFromPresupuesto(input: BuildAnexosInput):
  Promise<{ anexos: AnexoBuildResult[]; warnings: AnexoBuildWarning[] }>;
export async function generateAnexoConsumiblesPDF(data: AnexoConsumiblesData): Promise<Blob>;
```

<!-- gmailService (apps/sistema-modular/src/services/gmailService.ts línea 12): YA SOPORTA N attachments. -->
```typescript
interface EmailParams {
  accessToken: string; to: string[]; cc?: string[];
  subject: string; htmlBody: string;
  attachments?: { filename: string; mimeType: string; base64Data: string }[];
}
```

<!-- Servicios para pre-carga de catálogos: -->
```typescript
// from '../services/tiposEquipoService':
export const tiposEquipoService = { getAll(): Promise<TipoEquipoPlantilla[]>; /* ... */ };

// from '../services/equiposService':
export const sistemasService = {
  getById(id: string): Promise<Sistema | null>;
  // ...
};
export const modulosService = {
  getBySistema(sistemaId: string): Promise<ModuloSistema[]>;
};
```

<!-- Hook actual (useEnviarPresupuesto.ts) — líneas 12-19: stages -->
```typescript
export type EnviarStatus =
  | 'idle' | 'authorizing' | 'generating_pdf' | 'sending'
  | 'updating_firestore' | 'sent' | 'error';
// ⬅ AGREGAR: 'preparing_anexos' (entre generating_pdf y sending) cuando includeAnexos=true
```

<!-- Estado actual del attachments en sendGmail (useEnviarPresupuesto.ts líneas 109-113): -->
```typescript
attachments: [{
  filename: `${params.presupuestoNumero}.pdf`,
  mimeType: 'application/pdf',
  base64Data: pdfBase64,
}],
// ⬅ EXTENDER a [principal, ...anexos]
```
</interfaces>

<conventions>
- `EnviarPresupuestoModal.tsx` actual: 156 líneas. Agregar el sub-componente como import + render condicional → quedará ~180 líneas. Si crece más, extraer.
- Los nuevos archivos siguen Editorial Teal (texto teal-700 en links, banners amarillos para warnings: `bg-amber-50 text-amber-800 border-amber-200`).
- Token-first order (FMT-02): NO cambiar el orden de stages. Los anexos se preparan ENTRE `generating_pdf` y `sending` — token ya validado, PDF principal listo, y si la prep de anexos falla NO se mandó nada todavía.
- Status messages se muestran en el banner inferior del modal — agregar literal en `statusMessages`.
</conventions>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: EnviarAnexosSection (sub-componente: checkbox + preview + banner warnings)</name>
  <files>apps/sistema-modular/src/components/presupuestos/EnviarAnexosSection.tsx</files>
  <behavior>
    - Componente `<EnviarAnexosSection>` que se renderiza solo si `anexos.length > 0` o `terminalWarnings.length > 0`.
    - Inputs (props):
      - `anexos: AnexoBuildResult[]`
      - `warnings: AnexoBuildWarning[]`
      - `includeAnexos: boolean`, `onToggleIncludeAnexos: (v: boolean) => void`
      - `disabled?: boolean` (true cuando `sending`)
    - Render:
      1. Checkbox "Adjuntar anexos de consumibles" — controlado por `includeAnexos`. Default ON al montar (decisión del modal padre).
      2. Lista de filenames de los anexos a adjuntar (texto pequeño, `text-[10px] text-slate-500`):
         - "Se adjuntarán: Anexo 1 — {sistema A}, Anexo 2 — {sistema B}, ..."
      3. Botón "Ver anexos" (link teal-700) → abre el primer anexo en nueva pestaña. Si N > 1, dropdown que permite elegir cuál ver.
      4. Banners de warnings:
         - Warnings tipo `modulo_sin_codigo` o `codigo_no_en_catalogo` → banner amarillo informativo "Algunos módulos del Item X aparecen como 'sin consumibles especificados' por falta de catálogo. ¿Adjuntar igual?" (NO bloqueante).
         - Warnings tipo `sistema_sin_modulos_ni_plantilla` → banner amarillo más prominente "El item Y lleva flag de anexo pero no se encontraron módulos ni plantilla matcheable. Se enviará sin anexo para ese item." (NO bloqueante).
    - Si `includeAnexos === false`, ocultar lista de filenames y ocultar el botón preview (pero NO los warnings — siguen visibles).
  </behavior>
  <action>
    Crear `apps/sistema-modular/src/components/presupuestos/EnviarAnexosSection.tsx` (~120 líneas).

    1. Imports:
    ```typescript
    import React, { useState, useMemo } from 'react';
    import type { AnexoBuildResult, AnexoBuildWarning } from './pdf';
    import { generateAnexoConsumiblesPDF } from './pdf';
    ```

    2. Props interface:
    ```typescript
    interface Props {
      anexos: AnexoBuildResult[];
      warnings: AnexoBuildWarning[];
      includeAnexos: boolean;
      onToggleIncludeAnexos: (v: boolean) => void;
      disabled?: boolean;
    }
    ```

    3. Estado interno:
    ```typescript
    const [previewIdx, setPreviewIdx] = useState(0);
    const [previewing, setPreviewing] = useState(false);
    ```

    4. Handler de preview:
    ```typescript
    const handlePreview = async () => {
      if (anexos.length === 0) return;
      const target = anexos[previewIdx];
      if (!target) return;
      try {
        setPreviewing(true);
        const blob = await generateAnexoConsumiblesPDF(target.data);
        const url = URL.createObjectURL(blob);
        const win = window.open(url, '_blank');
        if (!win) {
          // popup bloqueado — fallback a download
          const a = document.createElement('a');
          a.href = url; a.download = target.filename;
          document.body.appendChild(a); a.click(); document.body.removeChild(a);
        }
        setTimeout(() => URL.revokeObjectURL(url), 30000);
      } catch (err) {
        console.error('Error generando preview anexo:', err);
        alert('No se pudo generar el preview del anexo');
      } finally {
        setPreviewing(false);
      }
    };
    ```

    5. Separar warnings por tipo:
    ```typescript
    const { softWarnings, terminalWarnings } = useMemo(() => {
      const soft: AnexoBuildWarning[] = [];
      const terminal: AnexoBuildWarning[] = [];
      for (const w of warnings) {
        if (w.tipo === 'sistema_sin_modulos_ni_plantilla') terminal.push(w);
        else soft.push(w);
      }
      return { softWarnings: soft, terminalWarnings: terminal };
    }, [warnings]);
    ```

    6. Render. Estructura sugerida:
    ```jsx
    if (anexos.length === 0 && warnings.length === 0) return null;
    return (
      <div className="border border-amber-100 bg-amber-50/30 rounded-lg p-3 space-y-2">
        <div className="flex items-start justify-between gap-3">
          <label className="flex items-center gap-2 cursor-pointer">
            <input type="checkbox" disabled={disabled || anexos.length === 0}
              checked={includeAnexos} onChange={e => onToggleIncludeAnexos(e.target.checked)}
              className="w-3.5 h-3.5 text-teal-700 rounded" />
            <span className="text-xs font-medium text-slate-700">
              Adjuntar anexos de consumibles ({anexos.length})
            </span>
          </label>
          {includeAnexos && anexos.length > 0 && (
            <div className="flex items-center gap-2">
              {anexos.length > 1 && (
                <select className="text-[10px] border border-slate-200 rounded px-1.5 py-0.5"
                  value={previewIdx} onChange={e => setPreviewIdx(parseInt(e.target.value))}
                  disabled={previewing || disabled}>
                  {anexos.map((a, i) => (
                    <option key={i} value={i}>Anexo {i + 1} — {a.data.sistemaNombre}</option>
                  ))}
                </select>
              )}
              <button type="button" onClick={handlePreview} disabled={previewing || disabled}
                className="text-[11px] font-medium text-teal-700 hover:text-teal-900">
                {previewing ? 'Generando…' : 'Ver anexo'}
              </button>
            </div>
          )}
        </div>

        {includeAnexos && anexos.length > 0 && (
          <ul className="text-[10px] text-slate-500 space-y-0.5 ml-5">
            {anexos.map((a, i) => <li key={i}>• {a.filename}</li>)}
          </ul>
        )}

        {terminalWarnings.length > 0 && (
          <div className="rounded bg-amber-100 border border-amber-300 p-2 text-[11px] text-amber-900">
            <p className="font-semibold mb-1">Atención: {terminalWarnings.length} item(s) sin anexo posible</p>
            <ul className="space-y-0.5">
              {terminalWarnings.map((w, i) => <li key={i}>• {w.detalle}</li>)}
            </ul>
            <p className="mt-1.5 italic">El email se enviará sin anexo para esos items.</p>
          </div>
        )}

        {softWarnings.length > 0 && (
          <details className="text-[10px] text-amber-800">
            <summary className="cursor-pointer hover:underline">
              {softWarnings.length} aviso(s) sobre módulos sin consumibles en catálogo
            </summary>
            <ul className="mt-1 ml-3 space-y-0.5">
              {softWarnings.map((w, i) => <li key={i}>• {w.detalle}</li>)}
            </ul>
          </details>
        )}
      </div>
    );
    ```

    NO incluir lógica de carga de catálogos — eso vive en el hook (Task 2).

    Mantener el archivo ≤ 250 líneas (regla components.md). Apuntar a ~120.
  </action>
  <verify>
    <automated>cd apps/sistema-modular &amp;&amp; pnpm type-check 2>&amp;1 | grep -E "error TS" | grep "EnviarAnexosSection" | head -5 || echo "OK"</automated>
  </verify>
  <done>
    - `apps/sistema-modular/src/components/presupuestos/EnviarAnexosSection.tsx` existe.
    - Props/estado/render según behavior.
    - ≤ 250 líneas.
    - `pnpm type-check` pasa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: useEnviarPresupuesto: pre-carga catálogos + stage preparing_anexos + N attachments</name>
  <files>apps/sistema-modular/src/hooks/useEnviarPresupuesto.ts</files>
  <behavior>
    - Hook expone NUEVO sub-API:
      - `loadAnexos()`: pre-carga catálogos (`tiposEquipoService.getAll`, `sistemasService.getById` para cada `sistemaId` único, `modulosService.getBySistema` para cada uno) y llama `buildAnexosFromPresupuesto(...)`. Devuelve `{ anexos, warnings }`. Llamarse cuando el modal abre.
      - `send(opts)` extendido: recibe `includeAnexos: boolean`. Si `true` y hay anexos disponibles, los genera y los pasa como N attachments adicionales.
    - Stage machine: agregar `'preparing_anexos'` entre `'generating_pdf'` y `'sending'`. Solo se setea si `includeAnexos === true`.
    - Token-first order PRESERVADO: prep de anexos OCURRE DESPUÉS del PDF principal (que ya genera token validado). Si prep de anexos falla, abortar antes de `sendGmail` y retornar error claro — el ppto NO transiciona a `enviado`.
    - PDF principal: `attachments: [{ filename: numeroPresupuesto.pdf, ... principal }, ...anexosBase64]`.
    - El cliente y el presupuesto necesarios para `buildAnexosFromPresupuesto` ya viven en `params.pdfParams` (`PresupuestoPDFData` shape). Re-usar.
  </behavior>
  <action>
    Modificar `apps/sistema-modular/src/hooks/useEnviarPresupuesto.ts`. Cambios incrementales:

    1. **Tipos** — agregar `'preparing_anexos'` a `EnviarStatus`:
    ```typescript
    export type EnviarStatus =
      | 'idle' | 'authorizing' | 'generating_pdf'
      | 'preparing_anexos'        // NEW
      | 'sending' | 'updating_firestore' | 'sent' | 'error';
    ```

    2. **Imports** — al tope agregar:
    ```typescript
    import {
      buildAnexosFromPresupuesto,
      generateAnexoConsumiblesPDF,
      type AnexoBuildResult,
      type AnexoBuildWarning,
    } from '../components/presupuestos/pdf';
    import { tiposEquipoService } from '../services/tiposEquipoService';
    import { sistemasService, modulosService } from '../services/equiposService';
    import type { Sistema, ModuloSistema } from '@ags/shared';
    ```

    3. **State adicional** dentro del hook:
    ```typescript
    const [anexos, setAnexos] = useState<AnexoBuildResult[]>([]);
    const [anexoWarnings, setAnexoWarnings] = useState<AnexoBuildWarning[]>([]);
    const [anexosLoading, setAnexosLoading] = useState(false);
    ```

    4. **Nueva función exportada** `loadAnexos()`:
    ```typescript
    const loadAnexos = async (): Promise<void> => {
      setAnexosLoading(true);
      try {
        const presupuesto = params.pdfParams.presupuesto;
        const cliente = params.pdfParams.cliente;

        // Pre-carga catálogos en paralelo
        const sistemaIds = [...new Set(presupuesto.items.map(i => i.sistemaId).filter(Boolean))] as string[];
        const [plantillas, sistemasArr, modulosArr] = await Promise.all([
          tiposEquipoService.getAll(),
          Promise.all(sistemaIds.map(sid => sistemasService.getById(sid).catch(() => null))),
          Promise.all(sistemaIds.map(sid => modulosService.getBySistema(sid).catch(() => [] as ModuloSistema[]))),
        ]);

        const sistemas: Record<string, Sistema | null> = {};
        const modulosBySistema: Record<string, ModuloSistema[]> = {};
        sistemaIds.forEach((sid, idx) => {
          sistemas[sid] = sistemasArr[idx];
          modulosBySistema[sid] = modulosArr[idx];
        });

        const { anexos, warnings } = await buildAnexosFromPresupuesto({
          presupuesto, cliente, sistemas, modulosBySistema, plantillas,
        });
        setAnexos(anexos);
        setAnexoWarnings(warnings);
      } catch (err) {
        console.error('Error pre-cargando anexos:', err);
        setAnexos([]);
        setAnexoWarnings([{
          tipo: 'sistema_sin_modulos_ni_plantilla',
          itemId: '_',
          sistemaNombre: null,
          detalle: 'No se pudo cargar el catálogo de consumibles. Se enviará sin anexos.',
        }]);
      } finally {
        setAnexosLoading(false);
      }
    };
    ```

    5. **Modificar `send(opts)`** — extender la firma:
    ```typescript
    interface SendOpts {
      to: string[]; cc?: string[];
      subject: string; htmlBody: string;
      includeAnexos?: boolean;   // NEW — default false
    }
    ```

    Después del Stage 2 (Generate PDF principal) y ANTES del Stage 3 (Send Gmail), insertar:
    ```typescript
    // STAGE 2.5: Generate anexos PDFs (only if requested + available)
    let anexoAttachments: { filename: string; mimeType: string; base64Data: string }[] = [];
    if (opts.includeAnexos && anexos.length > 0) {
      try {
        setStatus('preparing_anexos');
        const blobs = await Promise.all(
          anexos.map(async (a) => {
            const blob = await generateAnexoConsumiblesPDF(a.data);
            const base64 = await blobToBase64(blob);
            return { filename: a.filename, mimeType: 'application/pdf', base64Data: base64 };
          })
        );
        anexoAttachments = blobs;
      } catch (err) {
        const msg = err instanceof Error ? err.message : 'Error desconocido';
        setError(`No se pudieron generar los anexos: ${msg}. El estado NO cambió — podés reintentar.`);
        setStatus('error');
        setSending(false);
        return;
      }
    }
    ```

    Modificar el `sendGmail` call (Stage 3, líneas 102-114) para spread de attachments:
    ```typescript
    await sendGmail({
      accessToken,
      to: opts.to,
      cc: opts.cc && opts.cc.length > 0 ? opts.cc : undefined,
      subject: opts.subject,
      htmlBody: opts.htmlBody,
      attachments: [
        { filename: `${params.presupuestoNumero}.pdf`, mimeType: 'application/pdf', base64Data: pdfBase64 },
        ...anexoAttachments,
      ],
    });
    ```

    6. **Return** del hook — exponer los nuevos:
    ```typescript
    return {
      send, status, error, sending,
      anexos, anexoWarnings, anexosLoading, loadAnexos,   // NEW
    };
    ```

    7. NO modificar Stage 4 (`updating_firestore`) ni Stage 5 (success) — el token-first order de Phase 7 sigue siendo el contrato.

    8. NO instalar deps — `Promise.all`, `tiposEquipoService`, etc. ya disponibles.

    Tamaño objetivo: ~250 líneas (vs 169 actuales). Bordeando el budget. La regla 250-líneas estricta aplica a `.tsx`, y este archivo es un hook (`.ts`) de orquestación, así que el budget es ergonómico, no invariante.

    **MANDATORY**: Si tras los cambios el archivo final supera **280 líneas**, extraer `loadAnexos` + sus tipos a un nuevo archivo `apps/sistema-modular/src/hooks/useEnviarAnexos.ts` (NO diferir a "iteración futura"). El split debe ocurrir EN ESTE PLAN, no después. Razón: el state machine de envío es la pieza load-bearing del flujo Phase 7 + Phase 4; un hook de >280 líneas con dos máquinas de estado entrelazadas (token-first OAuth + anexo prep) cruza el umbral donde el archivo deja de cargarse en una pasada de revisión.

    Sugerencia de split si se dispara:
    - `useEnviarAnexos.ts` exporta `{ anexos, anexoWarnings, anexosLoading, loadAnexos }` y consume `params.pdfParams`.
    - `useEnviarPresupuesto.ts` lo llama internamente y reexporta sus valores en su return — la API pública del hook NO cambia, los consumers (`EnviarPresupuestoModal`) siguen igual.
    - El stage `'preparing_anexos'` y la generación de attachments quedan en `useEnviarPresupuesto` (porque comparten el state machine con `sending`).

    Verificar al final: `wc -l apps/sistema-modular/src/hooks/useEnviarPresupuesto.ts` ≤ 280. Si se hizo el split, también `wc -l apps/sistema-modular/src/hooks/useEnviarAnexos.ts` ≤ 250.
  </action>
  <verify>
    <automated>cd apps/sistema-modular &amp;&amp; pnpm type-check 2>&amp;1 | grep -E "error TS" | grep "useEnviarPresupuesto" | head -10 || echo "OK"</automated>
  </verify>
  <done>
    - `useEnviarPresupuesto` exporta `anexos`, `anexoWarnings`, `anexosLoading`, `loadAnexos`.
    - `EnviarStatus` incluye `'preparing_anexos'`.
    - `send(opts)` recibe `includeAnexos?: boolean`.
    - Stage 2.5 prepara N PDF blobs y los pasa como attachments adicionales.
    - El estado del ppto NO transiciona si la prep de anexos falla.
    - **`wc -l apps/sistema-modular/src/hooks/useEnviarPresupuesto.ts` devuelve ≤ 280.** Si supera 280, el plan NO se considera cerrado: extraer `loadAnexos` a `apps/sistema-modular/src/hooks/useEnviarAnexos.ts` antes de marcar la task como done (mandatory split, no diferible).
    - Si se hizo el split: `wc -l apps/sistema-modular/src/hooks/useEnviarAnexos.ts` ≤ 250 y `EnviarPresupuestoModal` no necesita cambios extra (la API pública del hook orquestador permanece igual).
    - `pnpm type-check` pasa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: EnviarPresupuestoModal: integrar EnviarAnexosSection + status messages</name>
  <files>apps/sistema-modular/src/components/presupuestos/EnviarPresupuestoModal.tsx</files>
  <behavior>
    - Al abrir el modal (`open === true`), llamar `loadAnexos()` para pre-cargar (silencioso, indicador "Cargando anexos…" si visible).
    - Si `anexos.length > 0` o `anexoWarnings.length > 0`, renderizar `<EnviarAnexosSection>` arriba del banner del PDF principal (`Se adjuntará automáticamente: {numero}.pdf`).
    - Estado local `includeAnexos` default `true` cuando hay anexos disponibles. Permitir override.
    - El botón "Enviar email" llama `send({ to, cc, subject, htmlBody, includeAnexos })`.
    - Status message para `preparing_anexos`: "Generando anexos de consumibles…".
    - Mantener el modal ≤ 250 líneas. Si cruza, extraer el form interno a un sub-hook (no necesario en este momento — apuntar a ~190 líneas).
  </behavior>
  <action>
    Modificar `apps/sistema-modular/src/components/presupuestos/EnviarPresupuestoModal.tsx`:

    1. **Imports** — agregar:
    ```typescript
    import { EnviarAnexosSection } from './EnviarAnexosSection';
    ```

    2. **Estado adicional** dentro del componente (después de `body`):
    ```typescript
    const [includeAnexos, setIncludeAnexos] = useState(true);
    ```

    3. **Destructuring del hook** — extender:
    ```typescript
    const { send, status, error, sending, anexos, anexoWarnings, anexosLoading, loadAnexos } = useEnviarPresupuesto({ ... });
    ```

    4. **useEffect de pre-carga** — al abrir modal:
    ```typescript
    useEffect(() => {
      if (!open) return;
      // Reset values + cargar anexos
      setTo(defaultTo);
      setCc('');
      setSubject(`Presupuesto ${presupuestoNumero} — AGS Analítica`);
      setBody(buildDefaultBody(defaultContactoNombre || 'cliente', presupuestoNumero));
      setIncludeAnexos(true);  // default ON
      loadAnexos();  // fire-and-forget
    }, [open, defaultTo, defaultContactoNombre, presupuestoNumero]);
    ```

    NOTA: `loadAnexos` debe estar memoizada en el hook con `useCallback` o en su defecto incluir explicitamente sus deps en el effect. Si TypeScript se queja del exhaustive-deps lint, agregar `// eslint-disable-next-line react-hooks/exhaustive-deps` con justificación: "loadAnexos viene del hook y se reinventa cada render; firing solo en open=true".

    5. **handleSend** — pasar el flag:
    ```typescript
    const handleSend = () => {
      if (!to.trim()) { alert('Ingrese al menos un destinatario'); return; }
      const toList = to.split(',').map(e => e.trim()).filter(Boolean);
      const ccList = cc ? cc.split(',').map(e => e.trim()).filter(Boolean) : [];
      send({ to: toList, cc: ccList, subject, htmlBody: body, includeAnexos });
    };
    ```

    6. **statusMessages** — agregar mensaje:
    ```typescript
    const statusMessages: Record<string, string> = {
      authorizing: 'Autorizando con Google...',
      generating_pdf: 'Generando PDF...',
      preparing_anexos: 'Generando anexos de consumibles...',   // NEW
      sending: 'Enviando email...',
      updating_firestore: 'Actualizando estado...',
      sent: 'Email enviado correctamente',
    };
    ```

    7. **Render** — insertar `<EnviarAnexosSection>` ENTRE el field "Mensaje" (textarea) y el banner "Se adjuntará automáticamente":
    ```jsx
    <EnviarAnexosSection
      anexos={anexos}
      warnings={anexoWarnings}
      includeAnexos={includeAnexos}
      onToggleIncludeAnexos={setIncludeAnexos}
      disabled={sending}
    />

    {anexosLoading && (
      <div className="text-[10px] text-slate-400 italic">Cargando anexos de consumibles…</div>
    )}
    ```

    8. NO mover ni renombrar el banner existente del PDF principal. Quedar arriba del status banner.

    Tamaño objetivo: ~195 líneas. Si pasa de 250, extraer el form a `EnviarPresupuestoForm.tsx`.
  </action>
  <verify>
    <automated>cd apps/sistema-modular &amp;&amp; pnpm type-check 2>&amp;1 | grep -E "error TS" | grep "EnviarPresupuesto" | head -10 || echo "OK"</automated>
  </verify>
  <done>
    - `EnviarPresupuestoModal` importa y renderiza `EnviarAnexosSection`.
    - Llama `loadAnexos()` en el useEffect de open.
    - `includeAnexos` se pasa a `send(...)`.
    - Status `preparing_anexos` mapeado a string visible.
    - ≤ 250 líneas.
    - `pnpm type-check` pasa.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Smoke E2E del flujo completo</name>
  <what-built>
    Flujo completo end-to-end:
    1. Tildar `requiereAnexoConsumibles` en un servicio de plantilla.
    2. Crear (o usar existente) un presupuesto con un sistema cuyo módulo (real o por plantilla) está en `consumibles_por_modulo`.
    3. Abrir `EnviarPresupuestoModal` y verificar UX completa.
    4. Enviar el mail real al usuario y verificar que llegan los adjuntos correctos.
  </what-built>
  <how-to-verify>
    **Pre-requisitos** (datos en Firestore — cargar via UI antes del smoke):
    - Cargar al menos 2 entradas en `consumibles_por_modulo` desde `/presupuestos/consumibles-por-modulo`:
      - Código `G7129A`, descripción "Inyector Iso Pump", consumibles: 2 entries (ej `5067-4256` "Sello rotor", cant 1; `5063-6589` "Frit columna", cant 2).
      - Código `G1322A` (en plantilla HPLC 1100), consumibles: 1 entry (ej `5042-9924` "Filtro inline", cant 4).
    - En `/presupuestos/tipos-equipo`, editar plantilla "HPLC 1100" → tildar "Anexo" en el servicio "MP1_CN_11B - Mantenimiento Preventivo - HPLC 1100 Con ALS" → guardar.
    - Asegurarse de tener un presupuesto con un item que use el `servicioCode` `MP1_CN_11B` y tenga `sistemaId` ligado a un sistema de tipo HPLC 1100 (real o por plantilla matcheable).

    **Pasos del smoke:**
    1. Abrir el presupuesto → click "Enviar por email".
    2. **Modal abre** → indicador "Cargando anexos…" aparece brevemente.
    3. **`EnviarAnexosSection` aparece** con:
       - Checkbox tildado "Adjuntar anexos de consumibles (1)".
       - Lista: "Anexo Consumibles - {numero} - HPLC 1100.pdf".
       - Botón "Ver anexo" → click abre el PDF en nueva pestaña.
    4. **Visual del PDF anexo**:
       - Header con logo + "Anexo de Consumibles" + número + sistema + cliente + fecha.
       - Sección "G1322A — Filtro X" con tabla de 1 fila (`5042-9924` / "Filtro inline" / 4).
       - Si hay otros módulos sin código → header del módulo + texto italic placeholder.
    5. **Tildar email** propio (`evigna@agsanalitica.com`) en "Para" → click "Enviar email".
    6. **Status flow visible**: "Autorizando..." → "Generando PDF..." → "Generando anexos de consumibles..." → "Enviando email..." → "Email enviado correctamente".
    7. **Inbox**: el mail llega con 2 adjuntos:
       - `{numero}.pdf` (principal)
       - `Anexo Consumibles - {numero} - HPLC 1100.pdf`
    8. **Estado en Firestore**: el ppto pasó de `borrador` a `enviado` (si era el primer envío). Verificar en `/presupuestos/{id}`.
    9. **Caso warning** (opcional, si hay datos): tildar el flag en un servicio cuyo sistema NO tenga ni módulos reales ni plantilla matcheable → reabrir modal → debería mostrar banner amarillo "Atención: 1 item(s) sin anexo posible" con el detalle, NO bloqueante.
    10. **Caso desactivar**: destildar el checkbox antes de enviar → el mail llega solo con el PDF principal.

    **Resultado esperado:** Todos los pasos pasan sin errores. PDF anexo se ve limpio (Editorial Teal liviano, no agresivo). Status messages claros. Mail correcto en inbox.
  </how-to-verify>
  <files>apps/sistema-modular/src/components/presupuestos/EnviarPresupuestoModal.tsx, apps/sistema-modular/src/hooks/useEnviarPresupuesto.ts</files>
  <action>Ejecutar el smoke E2E descrito en how-to-verify. NO se modifica codigo en esta tarea — es validacion humana del trabajo de Tasks 1-3. Si algo falla, levantar issue y volver a Wave 3 con un gap-closure plan; NO confirmar approved.</action>
  <verify>El usuario ejecuta los 10 pasos de how-to-verify y confirma que el mail llega al inbox con principal + N anexos, banners de warning son visibles, status flow es correcto, y el estado del presupuesto transiciona solo cuando el send es exitoso.</verify>
  <done>El usuario responde "approved" al resume-signal. Si responde con issues, esta tarea queda bloqueada hasta que un gap-closure los resuelva.</done>
  <resume-signal>Type "approved" si todo OK, o describe issues encontrados (rendering, mail no llega, status incorrecto, etc.) — el siguiente paso será iterar en gap-closure.</resume-signal>
</task>

</tasks>

<verification>
**Phase-level checks:**

```bash
# Type-check
cd apps/sistema-modular && pnpm type-check
# Esperado: sin errores

# Verificar archivos
ls apps/sistema-modular/src/components/presupuestos/EnviarAnexosSection.tsx
grep -n "preparing_anexos\|loadAnexos\|includeAnexos" apps/sistema-modular/src/hooks/useEnviarPresupuesto.ts
grep -n "EnviarAnexosSection\|loadAnexos" apps/sistema-modular/src/components/presupuestos/EnviarPresupuestoModal.tsx

# Sizes — strict thresholds
wc -l apps/sistema-modular/src/components/presupuestos/EnviarAnexosSection.tsx       # ≤ 250 (regla components.md)
wc -l apps/sistema-modular/src/components/presupuestos/EnviarPresupuestoModal.tsx    # ≤ 250 (regla components.md)
wc -l apps/sistema-modular/src/hooks/useEnviarPresupuesto.ts                          # ≤ 280 (mandatory split si excede — Task 2 done block)
[ -f apps/sistema-modular/src/hooks/useEnviarAnexos.ts ] && wc -l apps/sistema-modular/src/hooks/useEnviarAnexos.ts  # ≤ 250 si existe

# El smoke checkpoint cubre el end-to-end. No hay test automatizado E2E para email real
# (gmail OAuth + render PDF + mail send es out-of-scope para Phase 4 — Phase 11 lo cubrirá con mocks).
```
</verification>

<success_criteria>
- Modal de envío gestiona el toggle "Adjuntar anexos" con default ON.
- Mail enviado tiene PDF principal + N anexos cuando se confirma con tilde.
- Token-first order preservado: prep de anexos no transiciona estado si falla.
- Casos terminales muestran banner amarillo no-bloqueante.
- Phase 4 cierra el ciclo: `Plantilla.requiereAnexoConsumibles=true` + sistema con módulo matcheable → cliente recibe el anexo en su inbox.
</success_criteria>

<output>
Tras completar (incluyendo aprobación del checkpoint), crear `.planning/phases/04-presupuestos-anexo-consumibles/04-05-SUMMARY.md` documentando:
- Resultado del smoke (qué se probó exactamente, qué adjuntos llegaron al inbox).
- Cualquier ajuste hecho durante el smoke (errores corregidos, polish UI).
- Limitaciones conocidas: módulos reales sin "code" estructurado se manejan con regex Agilent — si se cargan sistemas con códigos no-Agilent en producción, podrían caer en placeholder.
- Phase 4 lista para verificación / cierre.
</output>
