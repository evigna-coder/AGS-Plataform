---
phase: 10-presupuestos-partes-mixto-ventas
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - apps/sistema-modular/src/services/presupuestosService.ts
  - apps/sistema-modular/src/services/otService.ts
  - apps/sistema-modular/src/services/facturacionService.ts
autonomous: true
requirements: [PTYP-04, FMT-03]

must_haves:
  truths:
    - "Al aceptar un presupuesto tipo 'ventas' se crea 1 OT genérica de seguimiento asignada al adminConfig.usuarioCoordinadorOTId"
    - "Si falta usuarioCoordinadorOTId config → se registra pendingAction('notificar_coordinador_ot') — no crash"
    - "Si el create OT falla post-commit → pendingAction('notificar_coordinador_ot') con razón detallada — el ppto queda aceptado correctamente"
    - "Idempotencia ventas: si la OT ya existe (budgets.includes(pres.numero)), NO se duplica"
    - "cerrarAdministrativamente crea un doc en solicitudesFacturacion/{otNumber}_{presupuestoId} dentro de la misma tx"
    - "Idempotencia solicitudFacturacion: re-ejecutar cerrarAdministrativamente NO duplica docs (deterministic id + tx.get sentinel)"
    - "El doc solicitudFacturacion incluye: otNumbers: [otNumber], presupuestoId/Numero, clienteId/Nombre, items snapshot, montoTotal, moneda, estado: 'pendiente', ordenesCompraIds (backfill)"
    - "El mail body incluye link al dashboard con deep-link (/facturacion?solicitudId={id})"
    - "Existen métodos facturacionService.marcarEnviada(id, actor), marcarFacturada(id, actor), agregarNota(id, nota, actor)"
  artifacts:
    - path: "apps/sistema-modular/src/services/presupuestosService.ts"
      provides: "aceptarConRequerimientos extension: if tipo === 'ventas' → lazy-import otService + post-commit create OT + pendingAction fallback"
    - path: "apps/sistema-modular/src/services/otService.ts"
      provides: "cerrarAdministrativamente extension: tx.get idempotency sentinel + tx.set solicitudesFacturacion + mail body con deep link"
    - path: "apps/sistema-modular/src/services/facturacionService.ts"
      provides: "marcarEnviada/marcarFacturada/agregarNota methods con audit trace"
  key_links:
    - from: "presupuestosService.aceptarConRequerimientos"
      to: "ordenesTrabajoService.getNextOtNumber + .create"
      via: "lazy import para break circular dep"
      pattern: "await import\\(['\"]\\.\\/otService['\"]\\)"
    - from: "otService.cerrarAdministrativamente"
      to: "solicitudesFacturacion collection"
      via: "tx.set con deterministic id {otNumber}_{presupuestoId}"
      pattern: "solicitudesFacturacion"
    - from: "otService.cerrarAdministrativamente"
      to: "tx.get idempotency sentinel"
      via: "pre-read del solRef para skip si ya existe"
      pattern: "tx\\.get\\(solRef\\)"
    - from: "facturacionService"
      to: "solicitudesFacturacion.estado"
      via: "marcarEnviada/marcarFacturada con getUpdateTrace"
      pattern: "marcarEnviada\\|marcarFacturada"
---

<objective>
Wave 3 — Services extensions. Cierra PTYP-04 (auto-OT ventas) + FMT-03 (solicitudesFacturacion en mismo tx de cierre admin). RESEARCH findings críticos que este plan DEBE respetar:
- **Post-commit** para auto-OT (no nested runTransaction) + **lazy import** (circular dep)
- **Deterministic id** `{otNumber}_{presupuestoId}` + `tx.get` sentinel (pattern Phase 9-02) para idempotency de solicitudFacturacion
- **Extensión** de facturacionService (no rebuild — ya existe)

Purpose: piezas de servicio que unifican la UX del editor (10-02) + PDF (10-03) con los side-effects downstream (auto-OT ventas, aviso facturación).
Output: 3 services modificados con nuevos paths + backward compat preserved.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-CONTEXT.md
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-RESEARCH.md

<!-- Files we extend -->
@apps/sistema-modular/src/services/presupuestosService.ts
@apps/sistema-modular/src/services/otService.ts
@apps/sistema-modular/src/services/facturacionService.ts
@apps/sistema-modular/src/services/adminConfigService.ts

<interfaces>
<!-- Current shapes that matter -->

From presupuestosService.ts:785-907 (existing `aceptarConRequerimientos`):
```typescript
async aceptarConRequerimientos(
  presupuestoId: string,
  actor?: { uid: string; name?: string },
): Promise<{ requerimientosIds: string[] }>
// Already does:
//   - pre-read, filter itemsImport
//   - pre-reserve REQ numbers outside tx
//   - pre-load articulos
//   - runTransaction (update ppto + create requerimientos condicionales)
//   - post-commit: syncFromPresupuesto + _appendPendingAction('derivar_comex')
// EXTENSION POINT: después del bloque `if (itemsImport.length > 0)` pero ANTES del `return`
```

From otService.ts (existing `ordenesTrabajoService`):
```typescript
// Line 43-77
async getNextOtNumber(): Promise<string>  // USA RUNTRANSACTION — NO anidable
// Line 263-~370
async create(otData: Omit<WorkOrder, 'otNumber'> & { otNumber: string }): Promise<any>
// Line 394-523 (existing)
async cerrarAdministrativamente(otNumber, cierreData, actor):
  Promise<{ adminTicketId: string; mailQueueId: string }>
// ALREADY does:
//   - pre-read OT + adminConfig (for mailTo)
//   - pre-read presupuestos + collect ocIds
//   - runTransaction: 1 read (OT) + 3 writes (update OT, set admin ticket, set mailQueue)
//   - post-commit: syncFromOT
// EXTENSION POINT: agregar pre-read de idempotency sentinels + tx.set per presupuesto
```

From facturacionService.ts (existing — lines 1-117):
```typescript
export const facturacionService = {
  getAll, subscribe, getById, getByPresupuesto,
  create, update,
  registrarFactura(id, datos): // cambia estado a 'facturada'
  registrarCobro(id, fechaCobro): // cambia estado a 'cobrada'
};
// EXTENSION: agregar marcarEnviada / marcarFacturada (alias pro-Phase 10) / agregarNota
```

From adminConfigService:
```typescript
adminConfigService.getWithDefaults(): Promise<AdminConfigFlujos>
// retorna { usuarioSeguimientoId, usuarioCoordinadorOTId, mailFacturacion }
```

Phase 9-02 pattern (Phase 9-02 SUMMARY in STATE.md — "ID determinístico sentinel"):
- Sentinel doc: `collectionX/{deterministicId}` escrito como `{ ot: otNumber, ts }`
- Pre-tx: `tx.get(sentinelRef)`; si existe → return (idempotency)
- Post-tx: `tx.set(sentinelRef, ...)` al final

Phase 8-03 precedent (lazy import):
```typescript
// In leadsService.ts — lo replicamos para otService import en presupuestosService:
const { presupuestosService } = await import('./presupuestosService');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender aceptarConRequerimientos para ventas (post-commit auto-OT + pendingAction fallback)</name>
  <files>apps/sistema-modular/src/services/presupuestosService.ts</files>
  <behavior>
    - Flujo: ppto tipo !== 'ventas' → path existente sin cambios (backward compat)
    - Flujo: ppto tipo === 'ventas' → después de la runTransaction commit, llamar a lazy-imported otService para crear 1 OT
    - Config check: si adminConfig.usuarioCoordinadorOTId es null/undefined → _appendPendingAction('notificar_coordinador_ot', reason explicit) + return (no throw)
    - Idempotency: si ya existe OT con budgets.includes(pres.numero) → NO recrear (pre-check con query simple)
    - Failure path: si create OT falla (catch) → _appendPendingAction con reason detallado + console.error; NO propagar error (el ppto ya quedó aceptado)
    - OT shape: inline via ordenesTrabajoService.create con otNumber pre-reservado, clienteId/razonSocial del pres, ingenieroAsignadoId = coordId, estadoAdmin='CREADA', status='BORRADOR', descripcion explicativa, budgets=[pres.numero], fechaServicioAprox = ventasMetadata?.fechaEstimadaEntrega
    - Post-create: patch presupuesto con otsVinculadasNumbers: [...existing, otNumber]
  </behavior>
  <action>
En `apps/sistema-modular/src/services/presupuestosService.ts`:

Localizar el final de `aceptarConRequerimientos` (línea ~906, DESPUÉS del bloque `if (itemsImport.length > 0)` con `_appendPendingAction('derivar_comex')` y ANTES del `return { requerimientosIds: newReqIds };`).

**Agregar bloque completo:**

```typescript
    // ── Paso 6: Phase 10 — si tipo === 'ventas', auto-crear 1 OT genérica (post-commit) ──
    // Rationale: nested runTransaction está prohibido por Firebase SDK. getNextOtNumber()
    // ya usa su propia tx para el counter — no anidable. Best-effort post-commit + pendingAction
    // fallback. Precedente: Phase 8-03 _appendPendingAction retry path.
    if (pres.tipo === 'ventas') {
      try {
        // Idempotency check: si alguna OT ya tiene este ppto en budgets, skip
        const existingOTs = await getDocs(
          query(collection(db, 'reportes'), where('budgets', 'array-contains', pres.numero)),
        );
        if (existingOTs.size > 0) {
          console.log(`[aceptarConRequerimientos] OT ya existe para ppto ${pres.numero}, skip auto-create`);
        } else {
          const cfg = await adminConfigService.getWithDefaults();
          const coordId = cfg.usuarioCoordinadorOTId;
          if (!coordId) {
            await this._appendPendingAction(presupuestoId, {
              type: 'notificar_coordinador_ot',
              reason: `Auto ventas — ppto ${pres.numero} aceptado; falta adminConfig.usuarioCoordinadorOTId. Configurar en /admin/config-flujos y reintentar desde /admin/acciones-pendientes.`,
            });
          } else {
            // Lazy import rompe circular dep (otService ↔ presupuestosService) — precedente Phase 8-03
            const { ordenesTrabajoService } = await import('./otService');
            const otNumber = await ordenesTrabajoService.getNextOtNumber();
            const fechaServicio = pres.ventasMetadata?.fechaEstimadaEntrega ?? null;
            await ordenesTrabajoService.create({
              otNumber,
              clienteId: pres.clienteId,
              razonSocial: pres.responsableNombre ?? '',
              contactoId: pres.contactoId ?? null,
              ingenieroAsignadoId: coordId,
              ingenieroAsignadoNombre: null,
              fechaServicioAprox: fechaServicio,
              budgets: [pres.numero],
              estadoAdmin: 'CREADA',
              status: 'BORRADOR',
              leadId: pres.origenTipo === 'lead' ? pres.origenId : null,
              descripcion: `Seguimiento venta equipo — ppto ${pres.numero}. Coordinador arma OTs específicas (bench, entrega, instalación, QI, QO) manualmente.`,
            } as any);
            // Back-link en el presupuesto
            const prevOts = Array.isArray(pres.otsVinculadasNumbers) ? pres.otsVinculadasNumbers : [];
            await updateDoc(doc(db, 'presupuestos', presupuestoId), deepCleanForFirestore({
              otsVinculadasNumbers: [...prevOts, otNumber],
              updatedAt: Timestamp.now(),
              updatedBy: actor?.uid ?? null,
              updatedByName: actor?.name ?? null,
            }));
            console.log(`[aceptarConRequerimientos] auto-OT ventas creada: ${otNumber} para ppto ${pres.numero}`);
          }
        }
      } catch (err) {
        console.error('[aceptarConRequerimientos] auto-create OT ventas failed:', err);
        await this._appendPendingAction(presupuestoId, {
          type: 'notificar_coordinador_ot',
          reason: `Auto ventas — ppto ${pres.numero} aceptado; OT auto-create falló: ${err instanceof Error ? err.message : String(err)}. Reintentar desde /admin/acciones-pendientes.`,
        }).catch(() => {});
      }
    }
```

**Imports requeridos** (ya presentes al top del archivo — verificar): `collection`, `query`, `where`, `getDocs`, `doc`, `updateDoc`, `Timestamp` de `firebase/firestore`; `db`, `deepCleanForFirestore` de `./firebase`; `adminConfigService` de `./adminConfigService`.

Confirmar que `_appendPendingAction` existe (landed en Phase 8-03) aceptando la shape `{ type, reason }`.
  </action>
  <verify>
    <automated>pnpm --filter sistema-modular type-check 2>&1 | grep -E "presupuestosService|aceptarConRequerimientos" | head -10</automated>
  </verify>
  <done>
- Bloque ventas agregado al final de aceptarConRequerimientos
- Idempotency check por budgets.array-contains
- Lazy import documentado inline
- pendingAction fallback en todos los branches de error
- type-check pasa
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extender cerrarAdministrativamente con tx.set de solicitudesFacturacion + deterministic id + mail body con deep link</name>
  <files>apps/sistema-modular/src/services/otService.ts</files>
  <behavior>
    - cerrarAdministrativamente pre-reads (lines 399-435) extendidos con idempotency pre-check — get de sentinel refs fuera de tx
    - runTransaction: entre Write 3 (mailQueue) y el cierre, para cada presupuestoId válido → tx.get(solRef) + si !exists → tx.set(solRef, payload)
    - Deterministic id: `${otNumber}_${presupuestoId}` como docId en solicitudesFacturacion (replica pattern Phase 9-02 ot_cierre_idempotency)
    - Payload solicitud: compatible con SolicitudFacturacion shape existente + campos nuevos Wave 10-01 (ordenesCompraIds, estado 'pendiente')
    - Mail body extendido: agregar al final una línea con CTA/deep link al dashboard — "Ver detalles: {baseUrl}/facturacion?solicitudId={firstSolicitudId}"
    - NO eliminar el cuerpo existente del mail (backward compat de mailQueue consumer cuando llegue)
    - Backward compat: cerrarAdministrativamente retorno extendido — `{ adminTicketId, mailQueueId, solicitudIds: string[] }`. Callers existentes que ignoren el campo nuevo siguen funcionando.
  </behavior>
  <action>
En `apps/sistema-modular/src/services/otService.ts` en el método `cerrarAdministrativamente` (línea 394):

**1. Después de la línea 431 (el cálculo de `ocIds`), ANTES de `const subject = ...`, agregar:**

```typescript
    // Phase 10 — deterministic ids para idempotency de solicitudesFacturacion.
    // Pattern: Phase 9-02 (ot_cierre_idempotency). Evita duplicación si dos usuarios
    // disparan cerrarAdministrativamente concurrently (race) o si el método se re-ejecuta.
    const solicitudDeterministicIds: string[] = presupuestoIds.map(pid => `${otNumber}_${pid}`);
```

**2. Modificar el `body` builder (línea ~434)** — el helper `buildAvisoFacturacionBody` existe; si no se quiere tocar su firma, apendear al final del body el CTA. Enfoque más simple: después de la línea `const body = buildAvisoFacturacionBody(ot, presupuestosPorNumero);` agregar:

```typescript
    // Phase 10 — CTA deep link al dashboard. Asume que el consumer del mailQueue expone
    // baseUrl via env o config; en el body inline el path, el consumer lo compone.
    const firstSolId = solicitudDeterministicIds[0] || '';
    const deepLinkPath = firstSolId ? `/facturacion?solicitudId=${firstSolId}` : '/facturacion';
    const bodyWithCTA = `${body}\n\n---\nVer en sistema: ${deepLinkPath}`;
```

Usar `bodyWithCTA` en el payload del `mailQueue.set` (línea ~490).

**3. Dentro del `runTransaction` (línea 441-511), ENTRE el Write 3 (mailQueue set, línea ~510) y el cierre `})`, agregar:**

```typescript
      // Phase 10 — Write 4+: solicitudesFacturacion (una por presupuesto, idempotent)
      for (let i = 0; i < presupuestoIds.length; i++) {
        const pid = presupuestoIds[i];
        const p = presupuestosPorNumero[i];
        if (!p) continue;
        const solId = solicitudDeterministicIds[i];
        const solRef = doc(db, 'solicitudesFacturacion', solId);

        // Idempotency sentinel — si ya existe, skip (no duplicar)
        const existingSol = await tx.get(solRef);
        if (existingSol.exists()) continue;

        const solPayload = deepCleanForFirestore({
          presupuestoId: pid,
          presupuestoNumero: p.numero,
          clienteId: p.clienteId,
          clienteNombre: ot.razonSocial || '',
          condicionPago: '',  // se puede enriquecer post-commit si adminConfig tiene lookup; por ahora empty
          items: (p.items || []).map(it => ({
            id: (typeof crypto !== 'undefined' && crypto.randomUUID) ? crypto.randomUUID() : `${Date.now()}-${Math.random().toString(36).slice(2)}`,
            presupuestoItemId: it.id,
            descripcion: it.descripcion,
            cantidad: it.cantidad,
            cantidadTotal: it.cantidad,
            precioUnitario: it.precioUnitario,
            subtotal: it.subtotal,
          })),
          montoTotal: p.total,
          moneda: p.moneda,
          estado: 'pendiente' as const,
          otNumbers: [otNumber],
          ordenesCompraIds: p.ordenesCompraIds || [],    // Wave 10-01 new field
          observaciones: `Auto — cierre administrativo de OT ${otNumber} (Phase 10 FMT-03).`,
          solicitadoPor: actor?.uid ?? null,
          solicitadoPorNombre: actor?.name ?? null,
          createdAt: nowIso,
          updatedAt: nowIso,
        });
        tx.set(solRef, solPayload);
      }
```

**4. Modificar el return statement** (línea 522) — incluir los ids de solicitudes:

```typescript
    return {
      adminTicketId: newAdminTicketRef.id,
      mailQueueId: newMailQueueRef.id,
      solicitudIds: solicitudDeterministicIds,  // Phase 10 addition
    };
```

**5. Actualizar el JSDoc** del método (líneas 361-393 aprox) para documentar el nuevo Write 4+:
```typescript
   * Writes:
   *   1. Update OT a CIERRE_ADMINISTRATIVO
   *   2. Ticket admin (area='administracion')
   *   3. mailQueue doc (type='cierre_admin_ot')
   *   4. (Phase 10) solicitudesFacturacion doc por cada presupuesto vinculado
   *      - ID determinístico `{otNumber}_{presupuestoId}`
   *      - Idempotency via tx.get sentinel
   *      - Body del mail incluye CTA deep link al dashboard
```

**Verificación de limites Firestore tx (per RESEARCH §4):**
- Current: 1 read + 3 writes
- Agregado: N reads (sentinels) + N writes (sol docs) — con N ≤ 10 típico, siempre ≤ 20 total. 500 límite no se acerca.
- Reads-before-writes: OK — los `tx.get(solRef)` van DESPUÉS del `tx.get(otRef)` inicial pero ANTES del primer `tx.set(solRef)`. Firestore SDK permite reads e inquiries intercalados siempre que la primera operación de cada tx sea un read. Validar ejecutando un test.
  </action>
  <verify>
    <automated>pnpm --filter sistema-modular type-check 2>&1 | grep -E "otService|cerrarAdministrativamente" | head -10</automated>
  </verify>
  <done>
- Bloque de solicitudesFacturacion escribe con id determinístico dentro de tx
- Idempotency check via tx.get funciona
- Body del mail incluye deep link
- return extendido con solicitudIds
- type-check pasa
- JSDoc actualizado
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extender facturacionService con marcarEnviada, marcarFacturada, agregarNota</name>
  <files>apps/sistema-modular/src/services/facturacionService.ts</files>
  <behavior>
    - marcarEnviada(id, actor): cambia estado a 'enviada' + setea enviadaAt = Timestamp.now() + audit trace
    - marcarFacturada(id, actor, datos): delega en el registrarFactura existente pero también acepta ser llamado desde el dashboard Phase 10 (mismo behavior)
    - agregarNota(id, nota, actor): appendea/reemplaza el campo observaciones con la nota nueva; audit via updatedBy
    - NO rompe métodos existentes (getAll, subscribe, create, update, registrarFactura, registrarCobro)
  </behavior>
  <action>
En `apps/sistema-modular/src/services/facturacionService.ts` — agregar métodos nuevos AL FINAL del objeto exportado (antes del `};` de cierre, línea ~117):

```typescript
  /**
   * Phase 10 — Marcar solicitud como enviada (mail al contable ya fue disparado).
   * Transiciona estado 'pendiente' → 'enviada' y registra timestamp.
   */
  async marcarEnviada(id: string, actor?: { uid: string; name?: string }): Promise<void> {
    const ref = docRef('solicitudesFacturacion', id);
    const batch = createBatch();
    const cleaned = cleanFirestoreData({
      estado: 'enviada' as const,
      enviadaAt: new Date().toISOString(),  // Wave 10-01 field
      updatedAt: Timestamp.now(),
      updatedBy: actor?.uid ?? null,
      updatedByName: actor?.name ?? null,
    });
    batch.update(ref, cleaned);
    await batch.commit();
  },

  /**
   * Phase 10 — Marcar solicitud como facturada (alias semántico con más contexto que registrarFactura).
   * Acepta datos opcionales — si se pasan, registra la factura completa; si no, solo cambia estado.
   * Usable desde el row-action del dashboard sin requerir que el contable tenga los datos AFIP aún.
   */
  async marcarFacturada(
    id: string,
    actor?: { uid: string; name?: string },
    datos?: { numeroFactura?: string; fechaFactura?: string },
  ): Promise<void> {
    await this.update(id, {
      estado: 'facturada',
      numeroFactura: datos?.numeroFactura ?? null,
      fechaFactura: datos?.fechaFactura ?? null,
      facturadoPor: actor?.uid ?? null,
      facturadoPorNombre: actor?.name ?? null,
    });
  },

  /**
   * Phase 10 — Agregar/reemplazar observaciones (nota del contable).
   * NO es append — reemplaza. Si el caller quiere append, concatena en el UI.
   */
  async agregarNota(id: string, nota: string, actor?: { uid: string; name?: string }): Promise<void> {
    await this.update(id, {
      observaciones: nota,
      // update() ya setea updatedBy/updatedAt via getUpdateTrace
    });
    void actor;  // actor ya capturado por getUpdateTrace via firebase helpers
  },
```

Verificar imports al top del file (línea 1-3): `Timestamp`, `createBatch`, `docRef`, `cleanFirestoreData` ya están importados — no hace falta cambiar imports.
  </action>
  <verify>
    <automated>pnpm --filter sistema-modular type-check 2>&1 | grep -E "facturacionService" | head -10</automated>
  </verify>
  <done>
- 3 métodos nuevos exportados
- Backward compat preservada (otros métodos intactos)
- type-check pasa
  </done>
</task>

</tasks>

<verification>
**Automated:**
- `pnpm --filter sistema-modular type-check` pasa
- E2E: `pnpm --filter sistema-modular e2e -- -g "3\.7"` (ventas + auto-OT assertion) debería ya no fallar por "OT not created"
- E2E: `pnpm --filter sistema-modular e2e -- circuits/11-full-business-cycle.spec.ts -g "11\.13"` — assertion sobre solicitudesFacturacion debería ya no fallar por "doc not found"

**Manual:**
- Crear ppto ventas, aceptar, verificar en /ordenes-trabajo que aparece 1 OT genérica con razón social correcta
- Cerrar admin una OT, verificar en Firestore (o via dashboard de 10-06) que existe doc `solicitudesFacturacion/{otNumber}_{presupuestoId}`
- Re-disparar cerrarAdministrativamente (manualmente con script — no esperado UI flow) y verificar que NO se duplica el doc
</verification>

<success_criteria>
- PTYP-04 cerrada: auto-OT ventas con pendingAction fallback
- FMT-03 cerrada: solicitudesFacturacion creada en tx + idempotent
- Backward compat: flow de cierre admin no-ventas sigue igual
- Circular dep resuelta con lazy import
- Commits atómicos por task (3 commits)
</success_criteria>

<output>
Crear `.planning/phases/10-presupuestos-partes-mixto-ventas/10-04-SUMMARY.md` con:
- Archivos modificados + líneas delta
- Decisiones sobre shape del payload solicitud (crypto.randomUUID fallback, condicionPago empty, etc)
- Idempotency strategy documented
- E2E tests que desfixmear
</output>
