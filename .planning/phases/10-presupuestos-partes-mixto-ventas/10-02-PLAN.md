---
phase: 10-presupuestos-partes-mixto-ventas
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - apps/sistema-modular/src/components/presupuestos/ArticuloPickerPanel.tsx
  - apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx
  - apps/sistema-modular/src/components/presupuestos/VentasMetadataSection.tsx
  - apps/sistema-modular/src/components/presupuestos/EditPresupuestoModal.tsx
  - apps/sistema-modular/src/hooks/usePresupuestoEdit.ts
autonomous: true
requirements: [PTYP-02, PTYP-03, PTYP-04]

must_haves:
  truths:
    - "Al seleccionar tipo partes/mixto/ventas en AddItemModal, aparece un ArticuloPickerPanel para elegir artículo de stock"
    - "Al elegir artículo, StockAmplioIndicator muestra 4 buckets (disponible/reservado/en transito/comprometido)"
    - "El item creado tiene stockArticuloId + codigoProducto + descripcion rellenos desde el artículo seleccionado"
    - "itemRequiereImportacion se computa via atpHelpers.itemRequiresImportacion al seleccionar el artículo"
    - "Un ppto tipo 'ventas' muestra VentasMetadataSection con 3 campos (fechaEstimadaEntrega, lugarInstalacion, requiereEntrenamiento)"
    - "Los cambios en VentasMetadataSection persisten al guardar (Presupuesto.ventasMetadata)"
    - "Validación UX-only al aceptar: si ATP < cantidad de un item → warning no bloqueante (acceptance sigue disparando FLOW-03)"
    - "AddItemModal permanece ≤ 250 líneas tras refactor (panel extraído a ArticuloPickerPanel.tsx)"
  artifacts:
    - path: "apps/sistema-modular/src/components/presupuestos/ArticuloPickerPanel.tsx"
      provides: "Search + selección de artículo con StockAmplioIndicator inline"
      min_lines: 80
    - path: "apps/sistema-modular/src/components/presupuestos/VentasMetadataSection.tsx"
      provides: "3-field form para ventasMetadata"
      min_lines: 60
    - path: "apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx"
      provides: "Gated article picker visible cuando tipo ∈ {partes, mixto, ventas}"
    - path: "apps/sistema-modular/src/components/presupuestos/EditPresupuestoModal.tsx"
      provides: "Render de VentasMetadataSection + validación UX-only pre-acceptance"
  key_links:
    - from: "AddItemModal.tsx"
      to: "ArticuloPickerPanel.tsx"
      via: "prop gated by tipoPresupuesto ∈ {partes, mixto, ventas}"
      pattern: "<ArticuloPickerPanel"
    - from: "ArticuloPickerPanel.tsx"
      to: "useStockAmplio + StockAmplioIndicator"
      via: "hook + component import, renderea inline al seleccionar"
      pattern: "useStockAmplio\\|StockAmplioIndicator"
    - from: "ArticuloPickerPanel.tsx"
      to: "atpHelpers.itemRequiresImportacion"
      via: "compute flag on select, pasar a setNewItem"
      pattern: "itemRequiresImportacion"
    - from: "EditPresupuestoModal.tsx"
      to: "VentasMetadataSection.tsx"
      via: "render gated por form.tipo === 'ventas'"
      pattern: "form.tipo === 'ventas'"
    - from: "usePresupuestoEdit"
      to: "form.ventasMetadata"
      via: "setField('ventasMetadata', ...) + persistencia via save()"
      pattern: "ventasMetadata"
---

<objective>
Wave 2 — Editor UI extensions. Cierra el gap crítico de RESEARCH: hoy partes/mixto/ventas NO pueden vincular items a stock (AddItemModal.tsx:94 gate `tipoPresupuesto === 'contrato'`), por lo que FLOW-03 nunca dispara. Esta plan agrega el ArticuloPickerPanel para los tipos non-contrato + VentasMetadataSection + validación UX-only.

Purpose: sin esto, PTYP-02 y PTYP-03 son flows rotos en silencio. Wave 2 desbloquea el editor para los 3 tipos. Corre en paralelo con 10-03 (PDF branching) porque toca archivos distintos.
Output: panel nuevo + section nueva + wiring en AddItemModal + EditPresupuestoModal + persistencia de ventasMetadata en usePresupuestoEdit.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-CONTEXT.md
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-RESEARCH.md

<!-- Files we extend -->
@apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx
@apps/sistema-modular/src/components/presupuestos/EditPresupuestoModal.tsx
@apps/sistema-modular/src/components/presupuestos/contrato/ArticuloInlineAutocomplete.tsx

<!-- Reuse targets -->
@apps/sistema-modular/src/components/stock/StockAmplioIndicator.tsx
@apps/sistema-modular/src/hooks/useStockAmplio.ts
@apps/sistema-modular/src/services/atpHelpers.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From StockAmplioIndicator (existing):
```typescript
// apps/sistema-modular/src/components/stock/StockAmplioIndicator.tsx
interface StockAmplioIndicatorProps {
  stockAmplio: StockAmplio | null;  // from useStockAmplio hook
  size?: 'sm' | 'md' | 'lg';
}
```

From useStockAmplio (existing):
```typescript
// apps/sistema-modular/src/hooks/useStockAmplio.ts
export function useStockAmplio(articuloId: string | null): {
  stockAmplio: StockAmplio | null;
  loading: boolean;
  error: Error | null;
};
// Hook usa onSnapshot reactive. Con articuloId=null no subscribe.
```

From atpHelpers (existing — used in contrato flow):
```typescript
// apps/sistema-modular/src/services/atpHelpers.ts (pattern)
export async function itemRequiresImportacion(articuloId: string): Promise<boolean>;
```

From articulosService (existing):
```typescript
export const articulosService = {
  getAll(): Promise<Articulo[]>,  // usamos el full catalog como prop
  getById(id: string): Promise<Articulo | null>,
  // Articulo shape relevante: { id, codigo, descripcion, precio?, proveedorIds? }
};
```

From PresupuestoItem (@ags/shared):
```typescript
// existente — campos que setteamos al seleccionar artículo:
interface PresupuestoItem {
  id: string;
  descripcion: string;
  cantidad: number;
  unidad: string;
  precioUnitario: number;
  codigoProducto?: string | null;
  stockArticuloId?: string | null;       // ← ligamos aquí
  itemRequiereImportacion?: boolean;     // ← via atpHelpers
  // ...
}
```

From Presupuesto (@ags/shared, post Wave 10-01):
```typescript
interface Presupuesto {
  tipo: TipoPresupuesto;  // 'servicio' | 'partes' | 'ventas' | 'contrato' | 'mixto'
  ventasMetadata?: VentasMetadata | null;  // ← nuevo (Wave 10-01)
  // ...
}

interface VentasMetadata {
  fechaEstimadaEntrega?: string | null;
  lugarInstalacion?: string | null;
  requiereEntrenamiento?: boolean;
}
```

Pattern existente (`ArticuloInlineAutocomplete`) — lo usamos como base técnica pero el UX del PickerPanel es más grande (mostramos StockAmplioIndicator visible, no popup).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear ArticuloPickerPanel.tsx + VentasMetadataSection.tsx</name>
  <files>apps/sistema-modular/src/components/presupuestos/ArticuloPickerPanel.tsx, apps/sistema-modular/src/components/presupuestos/VentasMetadataSection.tsx</files>
  <behavior>
    - ArticuloPickerPanel: al seleccionar artículo del SearchableSelect (>= 2 chars), muestra StockAmplioIndicator inline debajo con size="sm" + los 4 buckets + ATP neto
    - ArticuloPickerPanel: al confirmar selección, llama onSelect(articulo, { itemRequiereImportacion: boolean }) con flag computado desde itemRequiresImportacion(articuloId)
    - ArticuloPickerPanel: un "Quitar artículo" que limpia el binding (stockArticuloId=null)
    - ArticuloPickerPanel: si `articuloSeleccionadoId` prop cambia externamente (ej. newItem ya tenía stockArticuloId), muestra el indicator de ese artículo
    - VentasMetadataSection: renderea 3 campos editables (date, text, checkbox)
    - VentasMetadataSection: emite onChange(partial) en cada tecla/click; parent compose el full object
    - VentasMetadataSection: labels monospace uppercase tracking-wide text-[10px] (Editorial Teal convention)
    - VentasMetadataSection: si ventasMetadata es null/undefined, muestra campos vacíos (valor default string '' / boolean false)
  </behavior>
  <action>
**ArticuloPickerPanel.tsx (~100 líneas, budget < 250):**

```tsx
// apps/sistema-modular/src/components/presupuestos/ArticuloPickerPanel.tsx
import { useMemo } from 'react';
import type { Articulo } from '@ags/shared';
import { SearchableSelect } from '../ui/SearchableSelect';
import { StockAmplioIndicator } from '../stock/StockAmplioIndicator';
import { useStockAmplio } from '../../hooks/useStockAmplio';
import { itemRequiresImportacion } from '../../services/atpHelpers';

interface Props {
  /** Catalog completo — caller lo pasa ya cargado (evita re-fetch por render). */
  articulos: Articulo[];
  /** Id actual del artículo ligado al item (null si no hay). */
  articuloSeleccionadoId: string | null;
  /** Callback: parent debe setear stockArticuloId, codigoProducto, descripcion en newItem. */
  onSelect: (
    articulo: Articulo | null,                 // null = unlink
    meta: { itemRequiereImportacion: boolean },
  ) => void;
  /** Si true, oculta el label superior — usado cuando el panel va dentro de otro con título. */
  compact?: boolean;
}

export const ArticuloPickerPanel: React.FC<Props> = ({
  articulos, articuloSeleccionadoId, onSelect, compact,
}) => {
  const options = useMemo(
    () => articulos.map(a => ({
      value: a.id,
      label: `[${a.codigo || '—'}] ${a.descripcion}`,
    })),
    [articulos],
  );

  const { stockAmplio, loading } = useStockAmplio(articuloSeleccionadoId);

  const handleChange = async (id: string) => {
    if (!id) { onSelect(null, { itemRequiereImportacion: false }); return; }
    const art = articulos.find(a => a.id === id);
    if (!art) return;
    // Phase 10: compute flag at select-time (no wait → setNewItem already persisted)
    let requiere = false;
    try { requiere = await itemRequiresImportacion(id); } catch { requiere = false; }
    onSelect(art, { itemRequiereImportacion: requiere });
  };

  return (
    <div className="space-y-1.5">
      {!compact && (
        <label className="text-[10px] font-mono uppercase tracking-wide text-slate-400 block">
          Artículo de stock
        </label>
      )}
      <SearchableSelect
        value={articuloSeleccionadoId || ''}
        onChange={(v) => { void handleChange(v); }}
        options={[{ value: '', label: 'Sin vincular (carga manual)' }, ...options]}
        placeholder="Buscar por código o descripción..."
      />
      {articuloSeleccionadoId && (
        <div className="bg-slate-50 rounded-lg p-2 border border-slate-200">
          {loading ? (
            <p className="text-[10px] text-slate-400 italic">Cargando stock...</p>
          ) : stockAmplio ? (
            <StockAmplioIndicator stockAmplio={stockAmplio} size="sm" />
          ) : (
            <p className="text-[10px] text-slate-400 italic">Sin datos de stock</p>
          )}
        </div>
      )}
    </div>
  );
};
```

**Test manual durante dev:** abrir AddItemModal en un ppto partes; elegir artículo; verificar que el indicator renderea los 4 buckets.

**VentasMetadataSection.tsx (~80 líneas):**

```tsx
// apps/sistema-modular/src/components/presupuestos/VentasMetadataSection.tsx
import type { VentasMetadata } from '@ags/shared';

interface Props {
  value: VentasMetadata | null | undefined;
  onChange: (patch: Partial<VentasMetadata>) => void;
}

export const VentasMetadataSection: React.FC<Props> = ({ value, onChange }) => {
  const v = value || {};
  return (
    <div className="border border-teal-200 bg-teal-50/40 rounded-lg p-3 my-3">
      <h4 className="text-[10px] font-mono uppercase tracking-wide text-teal-800 font-semibold mb-2">
        Datos de entrega e instalación
      </h4>
      <div className="grid grid-cols-2 gap-3">
        <div>
          <label className="text-[10px] font-mono uppercase tracking-wide text-slate-400 mb-0.5 block">
            Fecha estimada de entrega
          </label>
          <input
            type="date"
            value={v.fechaEstimadaEntrega || ''}
            onChange={e => onChange({ fechaEstimadaEntrega: e.target.value || null })}
            className="w-full border border-slate-200 rounded-md px-2 py-1 text-xs"
          />
        </div>
        <div>
          <label className="text-[10px] font-mono uppercase tracking-wide text-slate-400 mb-0.5 block">
            Lugar de instalación
          </label>
          <input
            type="text"
            value={v.lugarInstalacion || ''}
            onChange={e => onChange({ lugarInstalacion: e.target.value || null })}
            placeholder="Dirección libre"
            className="w-full border border-slate-200 rounded-md px-2 py-1 text-xs"
          />
        </div>
      </div>
      <label className="flex items-center gap-2 mt-2 cursor-pointer">
        <input
          type="checkbox"
          checked={!!v.requiereEntrenamiento}
          onChange={e => onChange({ requiereEntrenamiento: e.target.checked })}
          className="w-3.5 h-3.5 text-teal-600 rounded"
        />
        <span className="text-xs text-slate-700">Requiere entrenamiento post-instalación</span>
      </label>
    </div>
  );
};
```
  </action>
  <verify>
    <automated>pnpm --filter sistema-modular type-check 2>&1 | grep -E "ArticuloPickerPanel|VentasMetadataSection" || echo OK</automated>
  </verify>
  <done>Ambos archivos compilan; ≤ 250 líneas cada uno; exports correctos; imports canónicos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire ArticuloPickerPanel en AddItemModal + gate por tipo</name>
  <files>apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx</files>
  <behavior>
    - AddItemModal con tipoPresupuesto ∈ {partes, mixto, ventas} + articulos prop no-empty → renderea ArticuloPickerPanel ANTES del input manual "Código artículo"
    - AddItemModal con tipoPresupuesto === 'servicio' → NO renderea picker (servicio tiene conceptos, no articulos)
    - AddItemModal con tipoPresupuesto === 'contrato' → SIGUE usando EquipoLinkPanel (NO rompemos el flow existente)
    - onSelect del picker: setNewItem({...newItem, stockArticuloId, codigoProducto, descripcion, precioUnitario (si existe en articulo.precio), itemRequiereImportacion})
    - onSelect(null): limpia los 4 campos (stockArticuloId=null, itemRequiereImportacion=false); NO toca descripcion/precio ya tipeados si el user ya los editó manualmente
    - File ≤ 250 líneas después del refactor
  </behavior>
  <action>
En `apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx`:

1. **Agregar prop nueva** al `AddItemModalProps`:
```typescript
/** Full catalog de articulos. Solo se usa cuando tipo ∈ {partes, mixto, ventas}. */
articulos?: Articulo[];
```

2. **Importar** `ArticuloPickerPanel` + tipo `Articulo` desde `@ags/shared`.

3. **Agregar handler** arriba de `return (`:
```typescript
const handleSelectArticulo = (
  art: Articulo | null,
  meta: { itemRequiereImportacion: boolean },
) => {
  if (!art) {
    setNewItem({
      ...newItem,
      stockArticuloId: null,
      itemRequiereImportacion: false,
    });
    return;
  }
  setNewItem({
    ...newItem,
    stockArticuloId: art.id,
    codigoProducto: art.codigo || newItem.codigoProducto,
    descripcion: newItem.descripcion || art.descripcion,  // preserva si user ya tipeó
    precioUnitario: newItem.precioUnitario || (art as any).precio || 0,
    itemRequiereImportacion: meta.itemRequiereImportacion,
  });
};

const showArticuloPicker = (
  (tipoPresupuesto === 'partes' ||
   tipoPresupuesto === 'mixto' ||
   tipoPresupuesto === 'ventas') &&
  articulos && articulos.length > 0
);
```

4. **Renderear el panel** ANTES del bloque "Código artículo" (línea ~118), reemplazando el comentario vestigial de FLOW-03 (línea 58-62):

```tsx
{/* Article picker — partes/mixto/ventas */}
{showArticuloPicker && (
  <ArticuloPickerPanel
    articulos={articulos!}
    articuloSeleccionadoId={newItem.stockArticuloId || null}
    onSelect={handleSelectArticulo}
  />
)}
```

5. **IMPORTANTE — comentario vestigial TODO(FLOW-03):** remover las líneas 58-62 (`// FLOW-03: si algún día... void itemRequiresImportacion;`) — ya no son necesarias, reemplazadas por el panel real. Dejar solo el comentario en `handleSelectConcepto` (línea 43-46) que explica que conceptosServicio tienen `itemRequiereImportacion: false` por contrato.

6. **Verificar 250-line budget:** el archivo actual está en 171 líneas. Tras agregar panel ≈ 190 líneas. Si cruza 250 por cambios adicionales, extraer sección "tax preview" a componente separado en task fixup.

NO tocar:
- EquipoLinkPanel existing (sigue gated por `showEquipoPanel = tipoPresupuesto === 'contrato' && ...`)
- ConceptoServicio picker existing
- Tax preview logic
  </action>
  <verify>
    <automated>pnpm --filter sistema-modular type-check 2>&1 | grep "AddItemModal" || echo OK; wc -l apps/sistema-modular/src/components/presupuestos/AddItemModal.tsx</automated>
  </verify>
  <done>Panel wire-up completo; tipo-check pasa; archivo ≤ 250 líneas; showArticuloPicker gating exacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire VentasMetadataSection en EditPresupuestoModal + persistir en usePresupuestoEdit + validación pre-aceptación UX-only + pasar articulos a AddItemModal</name>
  <files>apps/sistema-modular/src/components/presupuestos/EditPresupuestoModal.tsx, apps/sistema-modular/src/hooks/usePresupuestoEdit.ts</files>
  <behavior>
    - Si `form.tipo === 'ventas'`, VentasMetadataSection aparece entre PresupuestoMetadataStrip y PresupuestoItemsTable
    - onChange del section llama setField('ventasMetadata', { ...form.ventasMetadata, ...patch })
    - usePresupuestoEdit.save() persiste form.ventasMetadata al doc (sin undefined → deepCleanForFirestore ya lo strips)
    - Al hacer click en "Aceptar" (o transitionar estado a 'aceptado'), si hay items con stockArticuloId cuya cantidad > stockAmplio.atp → mostrar confirm dialog no-bloqueante: "Warning: el artículo X tiene cantidad N pero ATP actual es M. FLOW-03 creará requerimientos condicionales. ¿Continuar?" — user OK → sigue; user cancel → no cambia estado
    - Validación NO bloquea la transición definitivamente — genera feedback. Si falla el compute de ATP, log + seguir (fail-soft; la aceptación genuina está cubierta por FLOW-03).
    - AddItemModal recibe articulos prop desde EditPresupuestoModal via PresupuestoItemsTable (si tipo !== contrato)
  </behavior>
  <action>
**EditPresupuestoModal.tsx:**

1. **Importar** `VentasMetadataSection`.

2. **Importar** `articulosService` desde `../../services/stockService` (o wherever se expone).

3. **Agregar load de articulos** — useEffect al mount (solo si tipo ∈ partes/mixto/ventas):

```typescript
const [articulos, setArticulos] = useState<Articulo[]>([]);
useEffect(() => {
  if (!open) return;
  const needsCatalog = form.tipo && ['partes', 'mixto', 'ventas'].includes(form.tipo);
  if (!needsCatalog) return;
  articulosService.getAll().then(setArticulos).catch(() => setArticulos([]));
}, [open, form.tipo]);
```

4. **Render VentasMetadataSection** — insertar ENTRE `<PresupuestoMetadataStrip ... />` (línea 184-191) y `<PresupuestoOTsVinculadas ... />` (línea 193-196):

```tsx
{form.tipo === 'ventas' && (
  <VentasMetadataSection
    value={form.ventasMetadata}
    onChange={(patch) => setField('ventasMetadata', { ...form.ventasMetadata, ...patch })}
  />
)}
```

5. **Pasar articulos prop a PresupuestoItemsTable** (línea 233-252) — agregar prop `articulos={articulos}` al render non-contrato. Esto requiere que `PresupuestoItemsTable` también propague la prop a su `AddItemModal` interno. Extender ambos:
   - `PresupuestoItemsTable.tsx`: agregar prop `articulos?: Articulo[]` al type, pasar a `<AddItemModal articulos={articulos} ... />`
   - (El wire-up real lo hace Task 2 — ya espera la prop en AddItemModal)

6. **Validación pre-acceptance** — interceptar `actions.handleEstadoChange` para tipo partes/mixto/ventas. Esto vive en `usePresupuestoActions` o directamente en EditPresupuestoModal. Enfoque más simple: envolver la llamada en el PresupuestoMetadataStrip `onEstadoChange` prop (línea 190):

```typescript
const handleEstadoChangeWithValidation = async (nuevo: PresupuestoEstado) => {
  if (nuevo !== 'aceptado') return actions.handleEstadoChange(nuevo);
  // Solo validar para tipos non-contrato
  if (!['partes', 'mixto', 'ventas'].includes(form.tipo)) {
    return actions.handleEstadoChange(nuevo);
  }
  // Chequear ATP por item con stockArticuloId
  const warnings: string[] = [];
  for (const item of form.items.filter(i => i.stockArticuloId)) {
    try {
      const s = await computeStockAmplio(item.stockArticuloId!);
      if ((item.cantidad || 0) > (s?.atpNeto ?? 0)) {
        warnings.push(`${item.descripcion}: cantidad ${item.cantidad}, ATP disponible ${s?.atpNeto ?? 0}`);
      }
    } catch (err) {
      console.warn('ATP check failed for', item.stockArticuloId, err);
    }
  }
  if (warnings.length > 0) {
    const ok = confirm(
      `Advertencia — ATP insuficiente para:\n\n${warnings.join('\n')}\n\n` +
      `Al aceptar, FLOW-03 creará requerimientos condicionales automáticos.\n\n¿Continuar?`,
    );
    if (!ok) return;
  }
  return actions.handleEstadoChange(nuevo);
};
```

Pasar `onEstadoChange={handleEstadoChangeWithValidation}` al `PresupuestoMetadataStrip`.

Importar `computeStockAmplio` desde `../../services/stockAmplioService`.

**usePresupuestoEdit.ts:**

- En `save()`, el payload a Firestore ya es un `...form` — verificar que `ventasMetadata` está incluido (campo del form state). Si el hook usa una whitelist explícita de campos, agregar `ventasMetadata` a esa lista.
- En `load()` + `onSnapshot`, cuando se parsea el doc, asegurarse que `ventasMetadata` se propaga al form state (probablemente ya lo hace via `{ ...docData }`; verificar).
- Si `form` es un `Partial<Presupuesto>` type, el campo nuevo hereda automáticamente de Wave 10-01.
- Use `grep -n "form\s*=\|setForm\|initialForm" apps/sistema-modular/src/hooks/usePresupuestoEdit.ts` para localizar el shape.
  </action>
  <verify>
    <automated>pnpm --filter sistema-modular type-check 2>&1 | tail -20</automated>
  </verify>
  <done>
- VentasMetadataSection renderea cuando tipo === 'ventas'
- AddItemModal recibe articulos prop y renderea picker
- onEstadoChange intercepta + valida con confirm dialog
- usePresupuestoEdit persiste ventasMetadata
- type-check pasa
- EditPresupuestoModal permanece ≤ 250 líneas (hoy ~463 — está sobre budget; si pasa 500 en esta task, abrir TODO de extracción a hook, NO bloquear Wave 2)
  </done>
</task>

</tasks>

<verification>
Manual smoke (en dev):
1. `pnpm dev:modular` → abrir /presupuestos → crear ppto tipo `partes` → abrir AddItemModal → verificar panel visible + StockAmplioIndicator al seleccionar artículo
2. Crear ppto `ventas` → verificar VentasMetadataSection visible; completar campos; guardar; recargar → valores persisten
3. Aceptar ppto partes con artículo sin stock → confirm dialog aparece; cancelar → estado no cambia; aceptar → estado cambia + FLOW-03 crea requerimiento

E2E (desfixmea 3.5, 3.6, 3.7 del spec Wave 0):
`pnpm --filter sistema-modular e2e -- circuits/03-presupuestos.spec.ts -g "3\.[567]"`
</verification>

<success_criteria>
- Gap crítico de RESEARCH cerrado: partes/mixto/ventas pueden vincular items a stock
- StockAmplioIndicator visible al seleccionar artículo (cierre loop Phase 9)
- VentasMetadata persiste + se rehidrata
- Validación UX-only funciona sin bloquear aceptación
- Tests 3.5/3.6/3.7 ya no fallan por "picker missing"
- Commits atómicos por task (3 commits)
</success_criteria>

<output>
Crear `.planning/phases/10-presupuestos-partes-mixto-ventas/10-02-SUMMARY.md` con:
- Archivos agregados + líneas
- Archivos modificados + líneas delta
- Decisiones de UX (layout VentasMetadataSection, confirm dialog wording)
- Notas sobre 250-line budget en EditPresupuestoModal
</output>
