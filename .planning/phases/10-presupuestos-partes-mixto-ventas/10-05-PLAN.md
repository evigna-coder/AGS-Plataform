---
phase: 10-presupuestos-partes-mixto-ventas
plan: 05
type: execute
wave: 4
depends_on: [10-01, 10-04]
files_modified:
  - apps/sistema-modular/src/utils/exportToExcel.ts
  - apps/sistema-modular/src/utils/exportToPDF.tsx
  - apps/sistema-modular/src/utils/exports/exportPresupuestos.ts
  - apps/sistema-modular/src/utils/exports/exportOCsPendientes.ts
  - apps/sistema-modular/src/utils/exports/exportSolicitudesFacturacion.ts
  - apps/sistema-modular/src/utils/exportVentasInsumosExcel.ts
  - apps/sistema-modular/src/pages/presupuestos/PresupuestosList.tsx
autonomous: true
requirements: [FMT-04, FMT-05, FMT-06]

must_haves:
  truths:
    - "Helper genérico exportToExcel({ data, columns, sheetName, filename }) existe y genera .xlsx plain strip (headers bold, sin fill teal, sin logos, primera fila freezada)"
    - "Helper genérico exportToPDF({ data, columns, title, subtitle, filename, orientation }) existe y usa @react-pdf/renderer para tabla simple"
    - "exportVentasInsumosExcel.ts migrado al pattern plain (sin teal fill) usando el helper genérico — BREAK VISUAL esperado mínimo y documentado"
    - "3 wrappers específicos (exportPresupuestos/exportOCsPendientes/exportSolicitudesFacturacion) exportan funciones {xxxExcel, xxxPDF} con firma (rows, filters) → void"
    - "PresupuestosList.tsx tiene botones 'Exportar Excel' + 'Exportar PDF' en toolbar, gated por hasRole('admin', 'admin_soporte'), filter-aware (toman los filters actuales del useUrlFilters)"
    - "Filtro 'OCs pendientes' = presupuestos con estado='aceptado' + sin ordenesCompraIds (o con OC cargada pero sin marca de recibida — a definir en task)"
    - "Freeze de header row funciona en Excel real — usa tanto `!views` (xlsx free edition) como `!freeze` (legacy compat)"
  artifacts:
    - path: "apps/sistema-modular/src/utils/exportToExcel.ts"
      provides: "Helper genérico con signature exportToExcel<T>(opts)"
      min_lines: 60
    - path: "apps/sistema-modular/src/utils/exportToPDF.tsx"
      provides: "Helper genérico con ExportDocument wrapper de @react-pdf/renderer"
      min_lines: 100
    - path: "apps/sistema-modular/src/utils/exports/exportPresupuestos.ts"
      provides: "exportPresupuestosExcel/PDF — 12 columnas desde CONTEXT"
    - path: "apps/sistema-modular/src/utils/exports/exportOCsPendientes.ts"
      provides: "exportOCsPendientesExcel/PDF — 8 columnas: Número OC | Cliente | Presupuesto(s) | Fecha OC | Estado ppto | Adjuntos | Días desde carga | Coordinador asignado"
    - path: "apps/sistema-modular/src/utils/exports/exportSolicitudesFacturacion.ts"
      provides: "exportSolicitudesExcel/PDF — 10 columnas: Número OT | Ppto | Cliente | Total | Moneda | Fecha cierre admin | Estado | Facturada por | Fecha facturación | Nota"
    - path: "apps/sistema-modular/src/pages/presupuestos/PresupuestosList.tsx"
      provides: "2 botones Export Excel/PDF + filtro OC pendiente + useAuth gating"
  key_links:
    - from: "exportToExcel.ts"
      to: "xlsx library"
      via: "import * as XLSX from 'xlsx'; XLSX.utils.aoa_to_sheet + book_new + writeFile"
      pattern: "XLSX\\.writeFile"
    - from: "exportToPDF.tsx"
      to: "@react-pdf/renderer"
      via: "Document + Page + View + Text + pdf().toBlob() + URL.createObjectURL download"
      pattern: "pdf\\(.*\\)\\.toBlob"
    - from: "PresupuestosList.tsx"
      to: "exportPresupuestosExcel / exportPresupuestosPDF"
      via: "toolbar buttons onClick handler pasa filtradas + filters"
      pattern: "exportPresupuestosExcel\\|exportPresupuestosPDF"
    - from: "PresupuestosList.tsx"
      to: "useAuth().hasRole('admin', 'admin_soporte')"
      via: "conditional render de los 2 botones"
      pattern: "hasRole\\('admin'"
---

<objective>
Wave 4 — Export helpers + integración en 3 list pages. Expandido del CONTEXT: **AMBOS formatos (XLSX + PDF) para los 3 exports**. Sigue el pattern descubierto en RESEARCH §5.

Purpose: FMT-04/05/06 cierran el circuito operativo de reconciliación con Bejerman. Exportable desde cada list page filter-aware. Los helpers genéricos son reusables — cuando aparezcan más exports en el futuro, solo hay que escribir wrappers.

Output: 2 helpers genéricos + 3 wrappers específicos + migración del existing `exportVentasInsumosExcel.ts` a plain (consistencia) + integración en PresupuestosList (los otros 2 consumers se integran: facturación en 10-06; OCs pendientes via filter en PresupuestosList aquí mismo).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-CONTEXT.md
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-RESEARCH.md

@apps/sistema-modular/src/utils/exportVentasInsumosExcel.ts
@apps/sistema-modular/src/components/presupuestos/pdf/PresupuestoPDFEstandar.tsx
@apps/sistema-modular/src/components/presupuestos/pdf/pdfFonts.ts
@apps/sistema-modular/src/components/presupuestos/pdf/pdfStyles.ts
@apps/sistema-modular/src/pages/presupuestos/PresupuestosList.tsx

<interfaces>
<!-- Reuse targets -->

From xlsx lib (installed ^0.18.5):
```typescript
import * as XLSX from 'xlsx';
XLSX.utils.aoa_to_sheet(aoa)  // array-of-arrays → sheet
XLSX.utils.book_new() + book_append_sheet + XLSX.writeFile  // download
// Freeze header row — xlsx free edition:
ws['!views'] = [{ state: 'frozen', ySplit: 1 }];
// Legacy compat (some older parsers honor this):
ws['!freeze'] = { xSplit: 0, ySplit: 1 };
```

From @react-pdf/renderer (installed, used in PresupuestoPDFEstandar):
```typescript
import { Document, Page, View, Text, pdf } from '@react-pdf/renderer';
const blob = await pdf(<Document>...</Document>).toBlob();
```

From pdfFonts.ts (side-effect registering fonts — import it to ensure fonts available):
```typescript
import '../components/presupuestos/pdf/pdfFonts';
```

From useAuth (existing AuthContext):
```typescript
const { hasRole } = useAuth();
hasRole('admin', 'admin_soporte')  // boolean
```

From Presupuesto (@ags/shared):
```typescript
interface Presupuesto {
  numero, tipo, estado, moneda, total, clienteId, responsableId, responsableNombre,
  createdAt, fechaEnvio, validezDias, ordenesCompraIds, ordenCompraNumero,
  proximoContacto, ...
}
```

From SolicitudFacturacion (@ags/shared post-Wave 10-01):
```typescript
interface SolicitudFacturacion {
  id, presupuestoNumero, clienteNombre, items, montoTotal, moneda, estado,
  otNumbers, ordenesCompraIds, numeroFactura, fechaFactura, observaciones,
  facturadoPor, facturadoPorNombre, enviadaAt, createdAt, updatedAt, ...
}
```

From exportVentasInsumosExcel.ts (existing template — migrate to plain):
- Headers array + XLSX.utils.aoa_to_sheet
- !cols array con {wch}
- !freeze en {xSplit: 0, ySplit: 1} for first row freeze
- Filename pattern: `${slug}_${range}_${fecha}.xlsx`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helpers genéricos exportToExcel.ts + exportToPDF.tsx + migrate exportVentasInsumosExcel.ts</name>
  <files>apps/sistema-modular/src/utils/exportToExcel.ts, apps/sistema-modular/src/utils/exportToPDF.tsx, apps/sistema-modular/src/utils/exportVentasInsumosExcel.ts</files>
  <behavior>
    - exportToExcel<T>({ data, columns, sheetName, filename, freezeHeader }): genera .xlsx con headers bold, sin fill teal, auto-width columns, primera fila freezada; descarga via XLSX.writeFile
    - Freeze header usa AMBOS `!views` (xlsx free edition real syntax) + `!freeze` (legacy compat) — W8 fix
    - exportToPDF<T>({ data, columns, title, subtitle, filename, orientation }): genera .pdf con tabla simple, header con title/subtitle + "Generado: dd/mm/yyyy hh:mm — N registros"; descarga via URL.createObjectURL
    - Column type ExportColumn<T> con: header, width (px o %), get(row): string|number|null, align?
    - exportVentasInsumosExcel.ts migrado: usa exportToExcel genérico; la sección "manual" (columnas post-AUTO_HEADERS) queda vacía (rellenar a mano en Excel) sin la fill yellow — plain consistency per CONTEXT
    - Pre-migración: grep de callers para verificar que la nueva firma `(rows, range)` no rompe contratos (W9 fix)
  </behavior>
  <action>
**W9 — Pre-step: audit callers de exportVentasInsumosExcel antes de migrar:**

```bash
grep -rn "exportVentasInsumosExcel" apps/
```

Verificar que todos los callers pasan la firma `(rows: VentasInsumosReportRow[], range: VentasInsumosRangeLabel)` sin cambios. Si alguno pasa args distintos o espera retorno distinto, fix en el mismo commit. La firma pública NO debe cambiar — solo la implementación interna (styling plain).

**1. `apps/sistema-modular/src/utils/exportToExcel.ts`** (~90 líneas, no JSX):

```typescript
import * as XLSX from 'xlsx';

export interface ExportColumn<T> {
  header: string;
  width?: number;  // character width
  get: (row: T) => string | number | null;
  align?: 'left' | 'center' | 'right';
}

export interface ExportToExcelOptions<T> {
  data: T[];
  columns: ExportColumn<T>[];
  sheetName: string;
  filename: string;  // sin extensión .xlsx
  freezeHeader?: boolean;  // default true
}

export function exportToExcel<T>(opts: ExportToExcelOptions<T>): void {
  const { data, columns, sheetName, filename, freezeHeader = true } = opts;

  const headers = columns.map(c => c.header);
  const aoa: (string | number | null)[][] = [headers];
  for (const row of data) {
    aoa.push(columns.map(c => c.get(row)));
  }

  const ws = XLSX.utils.aoa_to_sheet(aoa);

  // Column widths
  ws['!cols'] = columns.map(c => ({ wch: c.width ?? Math.max(12, c.header.length + 2) }));

  // Header styling — bold only (NO teal fill — Phase 10 plain strip per CONTEXT)
  for (let c = 0; c < headers.length; c++) {
    const addr = XLSX.utils.encode_cell({ r: 0, c });
    if (ws[addr]) {
      ws[addr].s = {
        font: { bold: true },
        alignment: { horizontal: 'center', vertical: 'center' },
      };
    }
  }

  if (freezeHeader) {
    // W8 fix — xlsx free edition uses !views (frozen panes with ySplit).
    // !freeze is legacy for some older parsers; set both for max compat.
    // Verify in real Excel: open generated file + confirm first row stays pinned on scroll.
    ws['!views'] = [{ state: 'frozen', ySplit: 1 } as any];
    // @ts-expect-error — !freeze is a valid runtime prop but not in the type (legacy compat)
    ws['!freeze'] = { xSplit: 0, ySplit: 1 };
  }

  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, sheetName);
  XLSX.writeFile(wb, `${filename}.xlsx`);
}

/** Utility: formato consistente de fecha corta para exports (es-AR dd/mm/yy). */
export function fmtDateShort(iso: string | null | undefined): string {
  if (!iso) return '—';
  try {
    const d = new Date(iso);
    if (isNaN(d.getTime())) return '—';
    return d.toLocaleDateString('es-AR', { day: '2-digit', month: '2-digit', year: '2-digit' });
  } catch { return '—'; }
}
```

**2. `apps/sistema-modular/src/utils/exportToPDF.tsx`** (~120 líneas, TSX):

```tsx
import { Document, Page, View, Text, pdf } from '@react-pdf/renderer';
import '../components/presupuestos/pdf/pdfFonts';  // side-effect — registra fonts

export interface ExportPDFColumn<T> {
  header: string;
  width: string | number;  // '20%' o px
  get: (row: T) => string;
  align?: 'left' | 'center' | 'right';
}

export interface ExportToPDFOptions<T> {
  data: T[];
  columns: ExportPDFColumn<T>[];
  title: string;
  subtitle?: string;
  filename: string;  // sin .pdf
  orientation?: 'portrait' | 'landscape';  // default 'landscape' para tablas anchas
}

export async function exportToPDF<T>(opts: ExportToPDFOptions<T>): Promise<void> {
  const blob = await pdf(<ExportDocument {...opts} />).toBlob();
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = `${opts.filename}.pdf`;
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
  URL.revokeObjectURL(url);
}

function ExportDocument<T>({
  data, columns, title, subtitle, orientation = 'landscape',
}: ExportToPDFOptions<T>) {
  const fmtGen = new Date().toLocaleString('es-AR');
  return (
    <Document title={title}>
      <Page size="A4" orientation={orientation} style={{ padding: 20, fontFamily: 'Helvetica' }}>
        <Text style={{ fontSize: 14, fontWeight: 700, marginBottom: 4 }}>{title}</Text>
        {subtitle && (
          <Text style={{ fontSize: 9, color: '#666', marginBottom: 6 }}>{subtitle}</Text>
        )}
        <Text style={{ fontSize: 8, color: '#999', marginBottom: 10 }}>
          Generado: {fmtGen} — {data.length} registro{data.length === 1 ? '' : 's'}
        </Text>

        {/* Table header */}
        <View style={{
          flexDirection: 'row', borderBottomWidth: 1, borderColor: '#0D6E6E', paddingVertical: 3,
        }}>
          {columns.map((c, i) => (
            <Text key={i} style={{
              width: c.width, fontSize: 8, fontWeight: 700, textAlign: c.align || 'left', paddingHorizontal: 2,
            }}>
              {c.header}
            </Text>
          ))}
        </View>

        {/* Table rows */}
        {data.map((row, i) => (
          <View key={i} style={{
            flexDirection: 'row', paddingVertical: 2, borderBottomWidth: 0.5, borderColor: '#eee',
          }}>
            {columns.map((c, j) => (
              <Text key={j} style={{
                width: c.width, fontSize: 7, textAlign: c.align || 'left', paddingHorizontal: 2,
              }}>
                {c.get(row)}
              </Text>
            ))}
          </View>
        ))}
      </Page>
    </Document>
  );
}
```

**3. Migrar `apps/sistema-modular/src/utils/exportVentasInsumosExcel.ts`:**

Reescribir el file usando el helper genérico. PRESERVAR la firma pública `(rows, range)` — solo cambia la implementación.

```typescript
import type { VentasInsumosReportRow, VentasInsumosRangeLabel } from './ventasInsumosReport';
import { fmtDateShort as fmtDate } from './ventasInsumosReport';  // keep existing
import { exportToExcel, type ExportColumn } from './exportToExcel';

export function exportVentasInsumosExcel(
  rows: VentasInsumosReportRow[],
  range: VentasInsumosRangeLabel,
): void {
  // Auto columns — derivadas del ticket
  const autoColumns: ExportColumn<VentasInsumosReportRow>[] = [
    { header: 'Ticket', width: 10, get: r => r.ticketId.slice(-6).toUpperCase() },
    { header: 'Fecha creación', width: 12, get: r => fmtDate(r.fechaCreacion) },
    // ... etc
  ];
  // Manual columns — quedan vacías, rellenar a mano (sin fill yellow ahora, plain consistency)
  const manualColumns: ExportColumn<VentasInsumosReportRow>[] = [
    { header: 'N° Presupuesto', width: 15, get: () => '' },
    { header: 'Monto final', width: 14, get: () => '' },
    // ...
  ];

  const filename = `ventas-insumos_${range.label.replace(/\s+/g, '-').toLowerCase()}_${fmtDate(new Date().toISOString()).replace(/\//g, '-')}`;
  exportToExcel({
    data: rows,
    columns: [...autoColumns, ...manualColumns],
    sheetName: 'Ventas Insumos',
    filename,
  });
}
```

**BREAK VISUAL esperado:**
- Los headers ya no tienen fill teal (solo bold)
- Las columnas manuales ya no tienen fill yellow
- La consistencia con los nuevos exports (Phase 10 plain) es el goal

Documentar el break en el SUMMARY. La firma pública queda intacta → callers no cambian.
  </action>
  <verify>
    <automated>pnpm --filter sistema-modular type-check 2>&1 | grep -E "exportToExcel|exportToPDF" | head -10</automated>
    <manual>Abrir el .xlsx generado en Excel real — NO en un preview web. Scroll verticalmente y verificar que la primera fila (headers) permanece pinned. Si no se freeza, `!views` syntax falló y hay que investigar versión de xlsx. W8 verification.</manual>
  </verify>
  <done>
- 3 archivos compilan; exports públicos con firmas correctas
- Migration preserve la data (solo styling difiere)
- Callers de exportVentasInsumosExcel NO rotas (audit W9 pasó antes de migrar)
- Freeze header verificado en Excel real (W8 fallback funciona)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wrappers específicos — exportPresupuestos + exportOCsPendientes + exportSolicitudesFacturacion</name>
  <files>apps/sistema-modular/src/utils/exports/exportPresupuestos.ts, apps/sistema-modular/src/utils/exports/exportOCsPendientes.ts, apps/sistema-modular/src/utils/exports/exportSolicitudesFacturacion.ts</files>
  <action>
Crear nuevo directorio `apps/sistema-modular/src/utils/exports/` con 3 archivos. Cada uno exporta 2 funciones: `xxxExcel` y `xxxPDF` con firma `(rows, meta)` donde `meta` incluye filter labels para incluir en el subtitle.

**`exportPresupuestos.ts` (FMT-04) — 12 columnas per CONTEXT:**

```typescript
import type { Presupuesto, Cliente, UsuarioAGS } from '@ags/shared';
import { MONEDA_SIMBOLO, TIPO_PRESUPUESTO_LABELS, ESTADO_PRESUPUESTO_LABELS } from '@ags/shared';
import { exportToExcel, fmtDateShort, type ExportColumn } from '../exportToExcel';
import { exportToPDF, type ExportPDFColumn } from '../exportToPDF';

export interface PresupuestoExportRow {
  presupuesto: Presupuesto;
  clienteNombre: string;
  responsableNombre: string;
}

export interface ExportFiltersMeta {
  /** Etiqueta resumida de filtros activos, ej. "cliente=ACME, estado=aceptado". */
  filtrosLabel?: string;
}

const buildColumns = () => [
  { key: 'numero', header: 'Número', width: 15, get: (r: PresupuestoExportRow) => r.presupuesto.numero },
  { key: 'cliente', header: 'Cliente', width: 28, get: (r) => r.clienteNombre },
  { key: 'tipo', header: 'Tipo', width: 10, get: (r) => TIPO_PRESUPUESTO_LABELS[r.presupuesto.tipo] || r.presupuesto.tipo },
  { key: 'estado', header: 'Estado', width: 12, get: (r) => ESTADO_PRESUPUESTO_LABELS[r.presupuesto.estado] || r.presupuesto.estado },
  { key: 'total', header: 'Total', width: 12, get: (r) => r.presupuesto.total ?? 0, align: 'right' as const },
  { key: 'moneda', header: 'Moneda', width: 8, get: (r) => r.presupuesto.moneda },
  { key: 'responsable', header: 'Responsable', width: 20, get: (r) => r.responsableNombre },
  { key: 'creado', header: 'Creado', width: 10, get: (r) => fmtDateShort(r.presupuesto.createdAt) },
  { key: 'enviado', header: 'Enviado', width: 10, get: (r) => fmtDateShort(r.presupuesto.fechaEnvio ?? null) },
  { key: 'validez', header: 'Validez (días)', width: 10, get: (r) => r.presupuesto.validezDias ?? '' },
  { key: 'ocs', header: 'OCs vinculadas', width: 18, get: (r) => (r.presupuesto.ordenesCompraIds || []).length },
  { key: 'proximo', header: 'Próx. contacto', width: 12, get: (r) => fmtDateShort(r.presupuesto.proximoContacto ?? null) },
];

export function exportPresupuestosExcel(rows: PresupuestoExportRow[], meta?: ExportFiltersMeta): void {
  const cols = buildColumns();
  exportToExcel({
    data: rows,
    columns: cols.map(c => ({ header: c.header, width: c.width, get: c.get, align: c.align })),
    sheetName: 'Presupuestos',
    filename: buildFilename('presupuestos', meta),
  });
}

export function exportPresupuestosPDF(rows: PresupuestoExportRow[], meta?: ExportFiltersMeta): Promise<void> {
  const cols = buildColumns();
  const pdfCols: ExportPDFColumn<PresupuestoExportRow>[] = cols.map(c => ({
    header: c.header,
    width: `${Math.round((c.width / 170) * 100)}%`,  // normaliza suma ~100%
    get: (r) => String(c.get(r) ?? ''),
    align: c.align,
  }));
  return exportToPDF({
    data: rows,
    columns: pdfCols,
    title: 'Presupuestos',
    subtitle: meta?.filtrosLabel || 'Todos los presupuestos',
    filename: buildFilename('presupuestos', meta),
    orientation: 'landscape',
  });
}

function buildFilename(slug: string, meta?: ExportFiltersMeta): string {
  const date = new Date().toISOString().slice(0, 10);  // yyyy-mm-dd
  return `${slug}_${date}`;
}
```

**`exportOCsPendientes.ts` (FMT-05) — 8 columnas per CONTEXT lines 135-137:**

Columnas exactas: **Número OC | Cliente | Presupuesto(s) | Fecha OC | Estado ppto | Adjuntos | Días desde carga | Coordinador asignado**

Input: derivado de `PresupuestoExportRow` (filtrado en el caller por `ordenesCompraIds.length > 0 && estado !== 'oc_recibida'` O `estado === 'aceptado'` sin ordenesCompraIds — criterio definido por el filter del caller; este wrapper no re-filtra, solo renderea).

```typescript
import type { Presupuesto } from '@ags/shared';
import { ESTADO_PRESUPUESTO_LABELS } from '@ags/shared';
import { exportToExcel, fmtDateShort, type ExportColumn } from '../exportToExcel';
import { exportToPDF, type ExportPDFColumn } from '../exportToPDF';

export interface OCPendienteExportRow {
  presupuesto: Presupuesto;
  clienteNombre: string;
  /** Primer OC del array (o 'N/A' si no hay cargada pero el filter la marcó como pendiente de cargar). */
  ocNumero: string;
  /** ISO del primer OC recibido (o null si no hay). */
  ocFecha: string | null;
  /** Count de adjuntos (OC PDF + otros). */
  adjuntosCount: number;
  /** Días desde createdAt del primer OC (o desde estado='aceptado' si no hay OC aún). */
  diasDesdeCarga: number;
  coordinadorNombre: string;
}

const buildColumns = () => [
  { key: 'ocNumero', header: 'Número OC', width: 14, get: (r: OCPendienteExportRow) => r.ocNumero },
  { key: 'cliente', header: 'Cliente', width: 28, get: (r) => r.clienteNombre },
  { key: 'ppto', header: 'Presupuesto(s)', width: 16, get: (r) => r.presupuesto.numero },
  { key: 'fechaOC', header: 'Fecha OC', width: 10, get: (r) => fmtDateShort(r.ocFecha) },
  { key: 'estadoPpto', header: 'Estado ppto', width: 12, get: (r) => ESTADO_PRESUPUESTO_LABELS[r.presupuesto.estado] || r.presupuesto.estado },
  { key: 'adjuntos', header: 'Adjuntos', width: 8, get: (r) => r.adjuntosCount, align: 'center' as const },
  { key: 'dias', header: 'Días desde carga', width: 12, get: (r) => r.diasDesdeCarga, align: 'right' as const },
  { key: 'coord', header: 'Coordinador asignado', width: 20, get: (r) => r.coordinadorNombre },
];

export function exportOCsPendientesExcel(rows: OCPendienteExportRow[], meta?: ExportFiltersMeta): void {
  const cols = buildColumns();
  exportToExcel({
    data: rows,
    columns: cols.map(c => ({ header: c.header, width: c.width, get: c.get, align: c.align })),
    sheetName: 'OCs Pendientes',
    filename: buildFilename('ocs-pendientes', meta),
  });
}

export function exportOCsPendientesPDF(rows: OCPendienteExportRow[], meta?: ExportFiltersMeta): Promise<void> {
  const cols = buildColumns();
  const totalW = cols.reduce((a, c) => a + c.width, 0);
  const pdfCols: ExportPDFColumn<OCPendienteExportRow>[] = cols.map(c => ({
    header: c.header,
    width: `${Math.round((c.width / totalW) * 100)}%`,
    get: (r) => String(c.get(r) ?? ''),
    align: c.align,
  }));
  return exportToPDF({
    data: rows,
    columns: pdfCols,
    title: 'OCs Pendientes',
    subtitle: meta?.filtrosLabel || 'OCs cargadas sin recibir / aceptados sin OC',
    filename: buildFilename('ocs-pendientes', meta),
    orientation: 'landscape',
  });
}

export interface ExportFiltersMeta { filtrosLabel?: string; }
function buildFilename(slug: string, meta?: ExportFiltersMeta): string {
  const date = new Date().toISOString().slice(0, 10);
  return `${slug}_${date}`;
}
```

**`exportSolicitudesFacturacion.ts` (FMT-06) — 10 columnas per CONTEXT lines 135-137:**

Columnas exactas: **Número OT | Ppto | Cliente | Total | Moneda | Fecha cierre admin | Estado | Facturada por | Fecha facturación | Nota**

Input: `SolicitudFacturacion[]`.

```typescript
import type { SolicitudFacturacion } from '@ags/shared';
import { SOLICITUD_FACTURACION_ESTADO_LABELS } from '@ags/shared';
import { exportToExcel, fmtDateShort, type ExportColumn } from '../exportToExcel';
import { exportToPDF, type ExportPDFColumn } from '../exportToPDF';

const buildColumns = () => [
  { key: 'ot', header: 'Número OT', width: 12, get: (s: SolicitudFacturacion) => (s.otNumbers || [])[0] || '—' },
  { key: 'ppto', header: 'Ppto', width: 14, get: (s) => s.presupuestoNumero },
  { key: 'cliente', header: 'Cliente', width: 26, get: (s) => s.clienteNombre },
  { key: 'total', header: 'Total', width: 12, get: (s) => s.montoTotal, align: 'right' as const },
  { key: 'moneda', header: 'Moneda', width: 8, get: (s) => s.moneda },
  { key: 'fechaCierre', header: 'Fecha cierre admin', width: 14, get: (s) => fmtDateShort(s.createdAt as any) },
  { key: 'estado', header: 'Estado', width: 12, get: (s) => SOLICITUD_FACTURACION_ESTADO_LABELS[s.estado] || s.estado },
  { key: 'facturadaPor', header: 'Facturada por', width: 18, get: (s) => s.facturadoPorNombre || '—' },
  { key: 'fechaFact', header: 'Fecha facturación', width: 14, get: (s) => fmtDateShort(s.fechaFactura ?? null) },
  { key: 'nota', header: 'Nota', width: 30, get: (s) => s.observaciones || '' },
];

export function exportSolicitudesExcel(rows: SolicitudFacturacion[], meta?: ExportFiltersMeta): void {
  const cols = buildColumns();
  exportToExcel({
    data: rows,
    columns: cols.map(c => ({ header: c.header, width: c.width, get: c.get, align: c.align })),
    sheetName: 'Solicitudes Facturación',
    filename: buildFilename('solicitudes-facturacion', meta),
  });
}

export function exportSolicitudesPDF(rows: SolicitudFacturacion[], meta?: ExportFiltersMeta): Promise<void> {
  const cols = buildColumns();
  const totalW = cols.reduce((a, c) => a + c.width, 0);
  const pdfCols: ExportPDFColumn<SolicitudFacturacion>[] = cols.map(c => ({
    header: c.header,
    width: `${Math.round((c.width / totalW) * 100)}%`,
    get: (r) => String(c.get(r) ?? ''),
    align: c.align,
  }));
  return exportToPDF({
    data: rows,
    columns: pdfCols,
    title: 'Solicitudes de Facturación',
    subtitle: meta?.filtrosLabel || 'Todas las solicitudes',
    filename: buildFilename('solicitudes-facturacion', meta),
    orientation: 'landscape',
  });
}

export interface ExportFiltersMeta { filtrosLabel?: string; }
function buildFilename(slug: string, meta?: ExportFiltersMeta): string {
  const date = new Date().toISOString().slice(0, 10);
  return `${slug}_${date}`;
}
```

Budget: ~80-100 líneas cada uno.
  </action>
  <verify>
    <automated>pnpm --filter sistema-modular type-check 2>&1 | grep -E "exportPresupuestos|exportOCs|exportSolicitudes" | head -10</automated>
  </verify>
  <done>3 wrappers creados con Excel + PDF versions; columnas exactas per CONTEXT; type-check pasa.</done>
</task>

<task type="auto">
  <name>Task 3: Integrar exports en PresupuestosList + filtro OCs pendientes</name>
  <files>apps/sistema-modular/src/pages/presupuestos/PresupuestosList.tsx</files>
  <action>
En `apps/sistema-modular/src/pages/presupuestos/PresupuestosList.tsx`:

**1. Importar helpers + useAuth:**

```typescript
import { useAuth } from '../../contexts/AuthContext';
import { exportPresupuestosExcel, exportPresupuestosPDF, type PresupuestoExportRow } from '../../utils/exports/exportPresupuestos';
import { exportOCsPendientesExcel, exportOCsPendientesPDF } from '../../utils/exports/exportOCsPendientes';
```

**2. Agregar hook al top del componente:**

```typescript
const { hasRole } = useAuth();
const canExport = hasRole('admin', 'admin_soporte');
```

**3. Agregar un filter flag "OCs pendientes"** al FILTER_SCHEMA (línea 63):

```typescript
const FILTER_SCHEMA = useMemo(() => ({
  // ... existing fields ...
  ocPendiente: { type: 'boolean' as const, default: false },
}), []);
```

Y en la filter toolbar (línea ~100-200 aprox), agregar un checkbox/toggle "OCs pendientes" que setea el filter. Usar mismo patrón que otros filtros.

**4. Adaptar el filter logic (línea ~120+) para considerar ocPendiente:**

```typescript
const filtradas = useMemo(() => {
  let result = presupuestos.filter(p => {
    // ... filtros existentes ...
    if (filters.ocPendiente) {
      // Criterio: ppto aceptado SIN OCs cargadas (esperando OC del cliente)
      if (p.estado !== 'aceptado') return false;
      if ((p.ordenesCompraIds || []).length > 0) return false;
    }
    return true;
  });
  // ... sort + return
}, [...]);
```

**5. Agregar 2 botones en la toolbar** (junto al "+ Nuevo Presupuesto" button, línea ~300 aprox):

```tsx
{canExport && (
  <>
    <Button
      variant="outline"
      size="sm"
      onClick={() => {
        const rows: PresupuestoExportRow[] = filtradas.map(p => ({
          presupuesto: p,
          clienteNombre: clientes.find(c => c.id === p.clienteId)?.razonSocial || '—',
          responsableNombre: usuarios.find(u => u.id === p.responsableId)?.nombre || p.responsableNombre || '—',
        }));
        const filtrosLabel = [
          filters.cliente && `cliente=${clientes.find(c => c.id === filters.cliente)?.razonSocial}`,
          filters.estado && `estado=${filters.estado}`,
          filters.tipo && `tipo=${filters.tipo}`,
          filters.ocPendiente && 'OCs pendientes',
        ].filter(Boolean).join(', ') || 'Sin filtros';
        if (filters.ocPendiente) {
          exportOCsPendientesExcel(rows as any, { filtrosLabel });
        } else {
          exportPresupuestosExcel(rows, { filtrosLabel });
        }
      }}
    >
      Exportar Excel
    </Button>
    <Button
      variant="outline"
      size="sm"
      onClick={async () => {
        const rows: PresupuestoExportRow[] = filtradas.map(p => ({
          presupuesto: p,
          clienteNombre: clientes.find(c => c.id === p.clienteId)?.razonSocial || '—',
          responsableNombre: usuarios.find(u => u.id === p.responsableId)?.nombre || p.responsableNombre || '—',
        }));
        const filtrosLabel = /* idem arriba */;
        if (filters.ocPendiente) {
          await exportOCsPendientesPDF(rows as any, { filtrosLabel });
        } else {
          await exportPresupuestosPDF(rows, { filtrosLabel });
        }
      }}
    >
      Exportar PDF
    </Button>
  </>
)}
```

**Budget:** PresupuestosList.tsx está grande ya — antes de crecer más, si el file cruza 450 líneas por estas adiciones, extraer el builder de rows + filtrosLabel a un helper interno `usePresupuestoExports(filtradas, filters, clientes, usuarios)`.

**NO tocar:** other list pages (la integración del export en /facturacion vive en 10-06 para mantener scope de 10-05 enfocado en helpers + un consumer).
  </action>
  <verify>
    <automated>pnpm --filter sistema-modular type-check 2>&1 | grep -E "PresupuestosList" | head -10; wc -l apps/sistema-modular/src/pages/presupuestos/PresupuestosList.tsx</automated>
  </verify>
  <done>
- 2 botones Export visibles para admin/admin_soporte
- Filtro ocPendiente añadido al schema + checkbox en toolbar
- Descargas .xlsx + .pdf funcionan al click
- Filter-aware: subtitle del PDF refleja filtros actuales
  </done>
</task>

</tasks>

<verification>
**Automated:**
- `pnpm --filter sistema-modular type-check` pasa
- E2E: `pnpm --filter sistema-modular e2e -- circuits/14-exports.spec.ts` — tests 14.1, 14.2 deberían desfixmear (download events capturan)

**Manual:**
- `pnpm dev:modular` → login admin → /presupuestos → click "Exportar Excel" → verificar que .xlsx se descarga con headers bold + sin fill teal + 12 columnas
- **W8 critical:** abrir el .xlsx generado en **Excel real** (no LibreOffice preview) → scroll vertical → verificar que la primera row permanece pinned (freeze pane funciona)
- Click "Exportar PDF" → verificar .pdf landscape con título/subtitle/fecha + tabla
- Filtrar por cliente → Exportar → verificar que subtitle del PDF dice "cliente=X" y rows están filtradas
- Toggle "OCs pendientes" → Exportar → verifica que usa exportOCsPendientes* (columnas distintas: 8 cols)
- Login con rol `ventas` → verificar que botones NO están visibles
- Smoke `exportVentasInsumosExcel` — abrir /tickets o wherever lo use, exportar → verificar que el archivo se genera (sin fill yellow, sin fill teal) + callers no rotos (W9)
</verification>

<success_criteria>
- 2 helpers genéricos exportables desde `utils/`
- 3 wrappers específicos en `utils/exports/` con columnas exactas per CONTEXT (12 / 8 / 10)
- `exportVentasInsumosExcel.ts` migrado a plain strip sin romper callers (W9 audit pasó)
- Freeze header funciona en Excel real (W8 fallback con `!views` + `!freeze`)
- PresupuestosList integra ambos botones + filter OCs pendientes
- RBAC gating funciona
- Commit: `feat(10-05): export helpers genéricos XLSX+PDF + integración PresupuestosList`
</success_criteria>

<output>
Crear `.planning/phases/10-presupuestos-partes-mixto-ventas/10-05-SUMMARY.md` con:
- Archivos creados
- Pattern exportable para futuros consumers
- Decisión sobre criterio de "OC pendiente"
- Screenshots / examples de output (incluyendo verificación Excel real del freeze)
- Break visual de exportVentasInsumosExcel documentado
- Audit W9: lista de callers auditados + verificación de no-break
</output>
</content>
</invoke>