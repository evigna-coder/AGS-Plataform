---
phase: 10-presupuestos-partes-mixto-ventas
plan: 01
type: execute
wave: 1
depends_on: [10-00]
files_modified:
  - packages/shared/src/types/index.ts
autonomous: true
requirements: [PTYP-04, FMT-03]

must_haves:
  truths:
    - "`VentasMetadata` type exists in @ags/shared and is importable from sistema-modular"
    - "`Presupuesto.ventasMetadata?: VentasMetadata | null` field is declared"
    - "`SolicitudFacturacion.ordenesCompraIds?: string[] | null` is declared"
    - "`SolicitudFacturacion.enviadaAt?: string | null` is declared"
    - "`SolicitudFacturacionEstado` union includes `'enviada'` as intermediate state"
    - "`SOLICITUD_FACTURACION_ESTADO_LABELS` + `..._COLORS` cover `'enviada'` value"
    - "`PresupuestoPDFData` (in sistema-modular) is a passthrough — no type-level changes here"
  artifacts:
    - path: "packages/shared/src/types/index.ts"
      provides: "VentasMetadata interface + Presupuesto.ventasMetadata + SolicitudFacturacion extensions + enviada estado"
      contains: "export interface VentasMetadata"
  key_links:
    - from: "packages/shared/src/types/index.ts"
      to: "Presupuesto interface"
      via: "ventasMetadata?: VentasMetadata | null field"
      pattern: "ventasMetadata\\?:"
    - from: "packages/shared/src/types/index.ts"
      to: "SolicitudFacturacionEstado union"
      via: "adding 'enviada' literal"
      pattern: "'pendiente' \\| 'enviada' \\| 'facturada'"
---

<objective>
Wave 1 — Shared types that downstream plans (10-02 editor UI, 10-03 PDF, 10-04 services, 10-06 dashboard) will consume. Landing these first unblocks ALL parallel work.

Purpose: Single source of truth in `@ags/shared`. Lazy-landing types in downstream plans would force re-exports or `any` casts everywhere. Interface-first ordering (per GSD philosophy): contracts land before implementation.
Output: Extended `packages/shared/src/types/index.ts` with 3 additive changes — minimal surface, backward compat.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-CONTEXT.md
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-RESEARCH.md

<!-- Existing shape we extend. Do NOT rewrite. -->
@packages/shared/src/types/index.ts

<interfaces>
<!-- Current shapes we're extending. Lines referenced from types/index.ts: -->

Line 1129-1201: `export interface Presupuesto` — 70+ fields, we add ONE new optional field (`ventasMetadata`) adjacent to `contratoFechaInicio` / `contratoFechaFin` (lines 1186-1190).

Line 1254: `export type SolicitudFacturacionEstado = 'pendiente' | 'facturada' | 'cobrada' | 'anulada';`

Line 1256-1261: `SOLICITUD_FACTURACION_ESTADO_LABELS: Record<SolicitudFacturacionEstado, string>`
Line 1263-1269: `SOLICITUD_FACTURACION_ESTADO_COLORS: Record<SolicitudFacturacionEstado, string>`

Line 1280-1308: `export interface SolicitudFacturacion` — has `otNumbers?: string[] | null` and `observaciones?: string | null` already.

Convention followed from adjacent code:
- Optional scalar fields use `?: T | null` (e.g. `contratoFechaInicio?: string | null`)
- JSDoc comment describes when field applies (e.g. "Solo aplica para tipo === 'contrato'")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VentasMetadata interface + Presupuesto.ventasMetadata field</name>
  <files>packages/shared/src/types/index.ts</files>
  <action>
In `packages/shared/src/types/index.ts`:

1. **Before `export interface Presupuesto {` (line 1129)**, add the new interface:

```typescript
/**
 * Datos de entrega e instalación — solo aplica cuando `Presupuesto.tipo === 'ventas'`.
 * Se muestra en el editor en sección dedicada (`VentasMetadataSection`) y se renderiza
 * en el PDF antes del detalle de items. Todos los campos son opcionales para permitir
 * save incremental del borrador (el vendedor completa mientras negocia).
 *
 * Phase 10 (10-01). Precedente estructural: contratoFechaInicio / contratoFechaFin
 * agrupados bajo un sub-objeto en vez de contaminar el root de Presupuesto.
 */
export interface VentasMetadata {
  /** ISO date string — fecha estimada de entrega del equipo. */
  fechaEstimadaEntrega?: string | null;
  /** Dirección libre donde se instalará. Puede diferir del establecimiento declarado. */
  lugarInstalacion?: string | null;
  /** Si el cliente requiere entrenamiento post-instalación (bench/usuario). */
  requiereEntrenamiento?: boolean;
}
```

2. **Inside `Presupuesto` interface**, add field adjacent to `contratoFechaFin` (after line 1190, before `// --- Flujo Automático de Derivación (Phase 8) ---`):

```typescript
  /** Datos de entrega e instalación — solo aplica para tipo === 'ventas'. Ver VentasMetadata. */
  ventasMetadata?: VentasMetadata | null;
```

**Rationale:** sub-object grouping (not 3 root fields) — keeps root clean, semantic grouping, mirrors `contratoFechaInicio/Fin` pattern at same sharpness level.
  </action>
  <verify>
    <automated>pnpm --filter @ags/shared build 2>&1 | tail -20</automated>
  </verify>
  <done>`VentasMetadata` exportado; `Presupuesto.ventasMetadata` declarado; build pasa sin errores de TS.</done>
</task>

<task type="auto">
  <name>Task 2: Extend SolicitudFacturacion + SolicitudFacturacionEstado</name>
  <files>packages/shared/src/types/index.ts</files>
  <action>
Mismo archivo `packages/shared/src/types/index.ts`.

1. **Extender `SolicitudFacturacionEstado` union (línea 1254)** — agregar `'enviada'` como estado intermedio entre `'pendiente'` y `'facturada'`:

```typescript
export type SolicitudFacturacionEstado = 'pendiente' | 'enviada' | 'facturada' | 'cobrada' | 'anulada';
```

2. **Extender `SOLICITUD_FACTURACION_ESTADO_LABELS`** — agregar entrada `enviada`:

```typescript
export const SOLICITUD_FACTURACION_ESTADO_LABELS: Record<SolicitudFacturacionEstado, string> = {
  pendiente: 'Pendiente',
  enviada: 'Enviada',                   // nueva — mail al contable ya fue disparado
  facturada: 'Facturada',
  cobrada: 'Cobrada',
  anulada: 'Anulada',
};
```

3. **Extender `SOLICITUD_FACTURACION_ESTADO_COLORS`** — agregar color tailwind consistent con resto del sistema (Phase 8 estableció `bg-orange-200 text-orange-900` para `oc_recibida` = "acción pendiente del contable"):

```typescript
export const SOLICITUD_FACTURACION_ESTADO_COLORS: Record<SolicitudFacturacionEstado, string> = {
  pendiente: 'bg-amber-100 text-amber-800',
  enviada: 'bg-blue-100 text-blue-800',   // nueva — color neutro "en progreso"
  facturada: 'bg-green-100 text-green-800',
  cobrada: 'bg-teal-100 text-teal-800',
  anulada: 'bg-slate-100 text-slate-500',
};
```
**IMPORTANTE:** mantené los valores actuales de los otros estados (no los adivines — leé las líneas 1263-1269 y preservá exactamente los mismos className strings).

4. **Extender `SolicitudFacturacion` interface (línea 1280)** — agregar DOS campos nuevos al final del bloque "Audit" (línea 1305, antes de `createdAt`), agrupados con comment:

```typescript
  // --- Phase 10 (10-01) — aviso facturación automático desde cerrarAdministrativamente ---
  /** OCs del cliente vinculadas. Back-ref al momento del cierre admin — no se mantiene sync con el ppto. */
  ordenesCompraIds?: string[] | null;
  /** ISO timestamp cuando el mail al contable fue marcado como enviado (estado 'enviada'). */
  enviadaAt?: string | null;
```

NOTA: NO eliminar campos existentes. NO cambiar el union de `MonedaPresupuesto`. Estos cambios son aditivos.
  </action>
  <verify>
    <automated>pnpm --filter @ags/shared build 2>&1 | tail -20 && pnpm --filter sistema-modular type-check 2>&1 | tail -10</automated>
  </verify>
  <done>
- `'enviada'` presente en union
- LABELS + COLORS tienen entrada `enviada` (sin destruir las otras)
- `ordenesCompraIds` + `enviadaAt` declarados
- `pnpm --filter sistema-modular type-check` pasa — ningún consumer existente rompe (el union se extendió, no se narrow-eó)
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @ags/shared build` — sin errores
- `pnpm --filter sistema-modular type-check` — sin errores (el codebase consume el union en FacturacionList.tsx + SolicitarFacturaModal.tsx — extensión aditiva no rompe)
- Smoke: `grep -n "VentasMetadata\|'enviada'\|ordenesCompraIds\|enviadaAt" packages/shared/src/types/index.ts` devuelve las 5 referencias esperadas
</verification>

<success_criteria>
- 3 cambios aditivos en `@ags/shared`: VentasMetadata type + Presupuesto.ventasMetadata + SolicitudFacturacion extensions
- Build del package y type-check de sistema-modular pasan
- Ningún consumer existente rompe (es pura extensión)
- Commit: `feat(shared): extend types for Phase 10 — VentasMetadata + SolicitudFacturacion.enviada/ordenesCompraIds`
</success_criteria>

<output>
Crear `.planning/phases/10-presupuestos-partes-mixto-ventas/10-01-SUMMARY.md` listando:
- Campos exactos agregados + líneas donde quedaron
- Decisión JSDoc para downstream
- Confirmación de backward compat
</output>
