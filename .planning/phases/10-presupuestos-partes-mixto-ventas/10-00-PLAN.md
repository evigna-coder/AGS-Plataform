---
phase: 10-presupuestos-partes-mixto-ventas
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/sistema-modular/e2e/helpers/firestore-assert.ts
  - apps/sistema-modular/e2e/circuits/03-presupuestos.spec.ts
  - apps/sistema-modular/e2e/circuits/07-facturacion.spec.ts
  - apps/sistema-modular/e2e/circuits/11-full-business-cycle.spec.ts
  - apps/sistema-modular/e2e/circuits/14-exports.spec.ts
autonomous: true
requirements: [PTYP-02, PTYP-03, PTYP-04, FMT-03, FMT-04, FMT-05, FMT-06]

must_haves:
  truths:
    - "Specs RED exist for every Phase 10 requirement before implementation starts"
    - "firestore-assert helpers cover SolicitudFacturacion, Presupuesto, WorkOrder reads"
    - "Wave 0 specs fail with informative errors (not crashes) until impl lands"
    - "14-exports.spec.ts is a new standalone spec covering FMT-04/05/06 E2E download assertions"
  artifacts:
    - path: "apps/sistema-modular/e2e/helpers/firestore-assert.ts"
      provides: "getSolicitudFacturacion, getSolicitudesFacturacionByOt, getPresupuesto, getOTsByBudget helpers"
      contains: "export async function getSolicitudFacturacion"
    - path: "apps/sistema-modular/e2e/circuits/03-presupuestos.spec.ts"
      provides: "Tests 3.5 (partes), 3.6 (mixto), 3.7 (ventas + auto-OT), 3.8/3.9 (exports RBAC)"
      min_lines: 150
    - path: "apps/sistema-modular/e2e/circuits/07-facturacion.spec.ts"
      provides: "Tests 7.4 (auto-created doc visible) + 7.5 (marcar enviada action)"
      min_lines: 60
    - path: "apps/sistema-modular/e2e/circuits/11-full-business-cycle.spec.ts"
      provides: "New 11.13c — solicitudFacturacion assertion in same tx as mailQueue"
    - path: "apps/sistema-modular/e2e/circuits/14-exports.spec.ts"
      provides: "FMT-04/05/06 E2E download assertions (waitForEvent('download'))"
      min_lines: 80
  key_links:
    - from: "e2e/circuits/03-presupuestos.spec.ts"
      to: "e2e/helpers/firestore-assert.ts"
      via: "import { getPresupuesto, getOTsByBudget }"
      pattern: "from '\\.\\./helpers/firestore-assert'"
    - from: "e2e/circuits/11-full-business-cycle.spec.ts"
      to: "getSolicitudesFacturacionByOt"
      via: "pollUntil assertion at step 11.13b"
      pattern: "getSolicitudesFacturacionByOt"
---

<objective>
Wave 0 — Create RED baseline E2E specs + Firestore assertion helpers for every Phase 10 requirement BEFORE implementation lands. Specs fail informatively; Wave 1-5 turn them GREEN.

Purpose: Nyquist compliance — every requirement has automated verification command defined. Prevents "ship without tests" drift and gives Wave 1-5 executors a clear completion signal.
Output: Extended `firestore-assert.ts` + extensions to `03-presupuestos.spec.ts` + `07-facturacion.spec.ts` + `11-full-business-cycle.spec.ts` + new `14-exports.spec.ts`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-CONTEXT.md
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-RESEARCH.md
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-VALIDATION.md

<!-- Existing infrastructure we extend -->
@apps/sistema-modular/e2e/helpers/firestore-assert.ts
@apps/sistema-modular/e2e/circuits/03-presupuestos.spec.ts
@apps/sistema-modular/e2e/circuits/07-facturacion.spec.ts

<interfaces>
<!-- Helpers the new specs will import. Already present in firestore-assert.ts: -->

```typescript
// From apps/sistema-modular/e2e/helpers/firestore-assert.ts (existing):
export async function getPendingActions(presupuestoId: string): Promise<PendingAction[]>;
export async function getMailQueueDocs(opts: { type?, status?, limit? }): Promise<...>;
export async function getRequerimientosByPresupuesto(presupuestoId, opts?): Promise<RequerimientoCompra[]>;
export async function pollUntil<T>(fn, predicate, opts?): Promise<T>;
```

From @ags/shared (existing at packages/shared/src/types/index.ts:1280):
```typescript
export interface SolicitudFacturacion {
  id: string;
  presupuestoId: string;
  presupuestoNumero: string;
  clienteId: string;
  clienteNombre: string;
  items: FacturaItem[];
  montoTotal: number;
  moneda: MonedaPresupuesto;
  estado: SolicitudFacturacionEstado;  // 'pendiente' | 'facturada' | 'cobrada' | 'anulada' (Wave 1 adds 'enviada')
  otNumbers?: string[] | null;
  // ... more
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend firestore-assert.ts with Phase 10 helpers</name>
  <files>apps/sistema-modular/e2e/helpers/firestore-assert.ts</files>
  <action>
Add these new helper functions to `apps/sistema-modular/e2e/helpers/firestore-assert.ts` (append after existing `pollUntil`, keeping current imports + types):

1. **`getPresupuesto(id: string)`** — reads `presupuestos/{id}`, returns full doc (any-typed since no canonical PresupuestoDoc type in tests). Returns `null` if missing.

2. **`getSolicitudFacturacion(id: string)`** — reads `solicitudesFacturacion/{id}`, returns `SolicitudFacturacion & { id }` or `null`.

3. **`getSolicitudesFacturacionByOt(otNumber: string)`** — queries `solicitudesFacturacion where otNumbers array-contains otNumber`, returns array. Uses query pattern already present in getOCsByPresupuesto.

4. **`getSolicitudesFacturacionByPresupuesto(presupuestoId: string)`** — queries by `presupuestoId`, returns array.

5. **`getOTsByBudget(budgetNumber: string)`** — queries `reportes where budgets array-contains budgetNumber`, returns array. Collection name `reportes` (not `ordenesTrabajo`) per otService.ts:40 comment "usa la colección 'reportes' existente".

Import `SolicitudFacturacion` from `@ags/shared`. All functions match pattern of existing `getOCCliente`/`getOCsByPresupuesto` — use `getDoc`/`getDocs` + `query`+`where`+`array-contains`.

IMPORTANT: Do NOT import Wave 1 new fields (`ordenesCompraIds?`, `enviadaAt?`, estado `'enviada'`) — they don't exist yet. Use `any` cast where needed and add TODO comment "Wave 1 will tighten type once SolicitudFacturacion extension lands".

File growth: currently 193 lines → budget new helpers to stay under ~280 lines (it's a helpers file, not a component — 250-line budget is for React components only).
  </action>
  <verify>
    <automated>cd apps/sistema-modular && pnpm exec tsc --noEmit e2e/helpers/firestore-assert.ts 2>&1 | grep -v "Cannot find module '@ags" || true</automated>
  </verify>
  <done>5 new exported functions present; imports compile; no breaking changes to existing readers.</done>
</task>

<task type="auto">
  <name>Task 2: Extend 03-presupuestos.spec.ts + 07-facturacion.spec.ts + 11-full-business-cycle.spec.ts</name>
  <files>apps/sistema-modular/e2e/circuits/03-presupuestos.spec.ts, apps/sistema-modular/e2e/circuits/07-facturacion.spec.ts, apps/sistema-modular/e2e/circuits/11-full-business-cycle.spec.ts</files>
  <action>
**03-presupuestos.spec.ts** — extend existing `test.describe('Circuito 3: Presupuestos')` serial block (do NOT break existing 3.1-3.4). Append these tests AFTER 3.4:

- **3.5 — Crear ppto partes + agregar artículo stock + aceptar → requerimiento condicional generado**:
  - Abrir "+ Nuevo Presupuesto", seleccionar tipo `'partes'`, cliente, abrir AddItemModal. Escenario exige: ArticuloPickerPanel visible, seleccionar artículo (primer selector article que aparezca en el modal), verificar StockAmplioIndicator visible (4 buckets: disponible/reservado/en transito/comprometido). Agregar. Guardar presupuesto. Cambiar estado a `aceptado`. Luego `pollUntil(() => getRequerimientosByPresupuesto(pid, { condicional: true }), arr => arr.length >= 1, { timeout: 15000 })`. Si ArticuloPickerPanel no está rendered → test falla con "ArticuloPickerPanel not visible — Wave 2 not landed" (informative). NOTA: usar `presupuestosService.getAll` NO está disponible en test — derivar presupuestoId navegando al row de la lista luego del create y leyendo del URL.

- **3.6 — Crear ppto mixto + generar PDF + verificar 2 secciones en download**:
  - Smoke: crear ppto tipo `'mixto'`, agregar 1 concepto (servicio) + 1 artículo (partes). Disparar "Descargar PDF" (existe en PresupuestoHeaderBar). Usar Playwright `page.waitForEvent('download')` con timeout 10s. Salvar a temp path; verificar que existe. NO se hace pixel diff ni parse PDF — solo download ≠ crash. Marcar test como `test.fixme(reason)` si PDF branching por tipo no llegó (Wave 2 / 10-03) → GREEN cuando esté.

- **3.7 — Crear ppto ventas + completar ventasMetadata + aceptar → 1 OT auto-creada**:
  - Crear ppto tipo `'ventas'`. VentasMetadataSection debe estar visible; completar `fechaEstimadaEntrega` (date picker, hoy + 30 días), `lugarInstalacion` ("Lab Prueba"), check `requiereEntrenamiento`. Agregar 1 artículo. Aceptar. `pollUntil(() => getPresupuesto(pid), p => p?.ventasMetadata?.fechaEstimadaEntrega != null, { timeout: 10000 })`. Luego `pollUntil(() => getOTsByBudget(pres.numero), arr => arr.length >= 1, { timeout: 15000 })`.

- **3.8 — Botón "Exportar Excel" + "Exportar PDF" visibles en /presupuestos para rol admin**:
  - `await expect(page.getByRole('button', { name: /exportar excel/i })).toBeVisible({ timeout: 5000 })`. Idem PDF.

- **3.9 — Botón Export oculto para rol no-admin** (`test.fixme` si test-base no tiene fixture de rol; de lo contrario implementar logout + login con `ventas` role).

**07-facturacion.spec.ts** — extend existing `Circuito 7` serial block:
- **7.4 — Dashboard lista doc auto-creado**: Luego de haberse ejecutado 11.13/11.13b (o standalone — buscar cualquier solicitud con `estado === 'pendiente'` via `getSolicitudesFacturacionByOt`). Navegar a /facturacion. Assert `tbody tr` count ≥ 1 y que al menos una row tiene estado "Pendiente".
- **7.5 — Acción "marcar enviada" cambia estado**: Abrir row-detail (o row menu); click button "Marcar enviada"; `pollUntil(() => getSolicitudFacturacion(id), s => s?.estado === 'enviada', { timeout: 10000 })`. `test.fixme` inicial — Wave 5 lo desfixmeará.

**11-full-business-cycle.spec.ts** — extend 11.13b (lines 396-413):
- Después del assert de `mailQueue`, agregar assert idempotent: `pollUntil(() => getSolicitudesFacturacionByOt(otNumber), docs => docs.length >= 1, { timeout: 15000 })`. Chequear que `docs[0].estado === 'pendiente'`, `docs[0].presupuestoId` match, `docs[0].otNumbers` contiene el otNumber. Inicial: `test.fixme(reason)` — Wave 3 (10-04) lo desfixmeará.

Todos los tests nuevos deben usar `TEST_PREFIX` para nombres de cliente/artículo si crean data. Formato codegen-style (primer click sin role-filter si el selector no es único) como el resto del spec.

Los tests RED deben fallar **informativos**: `expect(..., 'Requires Wave 2 implementation').toBeTruthy()` en lugar de crashes silenciosos.

Budget: cada spec ~150 líneas máx. Si crece más, extraer helper local dentro del spec (no a helpers/ — específico del circuito).
  </action>
  <verify>
    <automated>cd apps/sistema-modular && pnpm exec playwright test e2e/circuits/03-presupuestos.spec.ts --list 2>&1 | grep -E "3\.[5-9]" | wc -l</automated>
  </verify>
  <done>5 new tests listed in 03-presupuestos; 2 new in 07-facturacion; 11.13b extended. All RED or fixme, documented reason.</done>
</task>

<task type="auto">
  <name>Task 3: Create new 14-exports.spec.ts</name>
  <files>apps/sistema-modular/e2e/circuits/14-exports.spec.ts</files>
  <action>
Create NEW file `apps/sistema-modular/e2e/circuits/14-exports.spec.ts` modelado en `03-presupuestos.spec.ts` (mismo header imports con `test`, `expect`, `TEST_PREFIX`, `timestamp`).

Estructura:

```ts
import { test, expect, TEST_PREFIX, timestamp } from '../fixtures/test-base';
const ts = timestamp();

test.describe('Circuito 14: Exports XLSX + PDF (FMT-04/05/06)', () => {
  test.describe.configure({ mode: 'serial' });

  test('14.1 — /presupuestos: Exportar Excel descarga archivo', async ({ app, nav }) => {
    await nav.goToFresh('Presupuestos');
    await app.waitForTimeout(2000);
    const downloadPromise = app.waitForEvent('download', { timeout: 15_000 });
    await app.getByRole('button', { name: /exportar excel/i }).click();
    const dl = await downloadPromise;
    expect(dl.suggestedFilename()).toMatch(/presupuestos.*\.xlsx$/i);
  });

  test('14.2 — /presupuestos: Exportar PDF descarga archivo', ...);

  test('14.3 — /presupuestos: Export filter-aware — aplicar filtro cliente, exportar, verificar menos rows',
    async ({ app, nav }) => {
      // Seleccionar un cliente con al menos 1 presupuesto; exportar; leer xlsx;
      // verificar count <= el count total (sin filter).
      // Si no se tiene toolkit parse xlsx → smoke: solo assert download filename contiene slug del filter
      // (ej: 'presupuestos_cliente-TEST_...').
    });

  test('14.4 — /facturacion: Exportar Excel + PDF solicitudes (FMT-06)', ...);

  test('14.5 — OCs pendientes: Exportar Excel + PDF (FMT-05)', ...);
    // Cómo accedemos a "OCs pendientes": el user decide en 10-05 si es view/filter en /presupuestos
    // o subpage. Por ahora, `test.fixme(reason)` — Wave 4 impl landea.

  test('14.6 — Export buttons ocultos para rol no-admin', ...);
    // test.fixme si no hay fixture de rol; de lo contrario impl real.
});
```

Use `test.fixme('reason')` donde Wave 4 no landea aún. Todos deben listarse al correr `--list`.

Budget: ~120 líneas.
  </action>
  <verify>
    <automated>cd apps/sistema-modular && pnpm exec playwright test e2e/circuits/14-exports.spec.ts --list 2>&1 | grep -c "14\."</automated>
    <manual>
**A13 self-check — RED-informative verification:**
Run `pnpm e2e -- 14-exports 03-presupuestos 11-full-business-cycle --list` to confirm new tests appear in the listing.
Then manually run 1-2 RED tests (e.g., `pnpm e2e -- -g "3\.5"` and `pnpm e2e -- -g "14\.1"`) y confirmá que fallen con mensaje informativo (no crash).
Expected failure messages should reference the Wave that will make them GREEN (e.g., "Requires Wave 2 implementation" or "Wave 4 not landed").
NOT expected: generic "TypeError: cannot read property of undefined" or timeout with no context.
    </manual>
  </verify>
  <done>New spec file exists with 6 tests listed; all RED or fixme with clear reason; RED tests fail with informative messages (verified manually per A13 self-check).</done>
</task>

</tasks>

<verification>
- Type-check pasa: `cd apps/sistema-modular && pnpm exec tsc --noEmit e2e/helpers/firestore-assert.ts` (ignore `@ags/shared` missing resolution — it's a test-runtime alias)
- `pnpm --filter sistema-modular exec playwright test --list` lista los ~18 tests total (originales + nuevos)
- Los tests RED fallan con mensajes informativos, NO con crashes genéricos (A13 verified)
</verification>

<success_criteria>
- `firestore-assert.ts` exporta 5 nuevas funciones sin breaking change a las existentes
- `03-presupuestos.spec.ts` tiene 9 tests (3.1-3.9)
- `07-facturacion.spec.ts` tiene 5 tests (7.1-7.5)
- `11-full-business-cycle.spec.ts:11.13b` extendido con assert sobre `solicitudesFacturacion`
- `14-exports.spec.ts` es un archivo nuevo con 6 tests
- Todos los tests corren vía Playwright CLI; ninguno bloquea suite existente
- RED self-check pasó (A13): 1-2 tests probados manualmente fallan con mensaje informativo
- Commits con mensaje `test(10): wave 0 specs RED baseline for PTYP-02/03/04 + FMT-03/04/05/06`
</success_criteria>

<output>
After completion, create `.planning/phases/10-presupuestos-partes-mixto-ventas/10-00-SUMMARY.md` con:
- Tests agregados por archivo
- Helpers nuevos en firestore-assert
- Link a test file paths
- Notas sobre fixme markers + qué wave los desfixmea
- Resultado del A13 self-check (muestra de RED messages verificados)
</output>
</content>
</invoke>