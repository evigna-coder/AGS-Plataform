---
phase: 10-presupuestos-partes-mixto-ventas
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - apps/sistema-modular/src/components/presupuestos/pdf/PresupuestoPDFEstandar.tsx
autonomous: false
requirements: [PTYP-02, PTYP-03, PTYP-04]

must_haves:
  truths:
    - "PDF de ppto mixto renderea 2 secciones: 'Servicios' (items con conceptoServicioId) + 'Partes' (items con stockArticuloId) + subtotal por sección"
    - "PDF de ppto partes usa la misma estructura mixto pero OCULTA la sección 'Servicios' si está vacía"
    - "PDF de ppto ventas inserta un bloque 'Datos de entrega e instalación' (fechaEstimadaEntrega, lugarInstalacion, requiereEntrenamiento) ANTES del detalle de items"
    - "PDF de ppto servicio y contrato NO cambian su rendering — backward compat total"
    - "Total general al final sigue igual (la agrupación es presentacional, no cambia cálculos)"
    - "Checkpoint de verificación visual pasa: PDFs se abren sin crash y los 3 tipos se ven distintos"
  artifacts:
    - path: "apps/sistema-modular/src/components/presupuestos/pdf/PresupuestoPDFEstandar.tsx"
      provides: "Branching interno por presupuesto.tipo; sub-componentes MixtoItemsBlock + VentasMetadataBlock"
  key_links:
    - from: "PresupuestoPDFEstandar.tsx"
      to: "Presupuesto.tipo"
      via: "switch-like branching dentro del body del Page"
      pattern: "presupuesto\\.tipo === 'mixto'\\|'partes'\\|'ventas'"
    - from: "PresupuestoPDFEstandar.tsx"
      to: "Presupuesto.ventasMetadata"
      via: "rendering condicional del bloque instalación"
      pattern: "presupuesto\\.ventasMetadata"
---

<objective>
Wave 2 (parallel to 10-02) — PDF template unico con branching interno por `tipo`. Reemplaza múltiples generators distintos con un solo archivo con switch interno. Mínima superficie de cambio.

Purpose: FMT-01 ya se cerró para 'servicio' (Phase 7). Este plan extiende a los 3 tipos faltantes manteniendo la regla "un template = un lugar para fixes futuros". Runs en paralelo a 10-02 (editor UI) porque toca otro archivo.
Output: `PresupuestoPDFEstandar.tsx` extendido con 2 sub-componentes nuevos (MixtoItemsBlock, VentasMetadataBlock) + branching en el body del Page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-CONTEXT.md
@.planning/phases/10-presupuestos-partes-mixto-ventas/10-RESEARCH.md

@apps/sistema-modular/src/components/presupuestos/pdf/PresupuestoPDFEstandar.tsx
@apps/sistema-modular/src/components/presupuestos/pdf/pdfStyles.ts
@apps/sistema-modular/src/components/presupuestos/pdf/pdfUtils.ts

<interfaces>
<!-- Current PDF template shape. 473 líneas, usa @react-pdf/renderer. -->

From PresupuestoPDFEstandar (existing — PresupuestoPDFData):
```typescript
export interface PresupuestoPDFData {
  presupuesto: Presupuesto;  // incluye tipo + items + ventasMetadata (post-Wave 10-01)
  cliente: Cliente | null;
  establecimiento: Establecimiento | null;
  contacto: ContactoEstablecimiento | null;
  condicionPago: CondicionPago | null;
  categorias: CategoriaPresupuesto[];
  montoEnLetras: string;
  logoSrc: string;
  isoLogoSrc: string;
  impuestos: { iva21, iva105, ganancias, iibb };
  modulosBySistema?: Record<string, ModuloSistema[]>;
  totalsByCurrency?: Record<string, number>;
}

export function PresupuestoPDFEstandar({ data }: { data: PresupuestoPDFData }): JSX.Element;
```

From @ags/shared (post Wave 10-01):
```typescript
interface Presupuesto {
  tipo: TipoPresupuesto;  // 'servicio' | 'partes' | 'ventas' | 'contrato' | 'mixto'
  items: PresupuestoItem[];
  ventasMetadata?: VentasMetadata | null;
  // ...
}

interface PresupuestoItem {
  conceptoServicioId?: string | null;  // → clasifica como "Servicio"
  stockArticuloId?: string | null;     // → clasifica como "Parte"
  // si ambos null → servicio manual (clasifica como "Servicio" por default)
  // ...
}

interface VentasMetadata {
  fechaEstimadaEntrega?: string | null;
  lugarInstalacion?: string | null;
  requiereEntrenamiento?: boolean;
}
```

Pattern existente en el file — `agruparPorSistemaSimple` util para agrupar items. Estilos ya establecidos: `baseStyles` (S) + `COLORS`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar helper splitItemsByTipo + sub-componentes MixtoItemsBlock y VentasMetadataBlock</name>
  <files>apps/sistema-modular/src/components/presupuestos/pdf/PresupuestoPDFEstandar.tsx</files>
  <action>
En `apps/sistema-modular/src/components/presupuestos/pdf/PresupuestoPDFEstandar.tsx`:

**1. Helper `splitItemsByTipo` (agregar cerca de los otros helpers al top del file, después de `formatDate`):**

```typescript
/**
 * Phase 10: clasifica items de un ppto mixto/partes para rendering en 2 secciones.
 * Reglas:
 *   - Con `stockArticuloId` (no-null)    → 'partes'
 *   - Con `conceptoServicioId` (no-null) → 'servicios'
 *   - Sin ninguno (carga manual texto)   → 'servicios' (default, los partes tienen stock siempre)
 */
function splitItemsByTipo(items: PresupuestoItem[]): { servicios: PresupuestoItem[]; partes: PresupuestoItem[] } {
  const servicios: PresupuestoItem[] = [];
  const partes: PresupuestoItem[] = [];
  for (const it of items) {
    if (it.stockArticuloId) partes.push(it);
    else servicios.push(it);
  }
  return { servicios, partes };
}

function sumSubtotal(items: PresupuestoItem[]): number {
  return items.reduce((acc, it) => acc + (it.subtotal || 0), 0);
}
```

**2. Sub-componente `MixtoItemsBlock` (agregar antes del `export function PresupuestoPDFEstandar` principal):**

```tsx
/**
 * Phase 10 — Renderea items en 2 secciones con headers + subtotales.
 * Oculta la sección 'Servicios' si está vacía (caso partes puro).
 * Reusa los estilos de tabla del existing items-table de PresupuestoPDFEstandar.
 */
function MixtoItemsBlock({ items, moneda }: { items: PresupuestoItem[]; moneda: string }) {
  const { servicios, partes } = splitItemsByTipo(items);
  const sym = (moneda || 'USD');  // usar abrev, no símbolo (en PDF el helper existente ya lo resuelve)
  return (
    <View>
      {servicios.length > 0 && (
        <View style={{ marginBottom: 10 }}>
          <Text style={{ fontSize: 10, fontWeight: 'bold', color: COLORS.primary, marginBottom: 4 }}>
            Servicios
          </Text>
          {/* REUSAR la tabla estándar — llamar al mismo row-render que el existing. */}
          {/* IMPORTANTE: mirar el rendering actual de items en PresupuestoPDFEstandar (líneas ~200-300) y extraer la función que pinta una fila para reusar aquí. */}
          <ItemsTable items={servicios} moneda={moneda} />
          <Text style={{ fontSize: 9, textAlign: 'right', marginTop: 2, fontWeight: 'bold' }}>
            Subtotal servicios: {sym} {sumSubtotal(servicios).toLocaleString('es-AR', { minimumFractionDigits: 2 })}
          </Text>
        </View>
      )}
      {partes.length > 0 && (
        <View style={{ marginBottom: 10 }}>
          <Text style={{ fontSize: 10, fontWeight: 'bold', color: COLORS.primary, marginBottom: 4 }}>
            Partes
          </Text>
          <ItemsTable items={partes} moneda={moneda} />
          <Text style={{ fontSize: 9, textAlign: 'right', marginTop: 2, fontWeight: 'bold' }}>
            Subtotal partes: {sym} {sumSubtotal(partes).toLocaleString('es-AR', { minimumFractionDigits: 2 })}
          </Text>
        </View>
      )}
    </View>
  );
}
```

**ACCIÓN REQUERIDA previa:** lee el file completo para identificar la tabla de items existente (renderea encabezados + filas). Extrae esa porción a un sub-componente interno `ItemsTable({ items, moneda })` reusable — tanto el rendering "clásico" (todos los tipos) como MixtoItemsBlock usan la misma tabla subyacente.

Si la tabla actual está acoplada a la estructura completa de `PresupuestoPDFEstandar` (ej. usa `agruparPorSistemaSimple` internamente), crear `ItemsTable` como wrapper mínimo que llama a las mismas primitives pero sin la agrupación por sistema (la agrupación se usa solo en contrato). Para mixto/partes/ventas, la tabla es una lista flat.

**3. Sub-componente `VentasMetadataBlock`:**

```tsx
/**
 * Phase 10 — Bloque "Datos de entrega e instalación" para ppto tipo 'ventas'.
 * Se inserta ANTES del detalle de items.
 */
function VentasMetadataBlock({ metadata }: { metadata: VentasMetadata }) {
  const hasAny = metadata.fechaEstimadaEntrega || metadata.lugarInstalacion || metadata.requiereEntrenamiento;
  if (!hasAny) return null;
  const fechaStr = metadata.fechaEstimadaEntrega
    ? formatDate(metadata.fechaEstimadaEntrega)
    : '—';
  return (
    <View style={{ marginTop: 8, marginBottom: 10, padding: 6, borderWidth: 1, borderColor: COLORS.primary, borderStyle: 'solid' }}>
      <Text style={{ fontSize: 10, fontWeight: 'bold', color: COLORS.primary, marginBottom: 3 }}>
        Datos de entrega e instalación
      </Text>
      <View style={{ flexDirection: 'row', gap: 12 }}>
        <Text style={{ fontSize: 9 }}><Text style={{ fontWeight: 'bold' }}>Fecha estimada: </Text>{fechaStr}</Text>
        <Text style={{ fontSize: 9 }}><Text style={{ fontWeight: 'bold' }}>Lugar: </Text>{metadata.lugarInstalacion || '—'}</Text>
      </View>
      <Text style={{ fontSize: 9, marginTop: 2 }}>
        <Text style={{ fontWeight: 'bold' }}>Entrenamiento post-instalación: </Text>
        {metadata.requiereEntrenamiento ? 'Sí' : 'No'}
      </Text>
    </View>
  );
}
```

Import `VentasMetadata` type desde `@ags/shared` al top del file.
  </action>
  <verify>
    <automated>pnpm --filter sistema-modular type-check 2>&1 | tail -15</automated>
  </verify>
  <done>2 sub-componentes + 2 helpers definidos; imports correctos; type-check pasa. Los sub-componentes compilan aunque no se usen aún.</done>
</task>

<task type="auto">
  <name>Task 2: Branching interno en el body del Page para wire-up de los nuevos sub-componentes</name>
  <files>apps/sistema-modular/src/components/presupuestos/pdf/PresupuestoPDFEstandar.tsx</files>
  <action>
Mismo archivo. Localizar el body del componente principal `PresupuestoPDFEstandar` (línea 388 onwards) donde se renderea la tabla de items.

**Estructura target:**

```tsx
// dentro del return del Page, donde hoy va la tabla de items:
{data.presupuesto.tipo === 'ventas' && data.presupuesto.ventasMetadata && (
  <VentasMetadataBlock metadata={data.presupuesto.ventasMetadata} />
)}

{(data.presupuesto.tipo === 'mixto' || data.presupuesto.tipo === 'partes') ? (
  <MixtoItemsBlock items={data.presupuesto.items} moneda={data.presupuesto.moneda} />
) : (
  /* renderer existente (servicio, contrato, ventas caen aquí) — no tocar */
  <ExistingItemsRender ... />
)}
```

Detalles importantes:
- El renderer existente puede estar inlined en el return — si es así, encapsularlo en un sub-componente interno `DefaultItemsBlock` (extracción pura, mismo output). Esto hace el switch legible.
- **Ventas:** sigue usando el renderer default (tabla flat plana). NO envolver en MixtoItemsBlock — ventas es un solo bloque de items, lo especial es SOLO el `VentasMetadataBlock` antes.
- **Contrato:** no aplica a este file (contrato usa otro rendering o branching distinto con `hasGrupos` — verificar con grep `agruparPorSistemaSimple` en el file para confirmar).
- **Servicio:** sin cambios — cae al renderer default.

Después del cambio:
- `pnpm --filter sistema-modular type-check` pasa
- Abrir un ppto tipo `servicio` existente y descargar PDF → renderea igual que antes
- Crear ppto mixto con items mezclados → PDF muestra 2 secciones
- Crear ppto ventas con ventasMetadata → PDF muestra bloque antes de items
  </action>
  <verify>
    <automated>pnpm --filter sistema-modular type-check 2>&1 | tail -10</automated>
  </verify>
  <done>Body del Page con branching por tipo; renderer servicio/contrato sin cambios; type-check pasa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual manual de PDFs por tipo</name>
  <what-built>
    PDF template con branching interno. Sin cambios en servicio/contrato; nuevos bloques en mixto/partes/ventas.
  </what-built>
  <how-to-verify>
    1. `pnpm dev:modular` (si no está corriendo)
    2. Abrir un presupuesto existente tipo `servicio` → descargar PDF → verificar que se ve IGUAL que antes de este plan (baseline)
    3. Crear ppto tipo `mixto` con 2 conceptos de servicio + 2 artículos de stock (usa el ArticuloPickerPanel de 10-02) → descargar PDF → verificar 2 secciones con headers "Servicios" + "Partes" con subtotales independientes
    4. Crear ppto tipo `partes` con SOLO artículos de stock → descargar PDF → verificar que la sección "Servicios" NO aparece (hide-if-empty) y solo se ve "Partes"
    5. Crear ppto tipo `ventas` + completar ventasMetadata (via VentasMetadataSection de 10-02) + agregar artículos → descargar PDF → verificar bloque "Datos de entrega e instalación" visible ANTES del detalle de items, con los 3 campos llenos
    6. Verificar que el TOTAL GENERAL al final de cada PDF sigue siendo la suma correcta (subtotal servicios + subtotal partes + impuestos == total)

    Si cualquier tipo rompe el rendering o el total, rollback + reportar.
  </how-to-verify>
  <resume-signal>
    Type "approved" si los 4 PDFs pasaron inspección visual.
    Type "issues: [descripción]" si hay bug específico (ej: "subtotal partes no incluye descuento").
  </resume-signal>
</task>

</tasks>

<verification>
- Type-check pasa
- PDFs de los 4 tipos (servicio / partes / mixto / ventas) se abren sin crash
- Human verification checkpoint aprobado
- NO se toca contrato (backward compat crítico — Fase 8 landó esto)
</verification>

<success_criteria>
- Template único con branching interno implementado
- Sub-componentes MixtoItemsBlock + VentasMetadataBlock + ItemsTable extracted
- Sin regresión en servicio/contrato
- Human approval obtenido via checkpoint
- Commit: `feat(10-03): PDF branching interno por tipo de presupuesto`
</success_criteria>

<output>
Crear `.planning/phases/10-presupuestos-partes-mixto-ventas/10-03-SUMMARY.md` con:
- Sub-componentes creados
- Estrategia de branching
- Resultado del checkpoint human-verify
- Screenshots (si aplicable)
</output>
