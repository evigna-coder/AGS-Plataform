/**
 * Placeholder — plan 08-05 implementa el dashboard real de acciones pendientes
 * (lista agregada de todas las pendingActions con resolvedAt === null + filtros + retry).
 */
export default function AccionesPendientesPage() {
  return (
    <div className="p-8">
      <h1 className="font-serif text-2xl mb-2">Acciones Pendientes</h1>
      <p className="text-slate-500">Plan 08-05 implementa este dashboard.</p>
    </div>
  );
}
